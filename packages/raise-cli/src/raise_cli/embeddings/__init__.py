"""raise_cli.embeddings — foundation-tier embedding infrastructure (T5).

RAISE-16457: onnx_provider relocated here from raise_cli.cartridges so that
all consumers (graph, memory, cli, eval) use legal downward or intra-tier imports.
Mirrors raise_server.embeddings naming convention (ADR-139).
"""
