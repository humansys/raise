WHAT:      MCP Jira cannot create issues in JSM project RS — permission denied
WHEN:      jira_create_issue with project_key=RS via MCP
WHERE:     External — Jira JSM project permissions, not CLI code
EXPECTED:  Token has permissions or limitation documented
Done when: Token fixed or documented as known limitation

TRIAGE:
  Bug Type:    Configuration
  Severity:    S3-Low
  Origin:      Environment
  Qualifier:   Missing

STATUS: Still valid but not a code bug. External permission config for JSM project.
