WHAT:      `doctor()` callback en cli/commands/doctor.py tiene cognitive complexity 47 (Sonar S3776)
WHEN:      Siempre — análisis estático, no depende de runtime
WHERE:     src/raise_cli/cli/commands/doctor.py:40
EXPECTED:  Función con complexity ≤ 15; cada concern en su propia sub-función
Done when: Sonar S3776 no flaggea doctor.py:40; todos los tests existentes pasan
