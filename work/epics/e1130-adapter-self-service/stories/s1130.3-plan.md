---
story_id: "S1130.3"
title: "Adapter Doctor Check — Plan"
size: M
date: "2026-04-01"
---

# S1130.3 Implementation Plan

## Overview

Single `AdapterDoctorCheck` class implementing `DoctorCheck` Protocol. Checks both Jira and Confluence adapters in three levels: config exists → env vars set → live health. Follows pattern from `EnvironmentCheck`.

## Tasks

### T1: Config existence checks (S)

**RED:** Test that missing config files produce WARN results with fix hints.

**GREEN:** Implement `AdapterDoctorCheck` with config file existence checks for both `.raise/jira.yaml` and `.raise/confluence.yaml`.

**REFACTOR:** Clean up.

- **Files:** `doctor/checks/adapters.py` (create), `tests/doctor/checks/test_adapters.py` (create)
- **AC:** Scenarios 1, 5
- **Tests:** missing jira config → WARN, missing confluence config → WARN, both present → no WARN, fix hints correct
- **Verify:** `uv run pytest --tb=short`, `uv run ruff check src/ tests/`, `uv run ruff format --check src/ tests/`, `uv run pyright`

### T2: Env var checks (S)

**RED:** Test that missing env vars produce ERROR results with specific var names.

**GREEN:** Add env var validation: check `JIRA_API_TOKEN`/`JIRA_EMAIL` and `CONFLUENCE_API_TOKEN`/`CONFLUENCE_USERNAME` when respective configs exist.

**REFACTOR:** Extract common pattern.

- **Files:** `doctor/checks/adapters.py`, `tests/doctor/checks/test_adapters.py`
- **AC:** Scenario 2
- **Tests:** missing token → ERROR, missing username → ERROR, all present → PASS, instance-specific vars checked
- **Verify:** same gates
- **Depends on:** T1

### T3: Live health checks + entry point (S)

**RED:** Test that online mode checks backend connectivity.

**GREEN:** Add health check using `JiraClient.health()` and `ConfluenceClient.health()`. Only run when `context.online=True`. Register in pyproject.toml.

**REFACTOR:** Final cleanup.

- **Files:** `doctor/checks/adapters.py`, `tests/doctor/checks/test_adapters.py`, `pyproject.toml`
- **AC:** Scenarios 3, 4
- **Tests:** healthy backend → PASS, unhealthy → ERROR, offline mode skips live checks
- **Verify:** same gates + `rai doctor` manual test
- **Depends on:** T2

## Execution Order

```
T1 (config exists) → T2 (env vars) → T3 (live health + entry point)
```

Sequential — each level builds on the previous.

## Risks

| Risk | Mitigation |
|------|------------|
| Live health check imports fail when extras not installed | Guard with try/except ImportError, report as WARN |
| Env var check logic differs per adapter | Use adapter-specific config loaders to determine expected vars |

## Duration Tracking

| Task | Estimated | Actual | Notes |
|------|-----------|--------|-------|
| T1 | 10 min | | |
| T2 | 10 min | | |
| T3 | 10 min | | |
| **Total** | **30 min** | | |
