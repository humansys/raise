---
story_id: "S1130.1"
title: "Confluence Discovery Service — Plan"
size: S
tasks: 2
---

# S1130.1 Implementation Plan

## Task 1: ConfluenceSpaceMap model + ConfluenceDiscovery service (S)

**TDD: RED → GREEN → REFACTOR**

RED — Write tests first:
- `test_discover_all_spaces` — mocked client returns 2 spaces, verify ConfluenceSpaceMap
- `test_discover_specific_space` — filtered by space_key, verify top_level_pages populated
- `test_discover_space_not_found` — space_key not in results → ConfluenceNotFoundError
- `test_discover_auth_failure` — client raises ConfluenceAuthError → propagated

GREEN — Implement:
- `ConfluenceSpaceMap` Pydantic model (frozen=True): `spaces: list[SpaceInfo]`, `top_level_pages: dict[str, list[PageSummary]]`
- `ConfluenceDiscovery.__init__(client: ConfluenceClient)`
- `ConfluenceDiscovery.discover(space_key: str | None = None) → ConfluenceSpaceMap`

REFACTOR — Clean up if needed.

**Files:**
- Create: `packages/raise-cli/src/raise_cli/adapters/confluence_discovery.py`
- Create: `packages/raise-cli/tests/adapters/test_confluence_discovery.py`

**AC:** Scenarios 1-4 from story.md

**Verify:**
```bash
uv run pytest packages/raise-cli/tests/adapters/test_confluence_discovery.py --tb=short
uv run pyright
uv run ruff check src/ tests/ && uv run ruff format --check src/ tests/
```

## Task 2: Integration verification + commit

**Manual verification:**
- Import from top-level: `from raise_cli.adapters.confluence_discovery import ConfluenceDiscovery, ConfluenceSpaceMap`
- Full test suite passes (no regressions)
- Type check clean
- Lint clean

**Verify:**
```bash
uv run pytest --tb=short
uv run pyright
uv run ruff check src/ tests/ && uv run ruff format --check src/ tests/
```

## Execution Order

```
T1 (model + service + tests) → T2 (integration verify)
```

Linear — T2 validates T1 against the full project.

## Risks

| Risk | Mitigation |
|------|------------|
| `get_page_children` returns unexpected format | Mock tests cover shape; existing client already normalizes to `PageSummary` |

## Duration Tracking

| Task | Estimate | Actual | Status |
|------|----------|--------|--------|
| T1 | ~20 min | | pending |
| T2 | ~5 min | | pending |
