---
story_id: "S11.4"
jira_key: "RAISE-675"
size: "M"
planned: "2026-03-24"
tasks: 5
---

# S11.4 Implementation Plan

## Overview

Integrate rai-agent product code into raise-commons monorepo. Primarily file copy +
configuration — no new business logic. Risk-first: resolve workspace deps before
copying source, copy source before tests.

## Tasks

### T1: Create rai-agent pyproject.toml + wire workspace (RISKIEST)
**Size:** S | **Deps:** none | **AC:** AC1

Create `packages/rai-agent/pyproject.toml` with base deps, extras, hatch config.
Add rai-agent to root workspace sources and dev dependency group. Create minimal
`src/rai_agent/__init__.py` to make workspace resolvable.

**Files:**
- Create: `packages/rai-agent/pyproject.toml`
- Create: `packages/rai-agent/src/rai_agent/__init__.py` (minimal placeholder)
- Modify: root `pyproject.toml` (add rai-agent to sources + dev group)

**Verification:**
```bash
uv sync
uv run python -c "import rai_agent; print('OK')"
```

**Why first:** If deps conflict (especially claude-agent-sdk==0.1.48 pin), we learn
immediately before copying 78 files. Smallest blast radius.

---

### T2: Copy product source files (33 files)
**Size:** M | **Deps:** T1 | **AC:** AC5

Copy all product files from rai personal repo per manifest. Preserve directory
structure: daemon/ (21), knowledge/ (10), __init__.py, inference.py.

**Files:**
- Copy: `src/rai_agent/` from `/home/emilio/Code/rai/src/rai_agent/` (33 files per manifest)
- Exclude: `scaleup/` directory

**Verification:**
```bash
uv sync
uv run python -c "from rai_agent.daemon.app import create_app; print('OK')"
uv run python -c "from rai_agent.knowledge.retrieval.engine import RetrievalEngine; print('OK')"
```

**Why second:** Source must be in place before tests can import it. Cross-package
import (raise_core.graph) verified here.

---

### T3: Copy test files (45 files) + verify
**Size:** M | **Deps:** T2 | **AC:** AC2, AC3

Copy all test files from rai personal repo per manifest. Update root pyproject.toml
to add rai-agent testpaths, coverage sources.

**Files:**
- Copy: `tests/` from `/home/emilio/Code/rai/tests/` → `packages/rai-agent/tests/` (45 files per manifest)
- Exclude: any scaleup tests
- Modify: root `pyproject.toml` (pytest testpaths, coverage sources)

**Verification:**
```bash
uv run pytest packages/rai-agent/tests/ --tb=short
uv run pytest --tb=short  # all packages, no regressions
```

**Why third:** Tests depend on source (T2) and resolvable deps (T1).

---

### T4: Update tool configs + sync-github.sh
**Size:** S | **Deps:** T2 | **AC:** AC6, AC7

Update remaining tool configs for rai-agent paths. Add rai-agent to GitHub mirror
exclusion list.

**Files:**
- Modify: root `pyproject.toml` (pyright include, ruff src)
- Modify: `scripts/sync-github.sh` (add packages/rai-agent to exclusions)

**Verification:**
```bash
uv run ruff check .
uv run pyright
```

**Why parallel with T3:** Independent of test files. Only needs source (T2).

---

### T5: Final verification — all gates + wheel build
**Size:** XS | **Deps:** T3, T4 | **AC:** all

End-to-end verification:
1. `uv sync` — workspace resolves
2. `uv run pytest` — all tests pass
3. `uv run ruff check .` — lint clean
4. `uv run pyright` — type check clean
5. `uv build --package rai-agent` — wheel contains only rai_agent/

**Files:** none (verification only)

---

## Execution Order

```
T1 (pyproject + workspace) → T2 (copy source)
                                  │
                                  ├→ T3 (copy tests + verify) ─┐
                                  │                              ├→ T5 (final)
                                  └→ T4 (tool configs + sync) ──┘
```

**Rationale:** Risk-first. T1 catches dep conflicts early. T2 validates cross-package
imports. T3 and T4 are independent of each other. T5 gates everything.

## Risks

| Risk | Task | Mitigation |
|---|---|---|
| claude-agent-sdk==0.1.48 causes resolution conflict | T1 | Verify immediately; relax pin if needed |
| Integration tests fail without external services | T3 | Mark with @pytest.mark.integration per D4 |
| pyright errors on rai-agent code (different style) | T4 | Fix or add targeted ignores |
| Wheel contains cross-package code | T5 | Verify with zipfile listing |

## Duration Tracking

| Task | Estimated | Actual | Status |
|---|---|---|---|
| T1 | 15 min | — | pending |
| T2 | 20 min | — | pending |
| T3 | 25 min | — | pending |
| T4 | 15 min | — | pending |
| T5 | 10 min | — | pending |
| **Total** | **~1h 25m** | — | — |
