---
story_id: "S11.6"
jira_key: "RAISE-677"
---

# S11.6 Scope

## In Scope

- Create `packages/rai-agent/Dockerfile` for self-hosting
- Update `docker-compose.yml` to include rai-agent service
- Update `README.md` to reflect monorepo with 5 packages
- Verify `uv build --package rai-agent` produces correct wheel

## Out of Scope

- Kubernetes manifests or Helm charts
- CI/CD changes (done in S11.5 / RAISE-676)
- PyPI publishing (manual Trusted Publisher setup, not code)
- Personal instance cleanup (S11.6 scope.md / RAISE-678)
- Changes to CONTRIBUTING.md, AGENTS.md, LICENSE (already correct)

## Done Criteria

1. `packages/rai-agent/Dockerfile` exists and builds successfully
2. `docker compose config` validates with rai-agent service
3. `uv build --package rai-agent` produces wheel with only rai_agent/
4. README.md documents the 5-package workspace structure
5. All existing tests still pass
