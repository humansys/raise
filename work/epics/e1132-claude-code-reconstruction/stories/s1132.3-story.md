# S1132.3: Wave 2 — Agent Infrastructure Deep Dive

**Jira:** RAISE-1165
**Epic:** E1132 (RAISE-1162) — Claude Code Architecture Reconstruction
**Size:** M | **Type:** analysis

---

## User Story

**As a** RaiSE framework developer,
**I want** deep understanding of Claude Code's agent infrastructure (coordinator, tasks, query, state),
**So that** I can design E3 (scaleup-agent) to correctly orchestrate multi-agent workflows on top of CC.

---

## Acceptance Criteria

```gherkin
Feature: Agent infrastructure deep dive

  Scenario: Coordinator model documented
    Given the coordinator/ module (1 file, 19K)
    When I trace multi-agent coordination
    Then I produce a finding documenting how agents are spawned, communicate, and share context

  Scenario: Task system documented
    Given the tasks/ module (12 files, 320K)
    When I trace task lifecycle
    Then I produce a finding documenting task creation, persistence, coordination between agents

  Scenario: Query pipeline documented
    Given the query/ module + QueryEngine.ts + query.ts
    When I trace the query lifecycle
    Then I produce a finding documenting how the LLM is called and results processed

  Scenario: State management documented
    Given the state/ module (6 files, 58K)
    When I trace state management
    Then I produce a finding documenting how state is shared across components
```

## Method

ADR-016 Phase 2 — Targeted Deep Dives. Focus on agent orchestration patterns relevant to E3.
