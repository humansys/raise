# S1132.0b Scope: Module-Level Dependency Aggregation

**Story:** S1132.0b | **Jira:** RAISE-1171

---

## In Scope

- Fix `_resolve_import()` to validate that resolved path is a directory (module), not a root-level file
- Ensure `analyze_modules()` produces clean module→module dependency data
- Tests for root-level file exclusion edge case
- E2E validation against Claude Code source (35 modules)

## Out of Scope

- Dependency matrix visualization (separate concern — S1132.1 reconnaissance)
- Cross-package dependency tracking (monorepo imports)
- tsconfig path alias resolution
- CommonJS require() support
- Changes to ModuleInfo model

## Done When

- `_resolve_import()` only returns module names that correspond to actual directories under src/
- Root-level .ts files (commands.ts, Tool.ts, etc.) do not appear as import targets
- Existing 32 tests still pass
- New tests cover the root-file exclusion case
- E2E against Claude Code shows no false module references
- pyright strict, ruff, all green
