# S1133.2 Scope: Graph Schema Enhancements

## In Scope

1. Add `approach` and `risk` to pattern type taxonomy
2. Create `.raise/rai/learnings/` directory structure with README
3. Standardize context tag taxonomy for graph queries
4. Add story outcome metadata to graph schema
5. Validate graph queries from `aspects/introspection.md` work with new types

## Out of Scope

- Modifying existing patterns to use new types (migration is separate)
- Graph backend changes (ADR-036: backend unchanged)
- Learning record writing logic (that's in skill integration stories)
- Automatic pattern type validation at write time

## Done Criteria

- [ ] Pattern types `approach` and `risk` accepted by `rai pattern add --type`
- [ ] `.raise/rai/learnings/` directory exists with structure documentation
- [ ] Context tag taxonomy documented
- [ ] Story outcome metadata schema defined
- [ ] Graph queries from introspection aspect return results with new types
