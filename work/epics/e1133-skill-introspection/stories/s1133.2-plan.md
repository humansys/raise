# S1133.2 Implementation Plan

## Tasks

### F1: PatternSubType Extension + `--subtypes` Query Filter
**Files:**
- `packages/raise-cli/src/raise_cli/memory/models.py` — add `APPROACH`, `RISK` to enum
- `packages/raise-core/src/raise_core/graph/query.py` — add `subtypes` field to `Query`, filter in `_keyword_search()` and `_concept_lookup()`
- `packages/raise-cli/src/raise_cli/cli/commands/graph.py` — add `--subtypes` option
- `packages/raise-cli/src/raise_cli/cli/commands/pattern.py` — update help text

**Tests:**
- `packages/raise-cli/tests/memory/test_models.py` — enum values exist
- `packages/raise-core/tests/graph/test_query.py` — subtypes filter works
- `packages/raise-cli/tests/cli/test_graph_query.py` — CLI passes subtypes through

**TDD sequence:** RED (new subtypes filter), GREEN (enum + Query field + CLI), REFACTOR

### F2: LearningRecord Model + I/O
**Files:**
- `packages/raise-cli/src/raise_cli/memory/learning.py` — NEW: `PatternVote`, `LearningRecord`, `write_record()`, `read_record()`

**Tests:**
- `packages/raise-cli/tests/memory/test_learning.py` — NEW: model validation, write creates YAML, read returns None for missing, round-trip

**TDD sequence:** RED (model tests), GREEN (model), RED (write tests), GREEN (write), RED (read tests), GREEN (read)

### F3: Learnings Directory + Aspect Update
**Files:**
- `.raise/rai/learnings/README.md` — NEW: convention doc
- `aspects/introspection.md` — update query templates to `--subtypes`, add tag taxonomy section

**No tests** — documentation only.

### F4: Verification
- Run full test suite
- Run `rai pattern add "test" --type approach` to verify CLI
- Run `rai graph query "test" --subtypes approach` to verify filter
- Verify existing tests still pass

## Dependency Order

```
F1 (enum + query) → F2 (learning model) → F3 (docs + aspect) → F4 (verify)
```

F1 and F2 are logically independent but F2 imports from `models.py` so better sequential.

## Estimates

| Task | Estimate |
|------|----------|
| F1 | ~30 min |
| F2 | ~40 min |
| F3 | ~15 min |
| F4 | ~10 min |
| **Total** | **~95 min** |
