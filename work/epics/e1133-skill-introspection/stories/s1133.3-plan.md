# S1133.3 Implementation Plan

## Tasks

### F1: rai-epic-design — metadata + markers
**File:** `.claude/skills/rai-epic-design/SKILL.md`
- Add introspection metadata to frontmatter
- Add PRIME marker before Step 1 (no chain read — epic-design is first in chain)
- Add JIT markers at Steps 1, 2, 3, 4
- Add LEARN marker after Step 5

### F2: rai-epic-plan — metadata + markers + chain read
**File:** `.claude/skills/rai-epic-plan/SKILL.md`
- Add introspection metadata to frontmatter
- Add PRIME marker before Step 1 (with chain read: epic-design's learning record)
- Add JIT markers at Steps 2, 3
- Add LEARN marker after Step 5 (with downstream enrichment of epic-design's record)

### F3: rai-epic-close — metadata + markers + chain read
**File:** `.claude/skills/rai-epic-close/SKILL.md`
- Add introspection metadata to frontmatter
- Add PRIME marker before Step 1 (with chain read: all previous records)
- Add JIT markers at Steps 1, 2
- Add LEARN marker after Step 5 (aggregate learning summary)

### F4: Verification
- Verify all 3 skills have valid YAML frontmatter
- Verify marker positions match design doc
- Verify query templates use `--types pattern --subtypes approach,risk`

## Dependency Order

```
F1 → F2 → F3 → F4
```

Sequential because each builds on the chain pattern.

## Estimates

| Task | Estimate |
|------|----------|
| F1 | ~20 min |
| F2 | ~20 min |
| F3 | ~20 min |
| F4 | ~10 min |
| **Total** | **~70 min** |
