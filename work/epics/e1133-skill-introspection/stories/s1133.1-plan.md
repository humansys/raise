# S1133.1 Plan: Introspection Aspect Definition

**Story:** S1133.1 | **Size:** M | **Date:** 2026-04-01

## Status

The main artifact (`aspects/introspection.md`) is written with PRIME/JIT/LEARN protocol, schema, chain mapping, evaluation semantics, and token budget. Two gaps remain against done criteria.

## Tasks

### F1: Add per-phase guided query templates (S)

The aspect currently shows only `story.design` as example. Done criteria requires templates for all 7 participating phases.

**Action:** Add a "Guided Query Templates by Phase" section to `aspects/introspection.md` with suggested Tier 1 queries for each participating skill.

**Files:** `aspects/introspection.md` (modify)

**Verification:**
- Each of the 7 participating phases has 2-3 suggested query templates
- Templates use `{variable}` placeholders that the LLM refines at runtime

**Dependencies:** None

### F2: Update scope doc to reflect design decisions (XS)

Scope doc still references `output_quality` (4 sections → 3 sections) and `new_captures`. Update to match actual design decisions.

**Action:** Update `s1133.1-scope.md` done criteria to reflect: 3 sections (not 4), no output_quality, gaps only (not new_captures).

**Files:** `work/epics/e1133-skill-introspection/stories/s1133.1-scope.md` (modify)

**Verification:**
- Scope doc matches actual design decisions
- Done criteria checkable against actual artifact

**Dependencies:** None

### F3: Verify completeness and commit (XS)

Walk through all done criteria against the aspect file. Confirm each is met.

**Action:** Check each done criterion, mark complete, commit.

**Files:** `work/epics/e1133-skill-introspection/stories/s1133.1-scope.md` (modify)

**Verification:**
- All done criteria checked and passing
- Aspect file is self-contained — readable in <5 minutes

**Dependencies:** F1, F2

## Execution Order

```
F1 ──┐
     ├──→ F3
F2 ──┘
```

F1 and F2 are independent — can run in parallel. F3 verifies after both complete.

## Risks

| Risk | Likelihood | Mitigation |
|------|-----------|------------|
| Query templates too specific (won't generalize) | Low | Use {variable} placeholders, validated against each skill's actual decision points |

## Duration Tracking

| Task | Estimated | Actual | Notes |
|------|-----------|--------|-------|
| F1 | 20min | | |
| F2 | 5min | | |
| F3 | 10min | | |
| **Total** | **35min** | | |
