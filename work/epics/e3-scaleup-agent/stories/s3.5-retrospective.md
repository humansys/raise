# S3.5 Retrospective — Retrieval Engine

> **Story:** S3.5 | **Branch:** story/s3.5/retrieval-engine
> **Dates:** 2026-03-20
> **Estimated:** ~110min | **Actual:** ~80min (implementation ~60 + QR fix ~15 + plan rewrite ~5)

## Summary

Domain-agnostic retrieval engine with Spreading Activation scoring and ScaleUp adapter. 7 source files, 564 project tests passing (58 new for retrieval), pyright clean.

## What Went Well

- **AR before implementation** paid off — caught 3 structural simplifications (2 files not 4, function not class, annotate not rank) that would have been painful to refactor after coding
- **Plan rewrite** after AR reconciliation eliminated ambiguity — tasks mapped cleanly to code
- **TDD rhythm** was smooth — RED/GREEN/REFACTOR with no false starts
- **Implementation faster than estimated** (60min vs 110min) — good sign that design+plan were thorough enough

## What to Improve

- **Data flow gaps in planning** — the plan described components in isolation but missed wiring. `EDGE_WEIGHTS` defined in adapter, `edge_weights` param in SA, but `TraversalAdvice` wasn't specified as the carrier. Same pattern with `type_relevance()` becoming dead code after AR changed the adapter contract.
  - **Fix applied:** Added Step 2b (data flow trace) to `/rai-story-plan` skill and persisted PAT-E-001
- **Worktree friction** — `uv pip install -e .` needed after every new file creation. Should document in story-start worktree notes.

## Heutagogical Checkpoint

1. **Learned:** Component-level specs miss wiring bugs. Data flow traces catch them.
2. **Process change:** Step 2b in planning skill — 10-15 line trace of field-level data flow across boundaries.
3. **Framework improvement:** Planning skill updated. Worktree pip reinstall friction noted.
4. **New capability:** Spreading Activation implementation (BFS + decay + edge weights, strict `>` for cycle safety). Protocol-based adapter pattern (advise don't own).

## Patterns

- **PAT-E-001** (new): Data flow trace in plans for multi-component stories
- **Reinforced:** PAT-E-001 (+1) — validated by the exact bugs it would have prevented

## Quality Review Findings (all fixed)

| ID | Severity | Issue | Fix |
|----|----------|-------|-----|
| C1 | Critical | EDGE_WEIGHTS never passed to SA | Added to TraversalAdvice, plumbed through |
| C2 | Critical | type_relevance() dead code | Removed, renamed W_TYPE→W_DOMAIN |
| R1 | Recommended | retrieve() not in __init__.py | Exported |
| R2 | Recommended | Weak fallback test assertion | Strengthened to verify keyword match |
| R3 | Recommended | Vacuous test guard | Changed if→assert precondition |

## Metrics

- **Files:** 7 new source, 5 test files
- **Tests:** 58 new (564 total)
- **Commits:** 11 (scope → design → plan → AR → plan-rewrite → T1-T4 → QR-fix)
- **Pyright:** 0 errors on new code
