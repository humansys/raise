## Story Scope: S248.7 — Remove ceremony from all skills

**In Scope:**
- Remove all `rai signal emit-work` / `rai memory emit-work` calls from `skills_base/`
- Remove all `rai signal emit` calls from `skills_base/`
- Remove prerequisite check steps that are now handled by gates (GateBridgeHook)
- Remove telemetry steps (Step 0 / final step patterns) from skills
- Verify skills work identically but shorter

**Out of Scope:**
- Modifying hook/gate infrastructure (done in S248.1-S248.6)
- Adding new skill content or logic
- Changing `rai graph query` / `rai pattern add` calls (those are inputs, not ceremony — AD-4)
- Modifying skills outside `skills_base/` (project-level overrides)

**Done Criteria:**
- [ ] Zero `rai signal emit` / `rai memory emit-work` calls remain in `skills_base/`
- [ ] Zero `rai signal emit-work` calls remain in `skills_base/`
- [ ] All quality checks pass (ruff, pyright, pytest, bandit)
- [ ] Spot-check 3-4 skills to verify behavior identical minus ceremony
- [ ] Retrospective complete

**Approach:** PAT-E-445 — grep gate IS the design. Blast radius discovery first, then systematic deletion.
