# Implementation Plan: S248.6 — Built-in Gates + GateBridgeHook

## Overview
- **Story:** S248.6
- **Size:** S
- **Created:** 2026-02-23

## Tasks

### Task 1: 4 Built-in Gates
- **Description:** Create TestGate, TypeGate, LintGate, CoverageGate in `gates/builtin/`. Each wraps a subprocess call and returns GateResult. Register all 4 via `rai.gates` entry points in pyproject.toml.
- **Files:**
  - Create: `src/rai_cli/gates/builtin/__init__.py`
  - Create: `src/rai_cli/gates/builtin/tests.py` (TestGate)
  - Create: `src/rai_cli/gates/builtin/types.py` (TypeGate)
  - Create: `src/rai_cli/gates/builtin/lint.py` (LintGate)
  - Create: `src/rai_cli/gates/builtin/coverage.py` (CoverageGate)
  - Modify: `pyproject.toml` (4 entry points under `rai.gates`)
  - Create: `tests/test_gates/test_builtin_gates.py`
- **TDD Cycle:**
  - RED: Protocol conformance tests (isinstance WorkflowGate), subprocess mock tests for pass/fail/exception per gate
  - GREEN: Implement each gate with subprocess.run, error isolation via try/except
  - REFACTOR: Extract common subprocess pattern if duplication warrants it
- **Verification:** `pytest tests/test_gates/test_builtin_gates.py -v`
- **Size:** S
- **Dependencies:** None

### Task 2: GateBridgeHook
- **Description:** Create GateBridgeHook — a LifecycleHook that subscribes to `before:` events, discovers gates for that workflow_point, runs them, and returns `abort` if any fail. Register via `rai.hooks` entry point.
- **Files:**
  - Create: `src/rai_cli/hooks/builtin/gate_bridge.py`
  - Modify: `pyproject.toml` (1 entry point under `rai.hooks`)
  - Create: `tests/test_hooks/test_gate_bridge.py`
- **TDD Cycle:**
  - RED: Protocol conformance, abort on gate failure, ok on all pass, ok on no matching gates, error isolation (gate exception → abort)
  - GREEN: Implement bridge with GateRegistry.discover() + evaluate loop
  - REFACTOR: Ensure logging at DEBUG level
- **Verification:** `pytest tests/test_hooks/test_gate_bridge.py -v`
- **Size:** S
- **Dependencies:** Task 1 (needs real gates for integration test)

### Task 3: Manual Integration Test
- **Description:** Validate E2E: `rai gate list` shows 4 gates, `rai gate check --all` runs them, bridge hook fires on `before:release:publish` event.
- **Verification:** Run `rai gate list` and `rai gate check --all` interactively. Emit `BeforeReleasePublishEvent` through emitter with registry and verify bridge invokes gates.
- **Size:** XS
- **Dependencies:** Task 1, Task 2

## Execution Order
1. Task 1 — gates are independent, no hook dependency
2. Task 2 — bridge needs gates to exist for meaningful integration
3. Task 3 — final E2E validation

## Risks
- Subprocess mocking complexity: Low — well-established `unittest.mock.patch` pattern from telemetry hook tests
- Bridge double-execution: Low — bridge is the only path (tested in Task 2)

## Duration Tracking
| Task | Size | Actual | Notes |
|------|------|--------|-------|
| T1: Built-in gates | S | -- | |
| T2: GateBridgeHook | S | -- | |
| T3: Integration test | XS | -- | |
