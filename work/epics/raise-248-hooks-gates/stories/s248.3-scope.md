## Story Scope: S248.3 — Built-in TelemetryHook

**Epic:** RAISE-248 (Lifecycle Hooks & Workflow Gates)
**Size:** S
**Dependencies:** S248.2 (Hook Protocol & Registry) — Done

**In Scope:**
- `TelemetryHook` class implementing `LifecycleHook` Protocol
- Subscribes to lifecycle events and writes to `signals.jsonl`
- Replaces manual `rai signal emit-work` calls in skills
- Entry point registration via `rai.hooks`
- Uses existing signal infrastructure (`SignalStore` / telemetry module)

**Out of Scope:**
- Wiring events into CLI commands (S248.4)
- Removing ceremony from skills (S248.7)
- PRO/Enterprise telemetry backends
- New signal types or schema changes

**Done Criteria:**
- [ ] `TelemetryHook` implements `LifecycleHook` Protocol
- [ ] Hook subscribes to relevant lifecycle events
- [ ] Hook writes signals to `signals.jsonl` via existing infrastructure
- [ ] Entry point registered and discoverable via `HookRegistry`
- [ ] Error isolation: hook failure logs warning, never crashes CLI
- [ ] Tests pass with >90% coverage on new code
- [ ] All quality gates pass (ruff, pyright, bandit)
- [ ] TDD cycle followed
