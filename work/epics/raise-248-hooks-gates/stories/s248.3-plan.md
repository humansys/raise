# Implementation Plan: S248.3 — Built-in TelemetryHook

## Overview
- **Story:** S248.3
- **Size:** S
- **Created:** 2026-02-23

## Tasks

### Task 1: TelemetryHook class + unit tests
- **Description:** Create `TelemetryHook` implementing `LifecycleHook` Protocol. Subscribes to all 9 after-events. `handle()` maps event name to `CommandUsage` signal via `emit_command_usage()`. Error isolation: catch all exceptions from `emit()`, return `HookResult(status="error")`.
- **Files:**
  - Create: `src/rai_cli/hooks/builtin/__init__.py`
  - Create: `src/rai_cli/hooks/builtin/telemetry.py`
  - Create: `tests/test_hooks/test_telemetry_hook.py`
- **TDD Cycle:**
  - RED: Test Protocol conformance (`isinstance`), event subscription list, event→signal mapping for each event type, error isolation when `emit()` fails
  - GREEN: Implement TelemetryHook class
  - REFACTOR: Clean up if needed
- **Verification:** `pytest tests/test_hooks/test_telemetry_hook.py -v`
- **Size:** S
- **Dependencies:** None

### Task 2: Entry point registration + discovery test
- **Description:** Register `TelemetryHook` as entry point in `pyproject.toml` under `rai.hooks` group. Add integration test verifying `HookRegistry.discover()` finds it. Update `hooks/__init__.py` if needed.
- **Files:**
  - Modify: `pyproject.toml` (add `[project.entry-points."rai.hooks"]` section)
  - Modify: `tests/test_hooks/test_telemetry_hook.py` (add discovery test)
- **TDD Cycle:**
  - RED: Test that `HookRegistry.discover()` finds TelemetryHook
  - GREEN: Add entry point to pyproject.toml, reinstall
  - REFACTOR: N/A
- **Verification:** `pytest tests/test_hooks/test_telemetry_hook.py -v` + `pip install -e .`
- **Size:** XS
- **Dependencies:** Task 1

### Task 3: Manual integration test
- **Description:** Validate E2E: create emitter with registry, emit a lifecycle event, verify TelemetryHook fires and signal appears in signals.jsonl.
- **Verification:** Run quality gates (`ruff check src/`, `pyright`, `pytest --cov`) + demo the hook firing
- **Size:** XS
- **Dependencies:** Task 1, Task 2

## Execution Order
1. Task 1 (core implementation + tests)
2. Task 2 (entry point wiring)
3. Task 3 (E2E validation + quality gates)

## Risks
- `emit()` uses file I/O → mock in unit tests, real file in integration test
- Entry point discovery requires `pip install -e .` after pyproject.toml change

## Duration Tracking
| Task | Size | Actual | Notes |
|------|------|--------|-------|
| 1 | S | -- | |
| 2 | XS | -- | |
| 3 | XS | -- | |
