# Implementation Plan: S16.1 — Code-Aware Graph

## Overview
- **Story:** S16.1
- **Size:** S
- **Tasks:** 4
- **Created:** 2026-02-09

## Tasks

### Task 1: Models — ModuleInfo + CodeAnalyzer Protocol
- **Description:** Create the Pydantic model for language-agnostic module analysis results and the Protocol that all language analyzers must implement.
- **Files:**
  - CREATE `src/rai_cli/context/analyzers/__init__.py`
  - CREATE `src/rai_cli/context/analyzers/models.py` (ModuleInfo)
  - CREATE `src/rai_cli/context/analyzers/protocol.py` (CodeAnalyzer Protocol)
  - CREATE `tests/context/analyzers/__init__.py`
  - CREATE `tests/context/analyzers/test_models.py`
- **TDD Cycle:**
  - RED: Test ModuleInfo creation with valid/invalid data, test CodeAnalyzer Protocol compliance
  - GREEN: Implement ModuleInfo (Pydantic) and CodeAnalyzer (Protocol)
  - REFACTOR: Ensure clean exports
- **Verification:** `pytest tests/context/analyzers/test_models.py -v`
- **Size:** XS
- **Dependencies:** None

### Task 2: PythonAnalyzer — AST-based import/export extraction
- **Description:** Implement `PythonAnalyzer` that uses Python's `ast` module to extract imports, exports, and component counts from each module directory under `src/`. Must handle: absolute imports, relative imports, `TYPE_CHECKING` blocks (skip), `try/except` imports, and `__init__.py` `__all__` exports.
- **Files:**
  - CREATE `src/rai_cli/context/analyzers/python.py`
  - CREATE `tests/context/analyzers/test_python.py`
- **TDD Cycle:**
  - RED: Test `detect()` with/without pyproject.toml. Test `analyze_modules()` against fixture modules with known imports/exports. Test edge cases: relative imports, TYPE_CHECKING, try/except, `__all__`.
  - GREEN: Implement using `ast.parse()`, `ast.walk()` visiting `Import`/`ImportFrom` nodes. Export extraction from `__init__.py` `__all__` or top-level imports.
  - REFACTOR: Extract helpers if ast walking gets complex.
- **Verification:** `pytest tests/context/analyzers/test_python.py -v`
- **Size:** M
- **Dependencies:** Task 1

### Task 3: Builder integration — load_code_structure()
- **Description:** Add `load_code_structure()` to `UnifiedGraphBuilder` that runs detected analyzers and enriches existing `mod-*` nodes with code analysis data in metadata (`code_imports`, `code_exports`, `code_components`). Must not break existing graph build.
- **Files:**
  - MODIFY `src/rai_cli/context/builder.py` (add `load_code_structure()`, call from `build()`)
  - MODIFY `src/rai_cli/context/__init__.py` (export new public API if needed)
  - MODIFY `tests/context/test_builder.py` (add tests for code structure integration)
- **TDD Cycle:**
  - RED: Test that `build()` produces module nodes with `code_imports` in metadata. Test that nodes without matching code modules retain existing data.
  - GREEN: Implement `load_code_structure()`, integrate after `load_architecture()` in `build()`.
  - REFACTOR: Ensure clean merge of code data into existing node metadata.
- **Verification:** `pytest tests/context/test_builder.py -v && pytest tests/context/ -v`
- **Size:** S
- **Dependencies:** Task 2

### Task 4: Manual Integration Test
- **Description:** Run `rai memory build` on real raise-commons, verify that module nodes have real code data. Query `rai memory context mod-memory` and confirm it shows code-derived imports/exports alongside frontmatter data.
- **Verification:**
  1. `uv run rai memory build` — completes without error
  2. `uv run rai memory context mod-memory` — shows code analysis data
  3. `uv run rai memory context mod-context` — shows code analysis data
  4. Compare `code_imports` vs `depends_on` — identify any discrepancies
  5. Run full quality gates: `uv run ruff check src/ && uv run pyright && uv run pytest`
- **Size:** XS
- **Dependencies:** Task 3

## Execution Order

```
Task 1 (models)      XS
  ↓
Task 2 (analyzer)    M
  ↓
Task 3 (builder)     S
  ↓
Task 4 (integration) XS
```

Sequential chain — each task builds on the previous.

## Risks

- **Import pattern complexity:** Real code has TYPE_CHECKING, conditional imports, star imports. Mitigation: test against actual raise-commons modules in Task 4.
- **Module detection:** Need to correctly map `src/rai_cli/<name>/` directories to `mod-<name>` node IDs. Mitigation: explicit mapping logic, not guessing.

## Duration Tracking

| Task | Size | Actual | Notes |
|------|------|--------|-------|
| 1: Models | XS | -- | |
| 2: PythonAnalyzer | M | -- | |
| 3: Builder integration | S | -- | |
| 4: Integration test | XS | -- | |
