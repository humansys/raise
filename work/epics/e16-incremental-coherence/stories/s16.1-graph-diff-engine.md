## Story Scope: S16.1 — Code-Aware Graph

**Epic:** E16 (Incremental Coherence)
**Size:** S
**Milestone:** Prerequisite for M1
**Branch:** `story/s16.1/graph-diff-engine` (legacy name, content is code-aware graph)

**In Scope:**
- `CodeAnalyzer` Protocol defining language-agnostic contract
- `ModuleInfo` Pydantic model (universal output: imports, exports, components)
- `PythonAnalyzer` implementation using `ast` module
- Builder integration: `load_code_structure()` in `UnifiedGraphBuilder`
- Module nodes enriched with real imports/exports/component counts
- Pluggable architecture ready for future TS/PHP analyzers

**Out of Scope:**
- TypeScript/PHP analyzers (parkinglotted for post-E16)
- Graph diff engine (S16.2)
- Doc updating (S16.3)
- Lifecycle integration (S16.4)
- Monorepo multi-analyzer detection (future)

**Done Criteria:**
- [ ] `CodeAnalyzer` Protocol defined with `detect()` and `analyze_modules()`
- [ ] `ModuleInfo` Pydantic model with imports, exports, components, language
- [ ] `PythonAnalyzer` extracts real imports and exports via `ast`
- [ ] `UnifiedGraphBuilder.load_code_structure()` integrates analyzer output
- [ ] Module nodes (`mod-*`) have real `depends_on` and `public_api` from code
- [ ] Unit tests with >90% coverage
- [ ] All quality gates pass (pyright, ruff, tests)
- [ ] Retrospective complete
