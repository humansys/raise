# S337.4 Scope — Docs Protocol Support

## In Scope
- Add 5 `AsyncDocumentationTarget` methods to `DeclarativeMcpAdapter`: `can_publish`, `publish`, `get_page`, `search`, `health` (reuse existing)
- Same dispatch pattern as PM methods (tool call → response mapping → model)
- Return type inference for docs models: `PageContent`, `PageSummary`, `PublishResult`
- One class serves both protocols (AR-Q1)
- `health()` already exists from PM — shared, no duplication

## Out of Scope
- YAML discovery for docs protocol (already done in S337.3)
- Resolver integration for docs (already done in S337.3 via `_discover_docs`)
- New expression filters or schema changes
- Reference YAML config for a real docs adapter (S337.5)

## Done When
- `DeclarativeMcpAdapter` satisfies `AsyncDocumentationTarget` protocol (structural typing)
- All 5 docs methods dispatch to configured MCP tools
- Unmapped methods raise `NotImplementedError`
- Tests cover all 5 methods + unmapped case
- Types + lint clean
