# S337.3 Scope — YAML Discovery in Resolver

## In Scope
- `discover_yaml_adapters(protocol)` function in new `src/rai_cli/adapters/declarative/discovery.py`
- Scans `.raise/adapters/*.yaml` for configs matching requested protocol
- Factory closure pattern (AR-C1): each YAML produces a `Callable[[], DeclarativeMcpAdapter]`
- Update `_resolve.py` to merge YAML results into discover lambda
- Relax `resolve_entrypoint` type from `dict[str, type]` to `dict[str, Callable[[], Any]]`
- Entry points take priority over YAML on name collision
- Shared `McpBridge` per adapter lifetime (AR-C2)

## Out of Scope
- Docs protocol discovery (S337.4)
- Validation CLI (S337.5)
- Remote/URL YAML loading
- Hot-reload of YAML changes

## Done When
- `discover_yaml_adapters("pm")` returns factory closures from `.raise/adapters/*.yaml`
- `resolve_adapter(None)` works with YAML-only, entry-point-only, and mixed scenarios
- Entry points win on name collision
- All tests pass, types clean, lint clean
