# Plan: S337.5 — Reference Config + Validation CLI

## Overview

| Field | Value |
|-------|-------|
| Story | S337.5 |
| Size | S (3 SP) |
| Date | 2026-03-01 |
| Tasks | 3 |

## Tasks

### Task 1: Rename `adapters` → `adapter` + reference github.yaml (S)

**Description:** Rename CLI group from `adapters` to `adapter` for consistency with rest of CLI (`graph`, `pattern`, `session`). Create reference `github.yaml` with inline comments covering all 11 PM methods.

**Files:**
- MODIFY: `src/rai_cli/cli/commands/adapters.py` — `name="adapters"` → `name="adapter"`
- MODIFY: `src/rai_cli/cli/main.py` — `name="adapters"` → `name="adapter"`
- MODIFY: `tests/cli/commands/test_adapters.py` — update all `["adapters", ...]` → `["adapter", ...]`
- CREATE: `src/rai_cli/adapters/declarative/reference/github.yaml`
- CREATE: `src/rai_cli/adapters/declarative/reference/__init__.py` (empty, makes it a package)

**TDD:**
- RED: Test that `github.yaml` validates against `DeclarativeAdapterConfig` and has all 11 PM methods
- GREEN: Create the YAML file
- RED: Test that `rai adapter list` works (rename verification)
- GREEN: Apply rename

**Verification:** `uv run pytest tests/cli/commands/test_adapters.py -x && uv run pyright && uv run ruff check`

### Task 2: `rai adapter validate` command (S)

**Description:** Add `validate` command to adapter group. Reads YAML file, validates with `DeclarativeAdapterConfig.model_validate()`, outputs result.

**Files:**
- MODIFY: `src/rai_cli/cli/commands/adapters.py` — add `validate_command`
- MODIFY: `src/rai_cli/output/formatters/adapters.py` — add `format_validate_human`
- CREATE: `tests/cli/commands/test_adapter_validate.py`

**TDD:**
- RED: Test valid config → exit 0, shows name/protocol/methods count
- GREEN: Implement command + formatter
- RED: Test invalid config → exit 1, shows field errors
- GREEN: Handle ValidationError
- RED: Test missing file → exit 1
- GREEN: Handle FileNotFoundError
- RED: Test empty/non-YAML → exit 1
- GREEN: Handle yaml parse errors

**Verification:** `uv run pytest tests/cli/commands/test_adapter_validate.py -x && uv run pyright && uv run ruff check`

### Task 3: Integration test (XS)

**Description:** End-to-end: validate the shipped reference github.yaml via CLI invocation. Verify existing `list` and `check` commands still work under new `adapter` name.

**Files:**
- MODIFY: `tests/cli/commands/test_adapter_validate.py` — add E2E test

**TDD:**
- RED: Test `rai adapter validate` on shipped reference config → exit 0, "github" in output
- GREEN: Should already pass (verifies integration)

**Verification:** `uv run pytest tests/cli/commands/ -x && uv run pyright && uv run ruff check`

## Execution Order

```
Task 1 (rename + reference YAML) → Task 2 (validate command) → Task 3 (E2E)
```

Sequential — each depends on the previous. Risk-first: rename is the riskiest (touches existing tests).

## Risks

| Risk | Mitigation |
|------|------------|
| Rename breaks imports in other modules | Grep for "adapters_app" — only imported in main.py |
| Reference YAML tool names don't match real GitHub MCP server | Acceptable — reference is illustrative, not functional |

## Duration Tracking

| Task | Estimated | Actual | Notes |
|------|-----------|--------|-------|
| T1 | 15 min | | |
| T2 | 20 min | | |
| T3 | 5 min | | |
| **Total** | **40 min** | | |
