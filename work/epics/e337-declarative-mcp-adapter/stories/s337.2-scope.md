# S337.2 Scope: DeclarativeMcpAdapter (PM Protocol)

## In Scope

- `DeclarativeMcpAdapter` class in `src/rai_cli/adapters/declarative/adapter.py`
  - Implements all 11 `AsyncProjectManagementAdapter` methods
  - Dispatches to MCP tools via `McpBridge` using YAML config
  - Uses `ExpressionEvaluator` to resolve template args
  - Response model inferred from method name (AR-R1), not YAML
  - Single shared `McpBridge` per adapter lifetime (AR-C2)
  - `null` methods → `NotImplementedError`
  - `batch_transition` auto-loops if not explicitly declared
  - `health()` delegates to bridge
  - `aclose()` delegates to bridge
- Unit tests with mocked `McpBridge` (no real MCP server)
- Update `__init__.py` exports

## Out of Scope

- Docs protocol methods (S337.4)
- Registry/resolver integration (S337.3)
- CLI commands (S337.5)
- Real MCP server interaction (integration test in S337.5)

## Done Criteria

- [ ] All 11 PM protocol methods implemented and tested
- [ ] `isinstance(adapter, AsyncProjectManagementAdapter)` passes
- [ ] Null/undeclared methods raise `NotImplementedError`
- [ ] `batch_transition` auto-loops correctly
- [ ] All tests pass, types check, lint clean
- [ ] ~200 LOC for adapter.py
