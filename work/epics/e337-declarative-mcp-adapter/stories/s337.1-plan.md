# S337.1 Plan: Expression Evaluator + YAML Schema

> Story: S337.1 | Size: S | Date: 2026-03-01

## Tasks

### T1: ExpressionEvaluator — RED + GREEN (S)

**Files:**
- Create `src/rai_cli/adapters/declarative/__init__.py`
- Create `src/rai_cli/adapters/declarative/expressions.py`
- Create `tests/adapters/declarative/__init__.py`
- Create `tests/adapters/declarative/test_expressions.py`

**TDD cycle:**
- RED: Tests for all 7 scenarios from story.md (var substitution, dot-access, 4 filters, literal passthrough) + edge cases (missing key, nested dot, chained access, None handling)
- GREEN: Implement `ExpressionEvaluator` with `evaluate()` and `evaluate_args()` methods
- REFACTOR: Clean up regex patterns, extract filter dispatch

**Verification:** `pytest tests/adapters/declarative/test_expressions.py -v`

**AC refs:** Scenarios 1-7 from s337.1-story.md

### T2: DeclarativeAdapterConfig — RED + GREEN (S)

**Files:**
- Create `src/rai_cli/adapters/declarative/schema.py`
- Create `tests/adapters/declarative/test_schema.py`
- Create `tests/adapters/declarative/fixtures/valid.yaml`
- Create `tests/adapters/declarative/fixtures/minimal.yaml`
- Create `tests/adapters/declarative/fixtures/invalid_missing_name.yaml`

**TDD cycle:**
- RED: Tests for valid config parsing, minimal config, missing required fields, invalid protocol value, null method mapping
- GREEN: Implement `AdapterMeta`, `ServerConfig`, `MethodMapping`, `ResponseMapping`, `DeclarativeAdapterConfig`
- REFACTOR: Validators, field descriptions

**Verification:** `pytest tests/adapters/declarative/test_schema.py -v`

**AC refs:** Scenarios 8-9 from s337.1-story.md

**Depends on:** T1 (schema imports are independent but same package)

### T3: Public API + Integration Verification (XS)

**Files:**
- Update `src/rai_cli/adapters/declarative/__init__.py` — export public API

**Actions:**
- Export `ExpressionEvaluator`, `DeclarativeAdapterConfig`, key schema models
- Run full validation: `pytest tests/adapters/declarative/ -v && pyright src/rai_cli/adapters/declarative/ && ruff check src/rai_cli/adapters/declarative/`
- Verify combined: load YAML fixture → parse to config → evaluate expressions from config args

**Verification:** All tests pass, types clean, lint clean

**Depends on:** T1, T2

## Execution Order

```
T1 (ExpressionEvaluator)  →  T2 (Schema models)  →  T3 (Public API + verify)
```

Sequential — T2 is in same package as T1, T3 ties them together.

## Risks

| Risk | Mitigation |
|------|------------|
| Regex for `{{ expr \| filter }}` gets complex | Keep it simple: split on `\|`, strip, dispatch. No nested expressions. |
| Pydantic validation of `methods: dict[str, MethodMapping \| None]` | Union discriminator via None check — standard Pydantic pattern |

## Duration Tracking

| Task | Estimated | Actual | Velocity |
|------|-----------|--------|----------|
| T1 | — | — | — |
| T2 | — | — | — |
| T3 | — | — | — |
