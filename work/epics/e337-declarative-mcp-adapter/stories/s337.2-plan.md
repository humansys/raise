# S337.2 Plan: DeclarativeMcpAdapter (PM Protocol)

> Story: S337.2 | Size: M | Date: 2026-03-01

## Tasks

### T1: Core dispatch + CRUD methods — RED + GREEN (M)

**Files:**
- Create `src/rai_cli/adapters/declarative/adapter.py`
- Create `tests/adapters/declarative/test_adapter.py`

**TDD cycle:**
- RED: Tests for create_issue, get_issue, update_issue, transition_issue with mocked McpBridge
  - Verify bridge.call() receives correct tool name and evaluated args
  - Verify response is parsed into correct Pydantic model (IssueRef, IssueDetail, IssueSummary)
  - Verify null method raises NotImplementedError
  - Verify undeclared method raises NotImplementedError
- GREEN: Implement `DeclarativeMcpAdapter.__init__()`, `_dispatch()`, `_parse_response()`, CRUD methods
- REFACTOR: Extract common dispatch pattern

**Verification:** `pytest tests/adapters/declarative/test_adapter.py -v --no-cov`

**AC refs:** Scenarios 1, 3, 4 from story.md

### T2: Query, relationships, comments, batch, lifecycle — RED + GREEN (S)

**Files:**
- Extend `test_adapter.py` with remaining method tests
- Extend `adapter.py` with remaining methods

**TDD cycle:**
- RED: Tests for search (list response with items_path), link_to_parent, link_issues, add_comment, get_comments, batch_transition (auto-loop), health, aclose
- GREEN: Implement remaining 7 methods + batch_transition auto-loop logic
- REFACTOR: Ensure all methods use consistent dispatch pattern

**Verification:** `pytest tests/adapters/declarative/test_adapter.py -v --no-cov`

**AC refs:** Scenarios 2, 5, 6, 7 from story.md

**Depends on:** T1

### T3: Protocol compliance + full gates (XS)

**Files:**
- Update `src/rai_cli/adapters/declarative/__init__.py`

**Actions:**
- Add `DeclarativeMcpAdapter` to public exports
- Test `isinstance(adapter, AsyncProjectManagementAdapter)` passes
- Run full gate: pytest + pyright + ruff on entire declarative package

**Verification:** All gates green

**Depends on:** T1, T2

## Execution Order

```
T1 (Core dispatch + CRUD)  →  T2 (Remaining methods)  →  T3 (Protocol compliance + gates)
```

## Risks

| Risk | Mitigation |
|------|------------|
| Response parsing varies per method (single item vs list) | Two code paths: single-model parse vs list parse with items_path |
| McpBridge mock complexity | Use AsyncMock with configurable return values per tool name |

## Duration Tracking

| Task | Estimated | Actual | Velocity |
|------|-----------|--------|----------|
| T1 | — | — | — |
| T2 | — | — | — |
| T3 | — | — | — |
