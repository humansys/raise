---
story_id: "S356.3"
type: "plan"
complexity: "S"
created: "2026-03-05"
tasks: 3
estimated_effort: "30min"
---

# S356.3: Cloudflare Pages Deployment — Plan

## Prerequisites

- [x] Branch: `story/s356.3/cloudflare-deployment`
- [x] Design: `s356.3-design.md`
- [x] Existing wrangler.jsonc verified correct (no changes needed)

## Tasks

### T1: Create GitHub Actions workflow (S)

**File**: `.github/workflows/deploy-site.yml`

**Actions**:
1. Create workflow file with:
   - Name: "Deploy Website"
   - Trigger: push to `main` with path filter `site/**`
   - Single job: `deploy` on `ubuntu-latest`
   - Steps:
     - `actions/checkout@v4`
     - `actions/setup-node@v4` with node-version 22
     - `npm ci` (working-directory: site/)
     - `npm run build` (working-directory: site/)
     - `npx wrangler pages deploy dist/ --project-name=raise-website` (working-directory: site/)
   - Environment variables: `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID` from secrets

**Validation**: YAML lint passes, structure matches design D1/D2/D3.

**Commit**: `ci(s356.3): add deploy-site workflow for Cloudflare Pages`

### T2: Create deployment documentation (S)

**File**: `site/DEPLOYMENT.md`

**Actions**:
1. Create documentation covering:
   - Prerequisites (Cloudflare account, API token with Pages + D1 permissions, GitHub secrets)
   - Repointing Cloudflare Pages from raise-gtm to raise-commons
   - D1 database continuity (same database_id, no migration)
   - Manual deploy fallback command
   - Troubleshooting common issues

**Validation**: All AC2/AC4 acceptance criteria addressed.

**Commit**: `docs(s356.3): add Cloudflare deployment documentation`

### T3: Verify build (XS)

**Actions**:
1. Run `npm run build` in `site/` to confirm the site builds
2. Verify wrangler.jsonc values match workflow (project name, output dir)

**Validation**: Build succeeds, wrangler config consistent with workflow.

**Commit**: No commit (verification only). If issues found, fix and commit.

## Task Dependencies

```
T1 ──┐
     ├── T3 (verify everything works together)
T2 ──┘
```

T1 and T2 are independent. T3 runs last as final verification.

## Done Criteria

- [ ] deploy-site.yml exists with correct triggers, steps, and secrets
- [ ] DEPLOYMENT.md documents prerequisites, repointing, D1, manual deploy
- [ ] `npm run build` succeeds
- [ ] wrangler.jsonc verified consistent with workflow
