# S3.4 Scope: Graph Integration (Walking Skeleton)

## In Scope

- **ScaleUp GraphNode subclasses** — Typed subclasses (methodology, worksheet, decision_area, tool) with auto-registration via `node_type=`
- **ScaleUp GraphEdge types** — Reuse CoreEdgeTypes + define scaleup-specific edge types as needed
- **YAML→Graph builder** — Reads Eduardo's ontology YAML, produces GraphNode + GraphEdge instances
- **Roundtrip tests** — Serialize/deserialize preserves typed subclass identity and fields
- **Walking skeleton** — Map ~9 existing nodes from Eduardo's S6.1 to prove the pattern
- **Multi-domain coexistence** — ScaleUp nodes live alongside RaiSE nodes in the same graph

## Out of Scope

- Modifications to raise-commons (raise_core.graph.models) — subclasses live in rai-agent
- `rai graph build` CLI integration (hooking into the build pipeline) — future story
- Extraction pipeline (S3.2 scope)
- Vector/embedding-based retrieval — retrieval is symbolic
- Visual graph rendering

## Done Criteria

- [ ] ScaleUp node subclasses defined with typed fields and auto-registered
- [ ] Builder reads Eduardo's YAML format and produces GraphNode + GraphEdge
- [ ] Roundtrip test: serialize → deserialize preserves subclass type and fields
- [ ] ~9 existing Eduardo nodes successfully mapped and queryable
- [ ] Cross-domain test: ScaleUp + RaiSE nodes coexist in one graph
- [ ] All tests pass, pyright strict, ruff clean
