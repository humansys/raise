# S494.3: Retrospective — Full Protocol Implementation

## Summary

- **Story:** S494.3
- **Size:** M (5 tasks)
- **Dates:** 2026-03-19
- **Estimated:** M (~60 min)
- **Actual:** ~50 min implementation + review

## What Went Well

- **Pre-impl AR was high-value**: Eliminated `is_flat` dead code and config-based status lookup before writing any code. Saved ~30 lines and a config dependency.
- **TDD cycle was smooth**: Each task had clear RED→GREEN→REFACTOR with mocked bridge calls. No rework needed.
- **Parser families were clean**: Three distinct parsers (nested read, envelope write, ACLI comments) with no ambiguity.
- **Full suite stayed green**: 3905 tests passed throughout, zero regressions.
- **Adapter is 170 LOC vs 437 MCP**: Simpler, fewer branches, no flat/nested detection.

## What to Improve

- **Spike should validate invocation, not just output**: S494.1 analyzed JSON responses but didn't run `acli jira workitem view KEY` to catch positional vs --key inconsistency. Manual test in T5 caught it.
- **JQL reserved word quoting**: `RAISE` is reserved in JQL. Not in scope (SHOULD in design) but will bite in real usage. Should be addressed in S494.4 or standalone fix.

## Heutagogical Checkpoint

1. **Learned:** ACLI flag inconsistency (view=positional, rest=--key), JQL reserved word quoting needed
2. **Process change:** Spikes should include real CLI invocation of every command, not just JSON inspection
3. **Framework improvement:** Pre-impl AR should be standard for M+ stories — it caught real issues
4. **More capable at:** Mapping CLI tools to typed protocols with clean parser separation

## Patterns Added

- **PAT-E-032**: ACLI view uses positional key, not --key flag
- **PAT-E-033**: Pre-impl AR for M+ stories catches dead code early
- **PAT-E-034**: Convention-based slug→display name via replace/title

## Patterns Reinforced

- **PAT-E-589** (+1): Module-level Path constant as test seam — used for _JIRA_YAML_PATH

## Metrics

- 11/11 protocol methods implemented
- 30 unit tests + 3905 full suite
- 170 LOC adapter (vs 437 MCP — 61% reduction)
- 0 pyright errors, 0 ruff issues
