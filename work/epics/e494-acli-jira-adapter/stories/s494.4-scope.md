# S494.4: Scope

## In Scope

- Extend `jira.yaml` schema with `instances:` section (name → site URL mapping)
- Add `instance:` field to project entries linking to an instance
- Adapter resolves project key → instance → site at call time
- Pass `site` to `AcliJiraBridge.call()` (parameter already reserved)
- Bridge runs `acli auth switch <site>` when target site differs from current
- Cache current site in bridge to avoid redundant switches
- Backward compatibility: configs without `instances:` work unchanged
- Warning log for unknown project keys (no site resolution)

## Out of Scope

- Protocol changes (AsyncProjectManagementAdapter stays as-is)
- CLI command changes (`rai backlog` interface unchanged)
- Deleting McpJiraAdapter (S494.5)
- Entry point registration change (S494.5)
- `rai doctor` checks (SHOULD scope, separate story if needed)
- JQL reserved word quoting (separate fix)

## Done When

- jira.yaml `instances:` section parsed and validated
- Project → site resolution works for multi-instance configs
- `acli auth switch` called when site differs from cached current
- Single-instance configs (no `instances:`) work without changes
- Unknown project keys log warning and proceed without site switch
- Type checks pass (pyright strict)
- All tests green, linting clean
