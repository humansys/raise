# S494.3: Scope

## In Scope

- `AcliJiraAdapter` class implementing `AsyncProjectManagementAdapter`
- All 11 protocol methods: create_issue, get_issue, update_issue, transition_issue, batch_transition, link_to_parent, link_issues, add_comment, get_comments, search, health
- Config loading from jira.yaml (status_mapping, project config)
- Site resolution per project key (multi-instance prep)
- Response parsing reusing nested-format extractors from mcp_jira.py
- Full test suite with mocked AcliJiraBridge calls

## Out of Scope

- Deleting McpJiraAdapter (S494.5)
- Entry point registration change (S494.5)
- Multi-instance config schema extension (S494.4)
- jira.yaml `instances:` section (S494.4)
- Protocol changes (protocol stays as-is)
- Data model changes (IssueSpec, IssueDetail, etc. stay as-is)

## Done When

- AcliJiraAdapter passes all 11 method contracts against mocked ACLI responses
- Parser reuse validated: nested Jira API format → existing models
- Config loading tested (jira.yaml present and absent cases)
- Type checks pass (pyright strict)
- All tests green, linting clean
