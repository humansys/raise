# S494.5: Delete MCP Adapter and Migrate Entry Point — Retrospective

## Summary

- **Story:** S494.5 (E494)
- **Date:** 2026-03-20
- **Size:** S (estimated 15m, actual ~20m)
- **Variance:** 1.33x — venv corruption from `uv pip install --reinstall` added recovery time

## What Went Well

- Design phase mapped all references upfront — clean deletion with no surprises in code
- Quality review caught dead ruff config (F1) that no automated gate detects
- Zero McpJiraAdapter references remain in Python source
- Net -1115 LOC — pure simplification

## What to Improve

- **Plan atomicity:** T1 (delete) and T2 (rewire entry point) should have been one task. Splitting created transient test failure on the intermediate state
- **Venv management:** Used `uv pip install --reinstall` which corrupted the workspace venv. Correct approach: `uv sync --all-extras`

## Heutagogical Checkpoint

1. **Learned:** `uv sync --all-extras` is the correct way to force workspace package reinstallation
2. **Process change:** Deletion + migration = one atomic task in plans
3. **Framework improvement:** Ruff silently ignores per-file-ignores for missing files — potential `rai doctor` check
4. **Growth:** Better dependency mapping before deletion — distinguishing live vs dead consumers of shared infrastructure (McpBridge)

## Patterns

- PAT-E-040: Deletion + entry point rewire must be atomic commit
- PAT-E-041: `uv pip install --reinstall` corrupts workspace venv; use `uv sync --all-extras`

## Quality Review

- **Verdict:** PASS (after F1 fix)
- **F1:** Dead ruff per-file-ignores line for deleted mcp_jira.py — fixed
