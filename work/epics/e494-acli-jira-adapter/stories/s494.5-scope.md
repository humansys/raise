# S494.5: Delete MCP Adapter and Migrate Entry Point — Scope

## In Scope

- Delete `McpJiraAdapter` class and its module
- Delete MCP Jira adapter tests
- Remove MCP-specific Jira dependencies (if any isolated to Jira adapter)
- Update `jira` entry point in pyproject.toml to point to `AcliJiraAdapter`
- Clear error message when ACLI binary not found (on adapter instantiation or first call)
- Remove any MCP Jira adapter references in docs/config

## Out of Scope

- Protocol changes (`AsyncProjectManagementAdapter` stays as-is)
- Other MCP servers (context7 etc. unaffected)
- ACLI adapter logic changes (already complete in S494.3–S494.4)
- `rai doctor` ACLI check (SHOULD item, separate if needed)

## Done Criteria

- [ ] `McpJiraAdapter` class and module deleted
- [ ] MCP Jira adapter tests deleted
- [ ] `jira` entry point resolves to `AcliJiraAdapter`
- [ ] Clear error when `acli` not in PATH
- [ ] Zero references to `McpJiraAdapter` in codebase
- [ ] All existing tests pass
- [ ] Type checks pass
- [ ] Linting passes
