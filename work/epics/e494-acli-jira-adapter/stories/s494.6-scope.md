# S494.6: Integration Test Suite — Scope

## In Scope

- `tests/integration/test_acli_jira_e2e.py` — integration tests for all adapter methods
- `conftest.py` with adapter fixture and test issue lifecycle (create → use → cleanup)
- `pytest.mark.integration` registration and default exclusion
- Skip logic when ACLI or jira.yaml unavailable
- Coverage of: search, get, get_comments, create, update, transition, comment, link, batch_transition, health, multi-instance routing

## Out of Scope

- CI pipeline integration (no secrets in CI yet)
- Performance/load testing
- Confluence adapter tests (separate epic)
- Fixing JQL reserved word bug (RAISE keyword — tracked separately)

## Done Criteria

- [ ] Integration tests exist and run with `pytest -m integration`
- [ ] All core operations tested against real Jira
- [ ] Multi-instance routing verified (at least 2 instances)
- [ ] Tests skip cleanly when ACLI/config unavailable
- [ ] Default `pytest` run excludes integration tests
- [ ] All existing unit tests still pass
