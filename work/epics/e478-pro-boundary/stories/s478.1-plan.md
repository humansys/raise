# S478.1 Plan — Move Pro Adapters to rai_pro

## Task Breakdown

### T1: Create directories and move source files

**Goal:** Move the 3 pro adapter source files to `rai_pro` and create required `__init__.py` files.

**Steps:**
1. Create `src/rai_pro/adapters/` and `src/rai_pro/hooks/` directories
2. Create `src/rai_pro/adapters/__init__.py` (empty)
3. Create `src/rai_pro/hooks/__init__.py` (empty)
4. `git mv src/raise_cli/adapters/mcp_jira.py src/rai_pro/adapters/mcp_jira.py`
5. `git mv src/raise_cli/adapters/mcp_confluence.py src/rai_pro/adapters/mcp_confluence.py`
6. `git mv src/raise_cli/hooks/builtin/jira_sync.py src/rai_pro/hooks/jira_sync.py`

**Verification:**
- `ls src/rai_pro/adapters/mcp_jira.py src/rai_pro/adapters/mcp_confluence.py src/rai_pro/hooks/jira_sync.py` — all exist
- `ls src/raise_cli/adapters/mcp_jira.py src/raise_cli/adapters/mcp_confluence.py src/raise_cli/hooks/builtin/jira_sync.py` — none exist
- `uv run pyright src/ tests/` — type checks pass
- `uv run ruff check src/ tests/` — lint passes

**Note:** Internal imports within the moved files that reference `raise_cli` shared infrastructure (models, bridge, events) stay unchanged — those are shared modules.

---

### T2: Move test files and update imports

**Goal:** Update test files so they import from `rai_pro` instead of `raise_cli`.

**Depends on:** T1

**Steps:**
1. Update `tests/adapters/test_mcp_jira.py` — change `raise_cli.adapters.mcp_jira` to `rai_pro.adapters.mcp_jira` in all imports and patch targets
2. Update `tests/adapters/test_mcp_confluence.py` — change `raise_cli.adapters.mcp_confluence` to `rai_pro.adapters.mcp_confluence` in all imports and patch targets
3. Update `tests/test_hooks/test_jira_sync.py` — change `raise_cli.hooks.builtin.jira_sync` to `rai_pro.hooks.jira_sync` in all imports and patch targets

**Verification:**
- `uv run pytest tests/adapters/test_mcp_jira.py tests/adapters/test_mcp_confluence.py tests/test_hooks/test_jira_sync.py -v` — all tests pass
- `uv run pyright src/ tests/` — type checks pass
- `uv run ruff check src/ tests/` — lint passes

---

### T3: Integration verification and cleanup

**Goal:** Confirm no raise_cli code imports the moved modules, and full suite passes.

**Depends on:** T2

**Steps:**
1. Search `src/raise_cli/` for any remaining imports of `mcp_jira`, `mcp_confluence`, or `jira_sync` that reference the old paths — remove or fix any found
2. Run full test suite
3. Run type checks and lint on full codebase

**Verification:**
- `grep -r "from raise_cli.adapters.mcp_jira\|from raise_cli.adapters.mcp_confluence\|from raise_cli.hooks.builtin.jira_sync" src/raise_cli/` — no matches
- `uv run pytest` — full suite passes
- `uv run pyright src/ tests/` — type checks pass
- `uv run ruff check src/ tests/` — lint passes

---

## Execution Order

```
T1 (move files) → T2 (update test imports) → T3 (integration verify)
```

## Estimated Effort

| Task | Estimate |
|------|----------|
| T1 | 10 min |
| T2 | 10 min |
| T3 | 5 min |
| **Total** | **25 min** |
