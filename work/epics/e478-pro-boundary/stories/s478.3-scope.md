---
story_id: "S478.3"
epic_id: "E478"
title: "Clean pro-specific CLI logic"
size: "XS"
created: "2026-03-11"
---

# S478.3: Scope

## In Scope

- Remove `_check_jira_config()` function from `src/raise_cli/cli/commands/adapters.py`
- Remove `_JIRA_ENV_VARS` constant from `src/raise_cli/cli/commands/adapters.py`
- Remove or generalize any Jira-specific status logic in the CLI
- Remove any remaining Jira/Confluence references from raise_cli source code
- Update or remove tests that reference removed functions

## Out of Scope

- Adding new adapter status check extension points (future enhancement)
- Changes to raise-pro code
- Changes to adapter discovery mechanism (already plugin-based via entry points)

## Done Criteria

- Zero Jira/Confluence-specific logic in `src/raise_cli/` production code
- `rai adapter list` still works (shows filesystem; adds jira/confluence when raise-pro installed)
- All tests pass
- Linting and type checks pass
