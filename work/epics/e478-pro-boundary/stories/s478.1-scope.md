# S478.1 — Scope

## In Scope

- Move `src/raise_cli/adapters/mcp_jira.py` to `src/rai_pro/adapters/mcp_jira.py`
- Move `src/raise_cli/adapters/mcp_confluence.py` to `src/rai_pro/adapters/mcp_confluence.py`
- Move `src/raise_cli/hooks/builtin/jira_sync.py` to `src/rai_pro/hooks/jira_sync.py`
- Move corresponding test files to appropriate locations under `tests/`
- Update all imports in moved files to reflect new package paths
- Ensure `__init__.py` files exist in new directories

## Out of Scope

- Changing adapter logic or behavior
- Modifying entry point registrations (separate story)
- Moving any other files from raise_cli
- Refactoring adapter interfaces

## Done Criteria

1. Pro adapter files exist in `rai_pro`, removed from `raise_cli`
2. All tests pass with updated imports
3. No `raise_cli` code imports pro adapters directly
4. Type checks pass (`pyright`)
5. Linting passes
