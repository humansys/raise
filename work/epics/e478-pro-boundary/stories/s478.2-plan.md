# S478.2 — Plan: Clean Entry Points and Dependencies

## Tasks

### T1: Create `packages/raise-pro/` workspace package

**Goal:** Establish the pro package that owns pro entry points and dependencies.

**Steps:**
1. Create `packages/raise-pro/pyproject.toml` with:
   - `name = "raise-pro"`
   - Pro entry points: jira (rai.adapters.pm), confluence (rai.docs.targets), jira-sync (rai.hooks)
   - Pro dependencies: atlassian-python-api, authlib, cryptography, requests, urllib3, certifi
   - Dependency on raise-cli
   - Hatch wheel config pointing to `../../src/rai_pro`
2. Add `raise-pro = { workspace = true }` to `[tool.uv.sources]` in root pyproject.toml
3. Add `--cov=src/rai_pro` to pytest addopts in root pyproject.toml
4. Run `uv sync` to verify workspace resolution

**Verification:**
- `uv run pytest --tb=short -q`

**Depends on:** Nothing

---

### T2: Clean raise-cli pyproject.toml

**Goal:** Remove all pro entry points and dependencies from the community package.

**Steps:**
1. Remove pro entry points:
   - `jira` from `[project.entry-points."rai.adapters.pm"]`
   - Entire `[project.entry-points."rai.docs.targets"]` section (confluence)
   - `jira-sync` from `[project.entry-points."rai.hooks"]`
2. Remove 6 pro dependencies from `[project.dependencies]`:
   - atlassian-python-api, authlib, cryptography, requests, urllib3, certifi
3. Remove `src/rai_pro` from wheel packages and sdist include
4. Run `uv sync` to verify clean resolution

**Verification:**
- `uv run pytest --tb=short -q`

**Depends on:** T1 (raise-pro must provide entry points before raise-cli drops them)

---

### T3: Integration verification

**Goal:** Confirm both packages work together and all gates pass.

**Steps:**
1. Verify entry point discovery works (both filesystem and jira adapters visible)
2. Verify no raise_cli code imports from removed dependencies
3. Run full gate check:
   - Tests: `uv run pytest --tb=short -q`
   - Types: `uv run pyright src/ tests/`
   - Lint: `uv run ruff check src/ tests/`

**Verification:**
- All three gate commands pass with exit 0

**Depends on:** T2

---

## Task Order

```
T1 (create raise-pro package)
 └→ T2 (clean raise-cli pyproject.toml)
     └→ T3 (integration verification)
```

Sequential. Cannot remove from raise-cli until raise-pro provides them.

## Commit Strategy

One commit per task:
- T1: `feat(s478.2): create raise-pro workspace package`
- T2: `refactor(s478.2): remove pro entry points and deps from raise-cli`
- T3: No code commit (verification only, unless fixes needed)

## Verification Commands

| Gate | Command |
|------|---------|
| Tests | `uv run pytest --tb=short -q` |
| Types | `uv run pyright src/ tests/` |
| Lint | `uv run ruff check src/ tests/` |
