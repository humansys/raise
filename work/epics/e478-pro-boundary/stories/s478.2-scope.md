# S478.2 — Scope

## In Scope

1. **Remove pro-only entry points from raise_cli's pyproject.toml**
   - `jira` adapter entry point
   - `confluence` adapter entry point
   - `jira-sync` hook entry point

2. **Remove pro-only dependencies from `[project.dependencies]`**
   - `atlassian-python-api`
   - `authlib`
   - `cryptography`
   - Verify whether `requests`, `urllib3`, `certifi` are used by community code before removing

3. **Create `packages/raise-pro/` workspace package**
   - `packages/raise-pro/pyproject.toml` with:
     - Pro entry points (jira adapter, confluence adapter, jira-sync hook)
     - Pro-only dependencies (atlassian-python-api, authlib, cryptography, etc.)
     - Dependency on `raise-cli` (the community package)
   - Follow existing workspace pattern (see `packages/raise-server/`)

4. **Remove rai_pro from raise_cli wheel**
   - Exclude `src/rai_pro` from raise_cli's packages/wheel config
   - Ensure `rai_pro` is included in raise-pro's package instead

## Out of Scope

- CLI logic cleanup (`_check_jira_config`) — that's S478.3
- Any functional changes to adapters or hooks
- Changes to rai_pro source code (already moved in S478.1)
- CI/CD pipeline changes for the new package

## Done Criteria

- [ ] `pip install raise-cli` installs zero Atlassian deps
- [ ] Pro entry points only register when raise-pro is installed
- [ ] `rai adapter list` shows filesystem only (community)
- [ ] `rai adapter list` shows jira/confluence when raise-pro is also installed
- [ ] All tests pass (community and pro)
- [ ] `packages/raise-pro/pyproject.toml` exists and is valid

## Dependencies

- S478.1 (complete) — pro adapter code already lives in `src/rai_pro/adapters/`
