# S478.3 — Plan: Clean Pro-Specific CLI Logic

## Tasks

### T1: Remove Jira-specific code from adapters.py

**Goal:** Delete the hardcoded `rai adapter status` command and its Jira-specific helpers.

**Steps:**
1. Remove `_JIRA_ENV_VARS` constant (lines 263-267)
2. Remove `_check_jira_config()` function (lines 270-295)
3. Remove `status_command()` and its decorator (lines 298-367)
4. Remove `import os` (only used by `_check_jira_config`)
5. Update module docstring to remove `rai adapter status` mention
6. Remove any tests that specifically test the removed functions
7. Verify no other code calls `status_command`, `_check_jira_config`, or references `_JIRA_ENV_VARS`

**Verification:**
- `uv run pytest --tb=short -q`
- `uv run pyright src/ tests/`
- `uv run ruff check src/ tests/`

**Depends on:** Nothing

---

### T2: Integration verification

**Goal:** Confirm remaining adapter commands work and all gates pass.

**Steps:**
1. Run full test suite: `uv run pytest --tb=short -q`
2. Run type checks: `uv run pyright src/ tests/`
3. Run linter: `uv run ruff check src/ tests/`
4. Verify `rai adapter list` still works (smoke test)

**Verification:**
- All three gate commands pass with exit 0

**Depends on:** T1

---

## Task Order

```
T1 (remove Jira-specific code)
 └→ T2 (integration verification)
```

Sequential. T2 validates T1.

## Commit Strategy

- T1: `refactor(s478.3): remove Jira-specific status command from community CLI`
- T2: No code commit (verification only, unless fixes needed)

## Verification Commands

| Gate | Command |
|------|---------|
| Tests | `uv run pytest --tb=short -q` |
| Types | `uv run pyright src/ tests/` |
| Lint | `uv run ruff check src/ tests/` |
