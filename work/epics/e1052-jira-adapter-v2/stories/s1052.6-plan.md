---
story_id: S1052.6
type: plan
status: approved
tasks: 3
---

# S1052.6 Plan — Integration Tests + Docs

## Tasks

### T1: Integration test suite for PythonApiJiraAdapter
- **File**: `packages/raise-cli/tests/adapters/test_jira_adapter_integration.py`
- **Actions**:
  1. Create test file with module-level skip when `JIRA_API_TOKEN` not set
  2. Implement `_make_adapter()` helper using env vars + project root
  3. Test scenarios: search, get_issue, create+get+transition, add_comment+get_comments, health, link_issues
  4. All tests are async (adapter methods are async)
  5. Verify: `uv run pytest packages/raise-cli/tests/adapters/test_jira_adapter_integration.py -v` (all skip)
  6. Verify: `uv run ruff check`, `uv run pyright`
- **Commit after task**

### T2: Architecture module doc
- **File**: `governance/architecture/modules/jira-adapter.md`
- **Actions**:
  1. Create module doc following confluence-adapter.md pattern
  2. Document: purpose, components, class diagram, config, design decisions, testing
  3. Publish via `rai docs publish`
- **Commit after task**

### T3: Final validation
- **Actions**:
  1. Run full test suite: `uv run pytest -x -q`
  2. Run type check: `uv run pyright`
  3. Run linter: `uv run ruff check`
  4. Verify integration tests show as skipped
- **No commit needed** (validation only)

## Dependencies

```
T1 → T2 → T3
```

## Estimated Time

- T1: 20 min
- T2: 15 min
- T3: 5 min
- Total: ~40 min
