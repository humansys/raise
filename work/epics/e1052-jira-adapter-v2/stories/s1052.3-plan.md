# S1052.3: PythonApiJiraAdapter — Plan

## Task Sequence

### T1: Adapter skeleton + constructor + _client_for + health + entry point
**Input:** JiraClient (S1052.1), JiraConfig (S1052.2), protocols.py
**Output:** jira_adapter.py with constructor, _client_for, health; entry point in pyproject.toml
**Tests:** Constructor loads config, _client_for caches by instance, health delegates
**Gate:** pytest + pyright + ruff

### T2: CRUD methods + response parsing
**Input:** acli_jira.py parsing logic, pm.py models
**Output:** create_issue, get_issue, update_issue with full response parsing
**Port:** _extract_issue_fields, _parse_issue_detail, _adf_to_text, build_url
**Tests:** Each method with mock JiraClient, edge cases (missing fields, None parent)
**Gate:** pytest + pyright + ruff

### T3: transition_issue + batch_transition + search (JQL normalization)
**Input:** acli_jira.py JQL logic
**Output:** transition_issue (live lookup), batch_transition, search with JQL normalization
**Port:** to_jql, _quote_project_values, normalize_status
**Tests:** Status lookup match/miss, batch with partial failures, JQL normalization
**Gate:** pytest + pyright + ruff

### T4: Relationships + comments
**Input:** JiraClient link/comment methods
**Output:** link_to_parent, link_issues, add_comment, get_comments
**Tests:** Delegation verification, comment parsing with ADF
**Gate:** pytest + pyright + ruff

### T5: Protocol compliance + final validation
**Input:** All 11 methods
**Output:** isinstance check, full suite green, entry point loadable
**Tests:** Protocol structural check, entry point discovery test
**Gate:** pytest (full) + pyright (strict) + ruff

## Estimates
- T1: 30 min
- T2: 40 min
- T3: 40 min
- T4: 30 min
- T5: 20 min
- **Total: ~2h 40min**
