# S1052.3: PythonApiJiraAdapter — Scope

## Identity
- **Story ID:** S1052.3
- **Epic:** E1052 (Jira Adapter v2 — Pure Python Transport)
- **Size:** M
- **Type:** code
- **Branch:** story/s1052.3/python-api-jira-adapter

## Objective
Implement PythonApiJiraAdapter — 11 AsyncProjectManagementAdapter protocol methods
delegating to JiraClient. Register as entry point in raise-cli. Walking skeleton
that proves the whole adapter stack (client + config + adapter).

## Dependencies
- S1052.1 (JiraClient) — DONE
- S1052.2 (JiraConfig) — DONE

## Acceptance Criteria
1. PythonApiJiraAdapter class implements all 11 AsyncProjectManagementAdapter methods
2. Lazy client cache: one JiraClient per Jira instance, created on first use
3. Status resolution via live get_transitions() lookup (no hardcoded IDs)
4. Response parsing: Jira nested format → Pydantic boundary models
5. JQL normalization: issue keys, plain text, raw JQL (ported from acli_jira.py)
6. Entry point registered: rai.adapters.pm → jira
7. All tests pass, pyright strict, ruff clean
8. Protocol structural compliance verified

## Deliverables
- `packages/raise-cli/src/raise_cli/adapters/jira_adapter.py`
- `packages/raise-cli/tests/adapters/test_jira_adapter.py`
- Modified: `packages/raise-cli/pyproject.toml` (entry point + optional dep)
