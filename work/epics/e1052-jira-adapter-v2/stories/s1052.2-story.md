# S1052.2: JiraConfig Pydantic

## Summary

Create a Pydantic model for `.raise/jira.yaml`. Backwards compatible with current
schema. Validation at load time. Pattern: mirror ConfluenceConfig from E1051.

## Type: code | Size: S

## Acceptance Criteria

1. JiraInstance, JiraProject, JiraConfig frozen Pydantic models
2. `load_jira_config(root)` reads and validates `.raise/jira.yaml`
3. `resolve_instance(project_key)` and `resolve_site(project_key)` helper methods
4. Extra YAML sections (workflow, team, issue_types) accepted but not modeled (forward compat)
5. Backwards compatible with current `.raise/jira.yaml` format
6. JiraClient.from_config() accepts JiraConfig (integration point)
7. Full test coverage, pyright strict, ruff clean

## Dependencies

- None (parallel with S1052.1, which is already done)

## Epic: E1052 — Jira Adapter v2
