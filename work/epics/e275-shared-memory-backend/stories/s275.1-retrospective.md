# S275.1 Retrospective: Extract rai-core Package

**Story:** S275.1 | **JIRA:** RAISE-276 (absorbs RAISE-145)
**Size:** S | **Estimated:** ~4h | **Actual:** ~3h (with rework)
**Date:** 2026-02-25

## Summary

Extracted graph domain from `rai_cli` into `rai_core` as a standalone uv workspace package.
Moved 6 files, renamed 6 classes (drop "Unified" prefix), updated ~50 imports, deleted old files.
No backward compat shims — clean cut.

## What Went Well

- **Architecture review before implementation** caught 3 simplifications:
  - Partial move (only KnowledgeGraphBackend + BackendHealth, not all 5 protocols)
  - No shims (all code is ours, tests catch breakage)
  - Eliminate `from_file()` subclass wrapper (inline 2-liner at 3 call sites)
- **Risk-first T1** (uv workspace setup) isolated the new territory — caught `tool.uv.sources` requirement before any code moved
- **DDD domain model review** during architecture review revealed that rai-core scope is broader than just graph — workflow and governance domains identified for future
- Git detected moves as renames (77%+ similarity) — clean history

## What To Improve

- **Subagents for bulk import updates failed partially** — hooks/stash reverted changes. A single Python script doing all replacements was the fix. Pattern: PAT-E-498.
- **`git stash` during extraction is catastrophic** — restores deleted files, creates impossible-to-debug dual-copy state. Pattern: PAT-E-497.
- **Entry point refresh requires `--reinstall-package`** — `uv sync` alone doesn't update metadata. Lost 5 min debugging. Pattern: PAT-E-499.
- **Bulk rename scripts need collision detection** — `TestConceptNode` and `TestGraphNode` both became `TestGraphNode`. Caught by ruff, easy fix.
- **`rai` global command broken after extraction** — global install doesn't have rai-core. Must use `uv run rai` in workspace context. This affects developer workflow.

## Patterns

| ID | Pattern | Type |
|----|---------|------|
| PAT-E-497 | Never git stash mid-extraction | process |
| PAT-E-498 | Script over subagents for bulk import updates | process |
| PAT-E-499 | uv workspace gotchas (sources, reinstall, entry points) | technical |
| PAT-E-500 | No backward compat shims when all code is yours | architecture |

## Reinforced

| Pattern | Vote | Reason |
|---------|:----:|--------|
| PAT-E-442 | +1 | Repetitive extraction compounds — this was mechanical after workspace setup |
| PAT-E-445 | +1 | Grep gate IS the design for extraction/deletion stories |

## Open Items

- `rai` global command broken (needs `uv run rai`) — affects developer workflow, may need documentation update or global reinstall step
- 12 pre-existing ruff errors in test files (not introduced by this story)
- 15 pre-existing pyright errors (NetworkX untyped)
