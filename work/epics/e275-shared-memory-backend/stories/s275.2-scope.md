# S275.2: Scope

## In Scope

- `rai-server` package skeleton (uv workspace member, depends on rai-core)
- SQLAlchemy 2.0 async models: Organization, ApiKey, GraphNode, GraphEdge
- JSONB columns with GIN indexes for properties
- Alembic setup with initial migration (create all 4 tables)
- Docker Compose with PostgreSQL 16
- Unit tests for model creation and relationships

## Out of Scope

- FastAPI app (S275.3)
- API endpoints (S275.4/S275.5)
- Auth middleware (S275.3)
- Data seeding or fixtures beyond tests
- pgAdmin in Docker Compose (parking lot)

## Done Criteria

- `uv sync` resolves rai-server as workspace package
- `alembic upgrade head` creates 4 tables with correct schema
- `docker compose up -d` starts PostgreSQL 16
- SQLAlchemy models have type annotations and Pydantic-compatible patterns
- Tests pass for model instantiation and FK relationships
- pyright + ruff clean
