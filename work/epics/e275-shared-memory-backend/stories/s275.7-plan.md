# S275.7 — Dogfood + Offline Fallback — Plan

> **Size:** S | **Date:** 2026-02-26 | **Tasks:** 3

## Tasks

### T1: PendingSyncMarker model + helpers (S)

**Files:**
- Create `src/rai_cli/graph/backends/pending.py` — Pydantic model + write/read/clear helpers
- Create `tests/graph/test_pending_sync.py` — marker lifecycle tests

**TDD:**
- RED: test write creates file, read returns model, clear deletes file, read on missing returns None
- GREEN: implement PendingSyncMarker model + 3 helper functions
- REFACTOR: clean up if needed

**Verification:** `pytest tests/graph/test_pending_sync.py -v && ruff check src/rai_cli/graph/backends/pending.py && pyright src/rai_cli/graph/backends/pending.py`

**Dependencies:** None

---

### T2: Integrate marker into DualWriteBackend (S)

**Files:**
- Modify `src/rai_cli/graph/backends/dual.py` — call write_pending_marker on remote fail, clear on success
- Modify `src/rai_cli/graph/backends/__init__.py` — pass raise_dir to DualWriteBackend
- Modify `tests/graph/test_dual_backend.py` — add marker creation/clearing tests

**TDD:**
- RED: test remote fail creates marker, test remote success clears existing marker
- GREEN: modify DualWriteBackend.__init__ to accept raise_dir, update persist() logic
- REFACTOR: ensure logging messages are clear

**Verification:** `pytest tests/graph/test_dual_backend.py -v && ruff check src/rai_cli/graph/backends/ && pyright src/rai_cli/graph/backends/`

**Dependencies:** T1

---

### T3: Integration test + full validation (XS)

**Files:**
- Run full test suite, type checks, lint
- Manual verification: confirm marker lifecycle with mock server scenario

**Verification:** `pytest --tb=short && ruff check src/ && pyright src/rai_cli/graph/backends/`

**Dependencies:** T2

---

## Execution Order

```
T1 (model + helpers) → T2 (DualWriteBackend integration) → T3 (full validation)
```

Sequential, risk-first: T1 is pure unit (lowest risk), T2 integrates into existing code (medium risk), T3 validates everything.

## Risks

| Risk | Mitigation |
|------|------------|
| raise_dir discovery in factory | Factory already has `path` — derive `.raise/` from project root via `Path.cwd() / ".raise"` |
| Breaking existing dual backend tests | Marker is optional behavior — only triggers when raise_dir is provided |

## Duration Tracking

| Task | Estimate | Actual | Notes |
|------|----------|--------|-------|
| T1 | 10 min | — | |
| T2 | 15 min | — | |
| T3 | 5 min | — | |
