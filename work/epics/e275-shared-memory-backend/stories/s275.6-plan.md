# S275.6 — ApiGraphBackend + DualWrite — Plan

> **Story:** S275.6 | **Size:** M | **Date:** 2026-02-26
> **Design:** `s275.6-design.md`

## Tasks

### T1: ApiGraphBackend (S)

**Description:** Create `ApiGraphBackend` implementing `KnowledgeGraphBackend` protocol. httpx sync client, persist → POST /api/v1/graph/sync, health → GET /health, load → NotImplementedError.

**Files:**
- Create: `src/rai_cli/graph/__init__.py`
- Create: `src/rai_cli/graph/backends/__init__.py`
- Create: `src/rai_cli/graph/backends/api.py`
- Create: `tests/graph/test_api_backend.py`

**TDD:**
- RED: test protocol compliance, test persist sends correct payload (mock httpx), test health returns healthy/unavailable, test load raises NotImplementedError, test timeout config
- GREEN: implement ApiGraphBackend with httpx
- REFACTOR: clean up

**AC ref:** MUST 1, 2 from design

**Verification:** `pytest tests/graph/test_api_backend.py -v && ruff check src/rai_cli/graph/ && pyright src/rai_cli/graph/`

**Dependencies:** None

---

### T2: DualWriteBackend (S)

**Description:** Create `DualWriteBackend` implementing `KnowledgeGraphBackend` protocol. Delegates persist to both local + remote (remote best-effort). Load from local only. Health aggregates both.

**Files:**
- Create: `src/rai_cli/graph/backends/dual.py`
- Create: `tests/graph/test_dual_backend.py`

**TDD:**
- RED: test persist calls both backends, test remote failure logs warning (not raises), test load delegates to local only, test health aggregates statuses (healthy+healthy=healthy, healthy+unavailable=degraded)
- GREEN: implement DualWriteBackend
- REFACTOR: clean up

**AC ref:** MUST 3, 4, SHOULD 1 from design

**Verification:** `pytest tests/graph/test_dual_backend.py -v && ruff check src/rai_cli/graph/ && pyright src/rai_cli/graph/`

**Dependencies:** T1

---

### T3: Factory + CLI wiring (S)

**Description:** Create `get_active_backend()` factory in rai-cli that checks `RAI_SERVER_URL` + `RAI_API_KEY` env vars. Update CLI graph commands to import from rai-cli factory instead of rai-core.

**Files:**
- Modify: `src/rai_cli/graph/backends/__init__.py` (add factory)
- Modify: `src/rai_cli/cli/commands/graph.py` (change import)
- Modify: `src/rai_cli/session/bundle.py` (change import if uses get_active_backend)
- Create: `tests/graph/test_backend_factory.py`

**TDD:**
- RED: test no env vars → FilesystemGraphBackend, test both env vars → DualWriteBackend, test URL without key → warning + FilesystemGraphBackend, test existing CLI tests still pass
- GREEN: implement factory, update imports
- REFACTOR: verify no remaining imports of rai-core's get_active_backend in CLI

**AC ref:** MUST 5, 6 from design

**Verification:** `pytest tests/graph/ -v && pytest tests/cli/commands/test_graph.py -v && ruff check src/rai_cli/ && pyright src/rai_cli/`

**Dependencies:** T1, T2

---

### T4: Integration test (XS)

**Description:** Manual integration test. Run `rai graph build` with and without env vars. Verify local-only works unchanged. Verify with mock/real server that dual write sends payload.

**Files:** None (manual verification)

**Steps:**
1. `rai graph build` without env vars → same behavior as before
2. `RAI_SERVER_URL=http://localhost:9999 RAI_API_KEY=rsk_fake rai graph build` → local succeeds, remote warning logged
3. Full test suite: `pytest --tb=short`
4. Type check: `pyright src/`
5. Lint: `ruff check src/`

**AC ref:** All MUST criteria

**Verification:** All commands pass

**Dependencies:** T3

## Execution Order

```
T1 (ApiGraphBackend) → T2 (DualWriteBackend) → T3 (Factory + CLI wiring) → T4 (Integration)
```

Sequential — each builds on the previous. Risk-first: T1 is the riskiest (httpx + serialization).

## Risks

| Risk | L/I | Mitigation |
|------|:---:|------------|
| Serialization mismatch between Graph and server schema | M/M | Use server's existing Pydantic schemas as reference for field names |
| httpx not available in test env | L/L | Already in rai-cli deps |
| Existing tests break from import change | L/M | T3 runs full existing test suite |

## Duration Tracking

| Task | Size | Estimated | Actual | Notes |
|------|:----:|:---------:|:------:|-------|
| T1 | S | 20 min | — | |
| T2 | S | 15 min | — | |
| T3 | S | 15 min | — | |
| T4 | XS | 10 min | — | |
| **Total** | **M** | **~60 min** | — | |
