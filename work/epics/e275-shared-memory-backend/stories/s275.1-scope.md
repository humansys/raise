# S275.1: Extract rai-core — Scope

## In Scope

- Create `packages/rai-core/` as uv workspace package with `pyproject.toml`
- Move to `rai_core/graph/models.py`: GraphNode + 18 subclasses, GraphEdge, CoreEdgeTypes, NodeType, EdgeType, backward compat aliases
- Move to `rai_core/graph/engine.py`: UnifiedGraph → renamed to `Graph`
- Move to `rai_core/graph/query.py`: UnifiedQueryEngine → `QueryEngine`, UnifiedQuery → `Query`, scoring functions. **Without** `from_file()`
- Move to `rai_core/graph/backends/protocol.py`: `KnowledgeGraphBackend` protocol only
- Move to `rai_core/graph/backends/models.py`: `BackendHealth` only
- Move to `rai_core/graph/backends/filesystem.py`: `FilesystemGraphBackend`, `get_active_backend()`
- Re-export layer in `rai_cli.context.*` and `rai_cli.adapters.*` for backward compat (old names + old paths)
- Drop "Unified" prefix: `UnifiedGraph` → `Graph`, `UnifiedQueryEngine` → `QueryEngine`, `UnifiedQuery` → `Query`, `UnifiedQueryResult` → `QueryResult`, `UnifiedQueryMetadata` → `QueryMetadata`, `UnifiedQueryStrategy` → `QueryStrategy`
- Placeholder dirs: `rai_core/workflow/__init__.py`, `rai_core/governance/__init__.py` (docstring only)
- Update ~30 import sites across src/ and tests/ (or verify re-exports cover them)
- rai-cli pyproject.toml: add `rai-core==X.Y.Z` dependency (exact pin)

## Out of Scope

- Moving governance/PM protocols (`ProjectManagementAdapter`, etc.) — stay in rai_cli
- Moving governance/PM models (`CoreArtifactType`, `ArtifactLocator`, etc.) — stay in rai_cli
- Moving `builder.py` — stays in rai_cli (CLI orchestrator)
- Moving `from_file()` factory — stays as CLI helper
- rai-server package creation (S275.2)
- Any new functionality — pure extraction + rename
- Publishing to PyPI (happens at epic close)

## Done Criteria

- [ ] `uv sync` resolves rai-core as workspace package
- [ ] `from rai_core.graph.models import GraphNode` works
- [ ] `from rai_core.graph.engine import Graph` works
- [ ] `from rai_core.graph.query import QueryEngine` works
- [ ] `from rai_cli.context.models import UnifiedGraph` still works (re-export + alias)
- [ ] `pytest` — full suite passes, zero regressions
- [ ] `pyright` — passes
- [ ] `ruff` — passes
- [ ] No circular imports between rai_core and rai_cli
