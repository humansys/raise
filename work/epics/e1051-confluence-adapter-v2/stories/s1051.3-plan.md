# S1051.3: Config Schema + Pydantic Models — Plan

## Tasks

### T1: ArtifactRouting model + tests
- RED: test ArtifactRouting validates parent_title required, labels default empty
- GREEN: implement ArtifactRouting
- **Gate: pytest green**

### T2: Extend ConfluenceInstanceConfig with routing
- RED: test routing field is optional dict, defaults to empty
- RED: test existing 4-field usage still works (backwards compat)
- GREEN: add `routing: dict[str, ArtifactRouting]` field
- **Gate: existing S1051.1 tests still pass**

### T3: ConfluenceConfig root model
- RED: test full config with default_instance + instances validates
- RED: test get_instance(None) returns default, get_instance("name") returns named
- RED: test get_instance with unknown name raises KeyError
- GREEN: implement ConfluenceConfig
- **Gate: 3 scenarios tested**

### T4: Flat config normalization
- RED: test flat {url, username, space_key} normalizes to ConfluenceConfig
- RED: test flat config get_instance() returns the single instance
- GREEN: implement normalization in ConfluenceConfig or loader
- **Gate: flat and full formats both produce valid ConfluenceConfig**

### T5: resolve_routing helper
- RED: test resolve_routing returns ArtifactRouting for known type
- RED: test resolve_routing returns None for unknown type
- RED: test resolve_routing on specific instance
- GREEN: implement
- **Gate: 3 scenarios tested**

### T6: load_confluence_config loader
- RED: test loads full YAML → ConfluenceConfig
- RED: test loads flat YAML → ConfluenceConfig (normalized)
- RED: test missing file → FileNotFoundError
- RED: test invalid YAML → ValidationError
- GREEN: implement with _CONFLUENCE_YAML_PATH module constant
- **Gate: 4 scenarios tested**

### T7: Final validation
- Run full test suite (including S1051.1 tests)
- pyright + ruff
- **Gate: all green, zero regressions**

## Sequence
T1 → T2 → T3 → T4 → T5 → T6 → T7

Models bottom-up (T1-T2), then composition (T3-T5), then I/O (T6), then validate (T7).
Commit after each task.
