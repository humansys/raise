# S1051.2: PythonApiConfluenceAdapter — Scope

## In Scope

- PythonApiConfluenceAdapter implementing DocumentationTarget (5 methods)
- No-arg constructor that loads .raise/confluence.yaml via load_confluence_config
- publish() with routing: resolve parent page, create/update, apply labels
- can_publish() based on routing config
- get_page(), search(), health() delegating to ConfluenceClient
- Entry point registration in pyproject.toml
- Tests with mocked ConfluenceClient

## Out of Scope

- Async implementation (sync is sufficient — ConfluenceClient is sync)
- SyncDocsAdapter wrapping (direct sync implementation)
- CLI command changes (resolver already handles entry points)
- Discovery service (S1051.4)
- Doctor integration (S1051.5)

## Done When

- isinstance(adapter, DocumentationTarget) is True
- publish routes to correct parent with labels via routing config
- Entry point registered, discoverable via get_doc_targets()
- All tests pass, pyright clean, ruff clean
