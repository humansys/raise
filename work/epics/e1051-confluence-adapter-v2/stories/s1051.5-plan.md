# S1051.5: Adapter Doctor -- Confluence -- Plan

| Field | Value |
|-------|-------|
| Story | S1051.5 |
| Size | S (3 SP) |
| Tasks | 3 |
| Date | 2026-04-03 |

## Task List

### T1: Space existence check (S)

**Description:** Add `_check_confluence_space_exists()` to `AdapterDoctorCheck`. Uses `ConfluenceDiscovery.discover(space_key=...)` -- PASS if found, ERROR with available spaces if `ConfluenceNotFoundError`, ERROR with connectivity hint on any other exception. Called from `evaluate()` after health check passes. Returns bool so T2 can skip on failure (D3).

**Files:**
- Modify: `packages/raise-cli/src/raise_cli/doctor/checks/adapters.py`
- Modify: `packages/raise-cli/tests/doctor/test_adapter_check.py`

**TDD Cycle:**
- RED: Test space exists -> PASS; test space missing -> ERROR with available spaces; test discovery exception -> ERROR with hint
- GREEN: Implement `_check_confluence_space_exists()`, wire into `evaluate()`
- REFACTOR: Extract shared config/client loading if duplicated with `_check_confluence_health`

**AC Reference:** AC1, AC2, AC5, AC6, AC7

**Verification:**
```bash
uv run pytest --tb=short
uv run pyright
uv run ruff check
```

**Dependencies:** None

---

### T2: Routing parent page checks (S)

**Description:** Add `_check_confluence_routing_parents()` to `AdapterDoctorCheck`. Iterates `inst.routing` dict, calls `client.get_page_by_title(routing.parent_title, space=inst.space_key)` per entry. PASS per found page, ERROR per missing page with actionable fix_hint. Check ID includes artifact type suffix (e.g. `adapter-confluence-routing-parent-adr`). Skipped when space check fails (D3). Exception per-page produces ERROR, never crash.

**Files:**
- Modify: `packages/raise-cli/src/raise_cli/doctor/checks/adapters.py`
- Modify: `packages/raise-cli/tests/doctor/test_adapter_check.py`

**TDD Cycle:**
- RED: Test routing parent exists -> PASS; test routing parent missing -> ERROR with page title; test skipped when space check failed; test exception per-page -> ERROR
- GREEN: Implement `_check_confluence_routing_parents()`, wire into `evaluate()` gated on space check result
- REFACTOR: Clean up check ID generation if needed

**AC Reference:** AC3, AC4, AC5, AC6, AC7

**Verification:**
```bash
uv run pytest --tb=short
uv run pyright
uv run ruff check
```

**Dependencies:** T1 (space check must exist for skip-on-failure gate)

---

### T3: Manual integration test (XS)

**Description:** Verify end-to-end with `rai doctor --online` against real Confluence instance. Confirm space check PASS, routing parent checks PASS, and that offline mode skips these new checks. No code changes -- validation only.

**Verification:**
- Run `rai doctor` (offline) -- no space/routing results
- Run `rai doctor --online` -- space check PASS, routing parent checks PASS
- Temporarily misconfigure space_key, re-run -- ERROR with available spaces
- Restore config

**Dependencies:** T1, T2

---

## Execution Order

| Order | Task | Rationale |
|-------|------|-----------|
| 1 | T1 | Foundation -- space check is prerequisite for routing checks |
| 2 | T2 | Depends on T1 for skip-on-failure gate |
| 3 | T3 | End-to-end validation after all code is in place |

## Risks & Mitigations

| Risk | Likelihood | Impact | Mitigation |
|------|-----------|--------|------------|
| Config/client loading duplication with health check | Medium | Low | Refactor to share in T1 if needed |
| `get_page_by_title` signature differs from design assumption | Low | Medium | Check client method signature before coding |
| Offline mode regression | Low | High | Existing test already verifies offline skips health; extend to cover new checks |

## Duration Tracking

| Task | Estimated | Actual | Notes |
|------|-----------|--------|-------|
| T1 | 25 min | | |
| T2 | 25 min | | |
| T3 | 10 min | | |
| **Total** | **60 min** | | |
