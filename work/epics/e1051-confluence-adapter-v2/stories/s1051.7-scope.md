# S1051.7: FilesystemDocsTarget + CompositeDocTarget — Scope

## In Scope

- FilesystemDocsTarget implementing DocumentationTarget (writes markdown to local paths)
- CompositeDocTarget wrapping N targets (publish to all, health/get/search from primary)
- Resolver change: auto-compose when 2+ targets found (instead of erroring)
- Entry point registration for filesystem target
- Routing config in confluence.yaml for all lifecycle artifact types
- Tests for all three components

## Out of Scope

- Skill changes (S1051.8 / S1051.9)
- Async implementation
- Content format conversion (markdown → XHTML) — Confluence API handles this
- FilesystemDocsTarget search/get_page (return empty/raise — filesystem is write-only target)

## Done When

- FilesystemDocsTarget writes files to metadata["path"] paths
- CompositeDocTarget publish delegates to all wrapped targets
- Resolver auto-composes filesystem + confluence when both registered
- `--target confluence` still works for single-target selection
- confluence.yaml has routing for all artifact types
- All tests pass, pyright clean, ruff clean
