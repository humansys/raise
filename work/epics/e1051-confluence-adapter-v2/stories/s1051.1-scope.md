# S1051.1: Confluence Client Wrapper — Scope

## In Scope
1. `ConfluenceClient` class wrapping `atlassian.Confluence` (10 methods)
2. Auth resolution: instance-specific env var → generic env var → clear error
3. Error normalization: all API exceptions → `ConfluenceApiError`
4. Data models: `PageContent`, `PageSummary`, `SpaceInfo`, `AdapterHealth`
5. Optional import guard: clear message if atlassian-python-api not installed
6. Unit tests with mocked `atlassian.Confluence`
7. `pyproject.toml` optional dependency: `raise-cli[confluence]`

## Out of Scope
- `DocumentationTarget` Protocol implementation (S1051.2)
- Config schema / Pydantic models for `.raise/confluence.yaml` (S1051.3)
- Discovery service (S1051.4)
- Doctor checks (S1051.5)
- Entry point registration (S1051.2)
- Live backend integration tests (manual validation only)

## Done Criteria
1. `ConfluenceClient` with 10 methods, all tested with mocks
2. Auth resolution works for instance-specific and generic tokens
3. All API errors normalized to `ConfluenceApiError`
4. `raise-cli[confluence]` installs `atlassian-python-api`
5. Import guard produces clear error when dependency missing
6. Tests pass, types pass (pyright), lint passes
