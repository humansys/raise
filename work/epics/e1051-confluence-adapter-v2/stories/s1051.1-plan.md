# S1051.1: Confluence Client Wrapper — Plan

## Tasks

### T1: Exceptions + Config (foundation, no external dep)
- Create `confluence_exceptions.py` — 3 classes (ConfluenceError, ConfluenceAuthError, ConfluenceNotFoundError, ConfluenceApiError)
- Create `confluence_config.py` — ConfluenceInstanceConfig (Pydantic, 4 fields)
- RED: test exceptions hierarchy, config validation
- GREEN: implement
- **Gate: pytest + pyright green**

### T2: Auth resolution
- RED: test token resolution — instance-specific env var, generic fallback, missing → ConfluenceAuthError
- GREEN: implement `_resolve_token(instance_name)` as staticmethod
- **Gate: 3 scenarios tested (specific, fallback, missing)**

### T3: Client constructor + import guard
- RED: test __init__ wires Confluence client with correct params (url, username, token, cloud=True, backoff_and_retry=True)
- RED: test ImportError when atlassian not installed
- GREEN: implement __init__ with mocked atlassian.Confluence
- **Gate: constructor tested, import guard tested**

### T4: Publishing methods (create_page, update_page, get_page_by_id, get_page_by_title)
- RED: test each method returns correct boundary model (PageContent / None)
- RED: test space param defaults to config.space_key, overridable
- RED: test error mapping — ApiPermissionError → AuthError, ApiNotFoundError → NotFoundError, ApiError → ApiError
- GREEN: implement 4 methods with mocked _client
- **Gate: 4 methods tested, error mapping isinstance-based**

### T5: Label methods (set_labels, get_labels)
- RED: test set_labels calls set_page_label for each label
- RED: test get_labels returns list[str]
- GREEN: implement
- **Gate: 2 methods tested**

### T6: Discovery methods (get_spaces, get_page_children)
- RED: test get_spaces returns list[SpaceInfo]
- RED: test get_page_children returns list[PageSummary]
- GREEN: implement
- **Gate: 2 methods tested**

### T7: Search + Health
- RED: test search with CQL returns list[PageSummary]
- RED: test health returns AdapterHealth (healthy + unhealthy paths)
- GREEN: implement
- **Gate: all 10 methods tested**

### T8: pyproject.toml optional dependency
- Add `confluence = ["atlassian-python-api>=3.41"]` to `[project.optional-dependencies]`
- Verify `uv pip install -e ".[confluence]"` works
- **Gate: dependency installable**

### T9: Final validation
- Run full raise-cli test suite
- Run pyright on adapters/
- Run ruff check + format
- **Gate: all green, zero regressions**

## Sequence
T1 → T2 → T3 → T4 → T5 → T6 → T7 → T8 → T9

Foundation first (T1-T3), then methods by concern (T4-T7), then packaging (T8-T9).
Commit after each task.

## Risk
- T4 is the largest task (4 methods + error mapping). If it gets heavy, split into T4a (create/update) and T4b (get methods).
