# S1051.6: Config Generator — `/rai-adapter-setup` Confluence — Plan

| Field | Value |
|-------|-------|
| Story | S1051.6 |
| Epic | E1051 |
| Size | M (5 SP) |
| Tasks | 4 |
| Date | 2026-04-03 |
| Branch | story/s1051.6/config-generator-confluence |

## Task Overview

| # | Task | Size | Depends | AC |
|---|------|------|---------|-----|
| T1 | `generate_confluence_config()` — v2 pure function | M | — | AC1, AC2, AC3 |
| T2 | `suggest_routing()` — page tree to routing suggestions | S | — | S1 |
| T3 | `write_confluence_config()` — YAML writer with overwrite guard | S | T1 | AC4, AC5, S2 |
| T4 | Skill update + manual integration test | S | T1, T3 | — |

## Execution Order

**T1 → T2 → T3 → T4** (sequential, risk-first)

- T1 is riskiest: new signature, must produce dict passing `ConfluenceConfig.from_dict()`. Existing tests must be updated to new API.
- T2 is independent of T1 logically but shares the module; sequencing avoids merge noise.
- T3 depends on T1 output dict format.
- T4 is integration; depends on all prior tasks.

## Tasks

### T1: `generate_confluence_config()` — v2 pure function (M)

**Goal:** Replace the v1 `generate_confluence_config()` that takes `ConfluenceSpaceMap` with a v2 that takes `list[SpaceInfo]` + `instance_name`, producing a multi-instance dict.

**Files:**
- MODIFY `packages/raise-cli/src/raise_cli/adapters/confluence_config_gen.py`
- MODIFY `packages/raise-cli/tests/adapters/test_confluence_config_gen.py`

**TDD Cycle:**

RED:
- Update existing tests to new signature (`spaces: list[SpaceInfo]`, `instance_name: str`).
- Add test: output dict passes `ConfluenceConfig.from_dict()` with multi-instance format (AC1).
- Add test: `default_instance` equals provided `instance_name`.
- Add test: ValueError when `selected_space` not in spaces list (AC2).
- Add test: default routing applied when `routing=None` (AC3).
- Add test: custom routing dict preserved in output.
- Run tests → all new tests FAIL.

GREEN:
- Rewrite `generate_confluence_config()` with new signature per design:
  - `spaces: list[SpaceInfo]` (replaces `space_map: ConfluenceSpaceMap`)
  - `selected_space: str`
  - `instance_url: str`
  - `instance_name: str` (new param, defaults to `"default"`)
  - `routing: dict[str, ArtifactRouting] | None = None`
- Output multi-instance format: `{default_instance, instances: {name: {url, space_key, instance_name, routing}}}`.
- Import `SpaceInfo` from `raise_cli.adapters.models.docs` (remove `ConfluenceSpaceMap` import).
- Run tests → all GREEN.

REFACTOR:
- Remove unused `ConfluenceSpaceMap` import from module (if no other users).

**Verification:**
```bash
uv run pytest packages/raise-cli/tests/adapters/test_confluence_config_gen.py --tb=short
uv run pyright
uv run ruff check
```

---

### T2: `suggest_routing()` — page tree to routing suggestions (S)

**Goal:** Pure function that takes a `PageNode` tree and returns routing suggestions by matching top-level page titles against known artifact type keywords.

**Files:**
- MODIFY `packages/raise-cli/src/raise_cli/adapters/confluence_config_gen.py`
- MODIFY `packages/raise-cli/tests/adapters/test_confluence_config_gen.py`

**TDD Cycle:**

RED:
- Test: tree with "Architecture Decision Records" child → `{"adr": ArtifactRouting(parent_title="Architecture Decision Records", labels=["adr"])}`.
- Test: tree with "Roadmaps" child → `{"roadmap": ...}`.
- Test: tree with "Developer Docs" child → `{"developer": ...}`.
- Test: tree with no matching children → empty dict.
- Test: tree with multiple matches → all returned.
- Run tests → FAIL.

GREEN:
- Implement `suggest_routing(tree: PageNode) -> dict[str, ArtifactRouting]`.
- Define keyword map: `{"adr": ["architecture decision", "adr"], "roadmap": ["roadmap"], "developer": ["developer doc", "developer guide"], "retrospective": ["retrospective", "retro"]}`.
- Match top-level children titles (case-insensitive substring) against keywords.
- Run tests → GREEN.

REFACTOR:
- Extract keyword map to module-level constant `_ROUTING_KEYWORDS`.

**Verification:**
```bash
uv run pytest packages/raise-cli/tests/adapters/test_confluence_config_gen.py --tb=short
uv run pyright
uv run ruff check
```

---

### T3: `write_confluence_config()` — YAML writer with overwrite guard (S)

**Goal:** I/O function that writes a config dict to `.raise/confluence.yaml` with overwrite protection and optional comments.

**Files:**
- MODIFY `packages/raise-cli/src/raise_cli/adapters/confluence_config_gen.py`
- MODIFY `packages/raise-cli/tests/adapters/test_confluence_config_gen.py`

**TDD Cycle:**

RED:
- Test: writes valid YAML to `project_root / .raise/confluence.yaml` (use `tmp_path`) (AC4).
- Test: written YAML round-trips through `ConfluenceConfig.from_dict()`.
- Test: raises `FileExistsError` when file exists and `overwrite=False` (AC5).
- Test: succeeds when file exists and `overwrite=True`.
- Test: creates `.raise/` directory if missing.
- Run tests → FAIL.

GREEN:
- Implement `write_confluence_config(config_dict: dict[str, Any], project_root: Path, overwrite: bool = False) -> Path`.
- Check existence → raise `FileExistsError` with clear message.
- `mkdir(parents=True, exist_ok=True)` for `.raise/`.
- Write YAML with header comment (S2).
- Return the written path.
- Run tests → GREEN.

REFACTOR:
- Ensure comment header is clean and minimal.

**Verification:**
```bash
uv run pytest packages/raise-cli/tests/adapters/test_confluence_config_gen.py --tb=short
uv run pyright
uv run ruff check
```

---

### T4: Skill update + manual integration test (S)

**Goal:** Update `/rai-adapter-setup` SKILL.md Confluence section to use v2 discovery and new `generate_confluence_config()` signature. Verify end-to-end.

**Files:**
- MODIFY `.claude/skills/rai-adapter-setup/SKILL.md`

**Changes:**
- Step 2 Confluence Discovery: replace `ConfluenceDiscovery` with `ConfluenceDiscoveryService`, use `discover_spaces()` instead of `discover()`.
- Step 3 Confluence Config Generation: update `generate_confluence_config()` call to new signature (`spaces=`, `instance_name=`), add `write_confluence_config()` call.
- Step 4: reference `write_confluence_config()` instead of raw `yaml.dump`.

**TDD Cycle:** N/A (skill is a markdown document, not code). Verification is manual.

**Manual Integration Test:**
- Read the updated skill and verify: imports are correct, function signatures match implementation, flow is coherent.
- Confirm no references to `ConfluenceSpaceMap` or old `ConfluenceDiscovery` remain in the skill.

**Verification:**
```bash
uv run pytest --tb=short
uv run pyright
uv run ruff check
```

## Risks & Mitigations

| Risk | Impact | Mitigation |
|------|--------|------------|
| `ConfluenceSpaceMap` used elsewhere | Breaking imports | Grep for all usages before removing; keep class if other consumers exist |
| `from_dict()` validation edge cases | AC1 fails on unexpected format | Test with actual `ConfluenceConfig.from_dict()` in every test |
| `suggest_routing()` keyword false positives | Suggests wrong routing | Keep keyword lists tight; substring match on title only |

## Duration Tracking

| Task | Estimated | Actual | Notes |
|------|-----------|--------|-------|
| T1 | 25 min | — | |
| T2 | 15 min | — | |
| T3 | 15 min | — | |
| T4 | 10 min | — | |
| **Total** | **65 min** | — | |
