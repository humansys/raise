# Retrospective: S354.4 — Doc Generation

## What went well
- Markdown template approach (user suggestion) is cleaner than f-strings
- Template is human-readable and customizable without Python changes
- Empty section removal works cleanly with regex post-processing
- Inline fallback ensures renderer works even without template file

## What could improve
- `_artifact_filename` imported from writer (private) — 3rd consumer now. Consider making it public or extracting to a shared utility.

## Patterns
- Markdown template + str.format() is a good middle ground between f-strings and Jinja for small template counts
- Pre-rendering repeating blocks in Python keeps the template clean (no loop syntax)

## Metrics
- Tests: 10 new, 47 total artifact tests
- Files: 1 new production, 1 new test, 1 template
