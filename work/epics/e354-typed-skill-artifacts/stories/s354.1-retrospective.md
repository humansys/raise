# Retrospective: S354.1 — Base Artifact Model + Storage

## What went well
- TDD cycle was clean: 20 tests, all red-green-refactor
- Module structure is minimal (4 files, 3 models, 2 functions)
- Round-trip test validates the whole pipeline
- Ruff auto-fix caught 4 minor issues early

## What could improve
- Docstring said "skips" but code raised — caught in QR, should have been right the first time
- Type checking (mypy/pyright) not available in worktree — should verify setup

## Patterns
- Flat YAML (no wrapper key) with `model_dump(mode="json")` works cleanly for Pydantic → YAML round-trips
- `_artifact_filename` convention `{id}-{suffix}.yaml` is simple and extensible

## Metrics
- Tests: 20 (9 model + 5 writer + 5 reader + 1 round-trip)
- Files: 4 production + 4 test
- LOC: ~90 production, ~180 test
