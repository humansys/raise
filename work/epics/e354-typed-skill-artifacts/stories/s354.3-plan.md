# Plan: S354.3 — Graph Ingestion

> **Story:** S354.3 | **Epic:** E354

## Tasks

### T1: Test load_artifacts (TDD)
- RED: Test load_artifacts returns GraphNodes from .raise/artifacts/
- RED: Test node id, type, content, metadata fields
- RED: Test empty/missing directory returns empty list
- GREEN: Implement load_artifacts() in builder.py

### T2: Wire into build() + integration test
- RED: Test build() includes artifact nodes in graph
- GREEN: Add load_artifacts() call to build()
- Lint + full test suite

## Verification
- `uv run pytest tests/unit/context/ -k artifact -v` passes
- `uv run ruff check src/rai_cli/context/builder.py`
