# Plan: S354.1 — Base Artifact Model + Storage

> **Story:** S354.1 | **Epic:** E354

## Tasks

### T1: Create module skeleton
- Create `src/rai_cli/artifacts/__init__.py` (empty)
- Create `src/rai_cli/artifacts/models.py` (empty)
- Create `src/rai_cli/artifacts/writer.py` (empty)
- Create `src/rai_cli/artifacts/reader.py` (empty)
- Create `tests/unit/artifacts/` with `__init__.py` and `conftest.py`

### T2: Implement models (TDD)
- RED: Test `ArtifactType` enum has `STORY_DESIGN` value
- RED: Test `ArtifactRefs` accepts optional fields
- RED: Test `SkillArtifact` creation with all fields
- RED: Test `SkillArtifact` validation (version >= 1, artifact_type required)
- GREEN: Implement `ArtifactType`, `ArtifactRefs`, `SkillArtifact` in models.py
- REFACTOR: Verify Field descriptors and defaults

### T3: Implement writer (TDD)
- RED: Test `write_artifact` creates YAML file at correct path
- RED: Test filename convention `{story}-{type}.yaml` (e.g., `s354.1-design.yaml`)
- RED: Test creates `.raise/artifacts/` directory if missing
- RED: Test YAML content matches model fields
- GREEN: Implement `write_artifact(artifact, project_root) → Path`
- REFACTOR: Ensure datetime serialization is ISO format

### T4: Implement reader (TDD)
- RED: Test `read_artifact` loads and validates YAML file
- RED: Test `read_all_artifacts` returns list from directory
- RED: Test invalid YAML raises ValidationError
- GREEN: Implement `read_artifact(path)` and `read_all_artifacts(dir)`
- REFACTOR: Error messages for invalid files

### T5: Round-trip test + exports
- RED: Test write → read → compare (round-trip integrity)
- GREEN: Should pass with existing implementations
- Update `__init__.py` with public exports
- Commit

## Dependencies
- None (foundation story)

## Verification
- `uv run pytest tests/unit/artifacts/ -v` passes
- `uv run mypy src/rai_cli/artifacts/` passes
- `uv run ruff check src/rai_cli/artifacts/` passes
