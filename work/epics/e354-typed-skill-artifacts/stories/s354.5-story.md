# Story: S354.5 — Pilot: Wire rai-story-design

> **Epic:** E354 — Typed Skill Artifacts
> **Branch:** `story/s354.5/pilot-wiring`
> **Size:** M
> **Depends:** S354.2 (schema), S354.4 (doc generation)

## Summary

Update the `rai-story-design` SKILL.md to produce a typed YAML artifact instead of free-form Markdown. Add `raise.output_type: story-design` to frontmatter. Update output section to write `.raise/artifacts/` YAML and generate Markdown docs. Clean replacement (AD4).

## Acceptance Criteria

1. SKILL.md frontmatter declares `raise.output_type: story-design`
2. Skill output section instructs writing to `.raise/artifacts/{id}-design.yaml`
3. Skill output section instructs generating Markdown via `render_artifact`
4. `SkillFrontmatter` model parses `raise.output_type` field
5. E2E validation: skill produces artifact → Pydantic validates → graph ingests → docs generated
