---
story_id: "S347.2"
epic_ref: "E347"
---

# Scope: FileAdapter Parity (Stories, Links, Comments)

## In Scope

- Parse stories from epic scope docs (`work/epics/e{N}-{name}/scope.md` Stories table)
- Route by key pattern: `E{N}` -> epic ops (existing), `S{N}.{M}` -> story ops (new)
- Story CRUD: search, get_issue, create_issue, update_issue, transition_issue
- Story comments: add_comment persists to story file, get_comments reads from story file
- Story links: link_issues updates Dependencies column in scope doc
- Batch transition support for stories
- All existing epic operations remain unchanged (backward compatible)

## Out of Scope

- Changing the `ProjectManagementAdapter` protocol (it is complete)
- Jira adapter changes (S347.2 is FileAdapter only)
- New CLI commands (adapter is internal, CLI unchanged)
- Bidirectional sync between adapters
- Story creation outside of epic scope docs (standalone stories)
- Rich comment formatting (plain text with timestamp is sufficient)

## Done Criteria

1. `search("S347")` returns story IssueSummary objects from scope docs
2. `get_issue("S347.1")` returns IssueDetail for stories
3. `create_issue(project_key, IssueSpec(type="Story"))` appends to scope doc
4. `transition_issue("S347.1", "in_progress")` updates scope doc status
5. `add_comment("S347.1", "text")` persists to story markdown file
6. `get_comments("S347.1")` returns comments from story file
7. `link_issues("S347.2", "S347.1", "depends_on")` records in scope doc
8. All existing epic tests still pass (no regressions)
9. New tests cover all story operations with TDD
10. `isinstance(FilesystemPMAdapter(...), ProjectManagementAdapter)` remains True
