---
story_id: "S347.1"
epic_ref: "E347"
size: "S"
date: "2026-03-03"
---

# Plan: S347.1 — Adapter Default in Manifest

## Tasks

### T1: Add BacklogConfig to manifest model + tests (S)

**RED:** Write test that `ProjectManifest` accepts `backlog.adapter_default` field and round-trips correctly. Test that omitting `backlog` section works (backward compat).

**GREEN:** Add `BacklogConfig` Pydantic model and `backlog: BacklogConfig | None = None` to `ProjectManifest`.

**REFACTOR:** Verify `load_manifest()` / `save_manifest()` need no changes (Pydantic handles it).

**Files:** `src/rai_cli/onboarding/manifest.py`, `tests/onboarding/test_manifest.py`
**Verify:** `uv run pytest tests/onboarding/test_manifest.py -x`
**AC ref:** Story AC #1 (manifest field exists)

### T2: Update resolve_adapter() to read manifest default + tests (S)

**RED:** Write tests for all 4 resolution scenarios:
1. Explicit `-a` flag → uses flag (existing behavior)
2. No flag, manifest default set → uses manifest default
3. No flag, no manifest, single adapter → auto-detect
4. No flag, no manifest, multiple adapters → error with guidance

**GREEN:** Modify `resolve_adapter()` to load manifest and read `backlog.adapter_default` when `adapter_name is None`.

**REFACTOR:** Ensure `resolve_entrypoint()` is untouched (generic).

**Files:** `src/rai_cli/cli/commands/_resolve.py`, `tests/cli/commands/test_resolve.py`
**Depends on:** T1
**Verify:** `uv run pytest tests/cli/commands/test_resolve.py -x`
**AC ref:** Story AC #1-4

### T3: Update manifest.yaml + integration validation (XS)

**GREEN:** Add `backlog.adapter_default: jira` to `.raise/manifest.yaml`.

**Verify:** `uv run rai backlog search 'project = "RAISE"' -n 1` (no `-a` flag, should work). Full test suite: `uv run pytest --tb=short`. Lint + types: `uv run ruff check src/ tests/` and `uv run pyright`.

**Files:** `.raise/manifest.yaml`
**Depends on:** T2
**AC ref:** Story AC #5 (integration)

## Execution Order

T1 → T2 → T3 (linear, each depends on previous)

## Risks

| Risk | Mitigation |
|------|------------|
| `load_manifest()` uses `Path.cwd()` which may not be project root in tests | Use `tmp_path` fixtures and monkeypatch |
| Existing tests may rely on no `backlog` field in manifest | Backward compat guaranteed by `Optional` field |

## Duration Tracking

| Task | Estimated | Actual | Notes |
|------|-----------|--------|-------|
| T1 | 10min | | |
| T2 | 15min | | |
| T3 | 5min | | |
