---
story_id: "S347.7"
epic_ref: "E347"
status: "in_progress"
---

# Scope: S347.7 Integration Tests + Dogfood

## In Scope

- Parameterized test suite exercising all ProjectManagementAdapter protocol methods
- Tests run against both McpJiraAdapter (mocked MCP bridge) and FilesystemPMAdapter
- Same assertions for both adapters — protocol parity validation
- Adapter default resolution tested (manifest.yaml integration)
- Dogfood review: verify E347 lifecycle used rai backlog CLI throughout
- Test coverage for error paths (adapter unavailable, invalid transitions)

## Out of Scope

- Live Jira integration tests (require credentials, not CI-friendly)
- Performance benchmarks
- New adapter types
- UI or dashboard for test results
- Modifying adapter implementations (test-only story)

## Done Criteria

1. Integration test suite covers all 7 ProjectManagementAdapter protocol methods (create, search, get, transition, comment, link, update)
2. Both adapters pass the same parameterized test cases
3. Adapter default resolution is tested with manifest.yaml config
4. Tests run in CI without external dependencies (mocked MCP, temp dirs)
5. Dogfood review documented — E347 lifecycle verified
6. All existing tests still pass (no regressions)
