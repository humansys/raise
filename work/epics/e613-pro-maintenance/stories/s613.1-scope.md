# S613.1: rai doctor ACLI Check — Scope

## In Scope

- `AcliCheck` class implementing `DoctorCheck` protocol
- Check 1: ACLI binary in PATH (`shutil.which`)
- Check 2: ACLI authenticated (`acli jira auth status`, requires `--online`)
- Entry point registration in `packages/raise-pro/pyproject.toml`
- Unit tests with mocked subprocess

## Out of Scope

- Multi-instance auth verification (check each site)
- jira.yaml validation (could be a separate check)
- Auto-fix (install ACLI automatically)

## Done Criteria

- [ ] `rai doctor` shows ACLI availability check
- [ ] Clear fix_hint when ACLI missing or not authenticated
- [ ] Auth check only runs with `--online` flag
- [ ] Unit tests cover all 3 scenarios
- [ ] All gates pass
