# S1248.3 Implementation Plan

> **Story:** S1248.3 — Bundle Integration + CLAUDE.local.md Kill
> **Size:** M (3 tasks)
> **Date:** 2026-04-03
> **Branch:** `story/s1248.3/bundle-integration-claude-local-kill`

## Tasks

### T1: Wire GitStateDeriver into bundle assembly (M)

**RED:** Test that `assemble_orientation()` uses git-derived `current_work` when git is available. Test fallback to YAML state when git derivation fails. Test that output format is unchanged.

**GREEN:**
- Modify `format_work_section()` to accept optional `current_work: CurrentWork | None` parameter (overrides `state.current_work`)
- Modify `assemble_orientation()` to call `GitStateDeriver().current_work(project_path)` before formatting
- Wrap in try/except RuntimeError for graceful fallback

**REFACTOR:** Extract derivation into a helper if needed.

**Files:**
- MODIFY: `packages/raise-cli/src/raise_cli/session/bundle.py`
- MODIFY: `packages/raise-cli/src/raise_cli/session/bundle_formatters.py`
- NEW/MODIFY: `packages/raise-cli/tests/session/test_bundle_derive.py`

**Verification:** `uv run pytest --tb=short`, `uv run ruff check`, `uv run pyright`

**Dependencies:** None

### T2: Remove CLAUDE.local.md references (S)

**GREEN:** Grep for all CLAUDE.local.md references in docs and remove/update them.

**Files:**
- MODIFY: `dev/context.md`
- MODIFY: `dev/happy-path-guide.md`
- MODIFY: `dev/happy-path-guide-es.md`
- Others found by grep

**Verification:** `grep -r "CLAUDE.local" .` returns zero results

**Dependencies:** None (parallel with T1)

### T3: Integration test — bundle with git-derived state (S)

**RED:** Test that `assemble_context_bundle()` in a real git repo produces output with correct branch and epic info derived from git (not YAML).

**GREEN:** Integration test using real repo path.

**Files:**
- NEW: `packages/raise-cli/tests/session/test_bundle_integration.py`

**Verification:** `uv run pytest -m integration --tb=short`

**Dependencies:** T1

## Execution Order

```
T1 (bundle wiring) ──→ T3 (integration)
T2 (doc cleanup, parallel with T1)
```

## Duration Tracking

| Task | Estimated | Actual | Status |
|------|-----------|--------|--------|
| T1 | 30 min | — | Pending |
| T2 | 10 min | — | Pending |
| T3 | 15 min | — | Pending |
| **Total** | **55 min** | — | — |
