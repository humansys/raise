# S1248.1 Plan: Session Protocols + GitStateDeriver

> **Size:** M | **Tasks:** 4 | **Date:** 2026-04-03
> **Branch:** story/s1248.1/session-protocols-git-deriver

## Task Overview

| # | Task | Size | Depends | Files |
|---|------|------|---------|-------|
| T1 | Protocol contracts + session models | S | — | `session/protocols.py` (NEW), `schemas/session_state.py` (MODIFY) |
| T2 | GitStateDeriver + worktree resolution | M | T1 | `session/derive.py` (NEW) |
| T3 | Entry points + registry discovery | XS | T1 | `pyproject.toml` (MODIFY), `adapters/registry.py` (MODIFY) |
| T4 | Integration test — verify derivation in real repo | XS | T2, T3 | `tests/session/test_derive_integration.py` (NEW) |

## Tasks

### T1: Protocol Contracts + Session Models

**Description:** Define the three session protocols (StateDeriver, SessionRegistry, WorkstreamMonitor) and add supporting Pydantic models to the schema module.

**TDD Cycle:**
- RED: Test that protocols are runtime_checkable, test model construction/validation
- GREEN: Define protocols and models
- REFACTOR: Align naming with existing adapter protocol conventions

**Files:**
- CREATE `packages/raise-cli/src/raise_cli/session/protocols.py`
  - `StateDeriver` protocol: `current_work(project: Path) -> CurrentWork`, `recent_activity(project: Path, limit: int) -> list[ActivityEntry]`
  - `SessionRegistry` protocol: `register(session: SessionInfo) -> None`, `active(project: Path | None) -> list[SessionInfo]`, `close(session_id: str, outcome: SessionOutcome) -> None`, `gc(max_age_hours: int) -> list[str]`
  - `WorkstreamMonitor` protocol: `analyze_session(session_id: str) -> SessionInsights`, `suggest_improvements(last_n: int) -> list[Improvement]`
- MODIFY `packages/raise-cli/src/raise_cli/schemas/session_state.py`
  - Add: `ActivityEntry`, `SessionInfo`, `SessionOutcome`, `SessionInsights`, `Improvement`
- CREATE `packages/raise-cli/tests/session/test_protocols.py`

**Verification:**
```bash
uv run pytest packages/raise-cli/tests/session/test_protocols.py --tb=short
uv run pyright
uv run ruff check
```

### T2: GitStateDeriver + Worktree Resolution

**Description:** Implement the git-only StateDeriver backend. Derives current_work from branch name, scope.md frontmatter, and git log. Includes worktree-aware path resolution.

**TDD Cycle:**
- RED: Test current_work for release branch, story branch, worktree, detached HEAD, no scope.md
- GREEN: Implement git commands, branch parsing, scope.md grep, phase inference
- REFACTOR: Extract helpers (_parse_release, _parse_story, _find_active_epic, _infer_phase, _resolve_project_root)

**Files:**
- CREATE `packages/raise-cli/src/raise_cli/session/derive.py`
  - `GitStateDeriver` class implementing `StateDeriver` protocol
  - `_resolve_project_root(project: Path) -> Path` — worktree awareness via `git rev-parse --git-common-dir`
  - `_git_current_branch(project: Path) -> str`
  - `_parse_release(branch: str) -> str`
  - `_parse_story(branch: str) -> str`
  - `_find_active_epic(project: Path) -> str`
  - `_infer_phase(project: Path, branch: str) -> str`
  - `_recent_activity(project: Path, limit: int) -> list[ActivityEntry]`
- CREATE `packages/raise-cli/tests/session/test_derive.py`
  - Test each derivation case with mocked git output (subprocess.run patches)
  - Test worktree resolution logic
  - Test edge cases: detached HEAD, no scope.md, empty git log

**Verification:**
```bash
uv run pytest packages/raise-cli/tests/session/test_derive.py --tb=short
uv run pyright
uv run ruff check
```

### T3: Entry Points + Registry Discovery

**Description:** Register GitStateDeriver as entry point and add discovery functions to the adapter registry.

**TDD Cycle:**
- RED: Test that `get_state_derivers()` returns discovered implementations
- GREEN: Add entry point and discovery function
- REFACTOR: Consistent with existing registry pattern

**Files:**
- MODIFY `packages/raise-cli/pyproject.toml`
  - Add `[project.entry-points."rai.session.state_deriver"]` with `git = "raise_cli.session.derive:GitStateDeriver"`
- MODIFY `packages/raise-cli/src/raise_cli/adapters/registry.py`
  - Add `EP_STATE_DERIVERS`, `EP_SESSION_REGISTRIES`, `EP_WORKSTREAM_MONITORS` constants
  - Add `get_state_derivers()`, `get_session_registries()`, `get_workstream_monitors()` functions
- CREATE `packages/raise-cli/tests/adapters/test_registry_session.py`

**Verification:**
```bash
uv run pytest packages/raise-cli/tests/adapters/test_registry_session.py --tb=short
uv run pyright
uv run ruff check
```

### T4: Integration Test — Real Repo Derivation

**Description:** Validate GitStateDeriver against the actual raise-commons repo state. Not mocked — runs real git commands.

**TDD Cycle:**
- RED: Test that current_work returns non-empty branch for this repo
- GREEN: Already passing from T2 implementation
- REFACTOR: Add assertions for specific expected values

**Files:**
- CREATE `packages/raise-cli/tests/session/test_derive_integration.py`
  - `test_derives_current_branch_in_real_repo` — verifies branch detection
  - `test_finds_active_epic_in_real_repo` — verifies scope.md parsing finds E1248
  - `test_recent_activity_returns_entries` — verifies git log parsing
  - Mark with `@pytest.mark.integration` for CI filtering

**Verification:**
```bash
uv run pytest packages/raise-cli/tests/session/test_derive_integration.py --tb=short -v
uv run pyright
uv run ruff check
```

## Execution Order

```
T1 (protocols + models) ──► T2 (GitStateDeriver) ──► T4 (integration)
                          ──► T3 (entry points)   ──►
```

T1 first (foundation). T2 and T3 can be done sequentially in same session (T3 is XS). T4 closes with real-repo validation.

**Risk-first rationale:** T1 defines the API surface that S1248.2/S1248.3/S1248.4 depend on. Getting protocols wrong blocks everything. T2 is the novel code (git parsing, worktree detection). T3 is mechanical. T4 validates the whole thing.

## Duration Tracking

| Task | Estimated | Actual | Notes |
|------|-----------|--------|-------|
| T1 | — | — | |
| T2 | — | — | |
| T3 | — | — | |
| T4 | — | — | |
