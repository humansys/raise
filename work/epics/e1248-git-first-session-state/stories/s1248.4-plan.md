# S1248.4 Implementation Plan

> **Story:** S1248.4 — WorkstreamMonitor Basics
> **Size:** S (2 tasks)
> **Branch:** `story/s1248.4/workstream-monitor-basics`

## Tasks

### T1: LocalWorkstreamMonitor — analyze + suggest (S)

**RED:** Test protocol compliance, analyze_session returns valid SessionInsights,
suggest_improvements returns list of Improvement.

**GREEN:** Implement in `session/monitor.py`:
- `analyze_session(session_id)` — git log heuristics: count commits, count test commits
  (subject starts with "test"), count reverts, estimate duration from first-to-last commit
- `suggest_improvements(last_n)` — simple heuristics: low test ratio, high revert count

**Files:** NEW `session/monitor.py`, NEW `tests/session/test_monitor.py`

### T2: Entry point + discovery (XS)

Register `rai.session.monitor` → `local` in pyproject.toml. Test discovery.

## Duration: ~45 min
