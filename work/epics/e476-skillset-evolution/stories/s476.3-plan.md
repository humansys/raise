# S476.3 Plan: Create raise-dev Skillset

**Size:** XS (1 SP) | **Tasks:** 2

---

## Task 1: Scaffold raise-dev skillset directory

**Goal:** Create `.raise/skills/raise-dev/` with subdirectories for the 3 skills.

**Steps:**
1. Run `rai skill set create raise-dev --empty` to scaffold the directory
2. If CLI is not ready, manually create `.raise/skills/raise-dev/`
3. Create subdirectories: `rai-story-implement/`, `rai-bugfix/`, `rai-story-plan/`

**Verify:** Directory structure exists at `.raise/skills/raise-dev/` with 3 subdirs.

---

## Task 2: Create overlay SKILL.md files

**Goal:** Add minimal overlay SKILL.md to each subdirectory with Python verification commands.

**Files to create:**
- `.raise/skills/raise-dev/rai-story-implement/SKILL.md`
- `.raise/skills/raise-dev/rai-bugfix/SKILL.md`
- `.raise/skills/raise-dev/rai-story-plan/SKILL.md`

**Content per overlay:** Minimal markdown with:
- Identification header (skill name, overlay purpose)
- Verification Commands section with explicit Python commands:
  - `uv run pytest --tb=short -x`
  - `uv run ruff check`
  - `uv run pyright --pythonversion 3.11`
- Per-skill notes on which base section this replaces (per design doc table)

**Verify:** 3 overlay files exist, each contains pytest/pyright/ruff commands, each is minimal (not a full skill copy).

**Commit:** `feat(s476.3): create raise-dev skillset with Python overlays`

---

## Done Criteria (from scope)
- [x] `.raise/skills/raise-dev/` exists with 3 overlay files
- [x] Each overlay contains explicit Python tool commands (pytest, pyright, ruff)
- [x] Overlays are minimal -- only verification sections, not full skill copies
