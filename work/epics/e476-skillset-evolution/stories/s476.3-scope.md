# S476.3: Scope

## In Scope
- Create `.raise/skills/raise-dev/` directory structure
- Overlay for rai-story-implement: Python verification (pytest, pyright strict, ruff)
- Overlay for rai-bugfix: Python verification commands
- Overlay for rai-story-plan: Python verification commands in plan template
- Each overlay replaces only verification sections, inheriting everything else from base

## Out of Scope
- Changes to base skill SKILL.md files (done in S476.2)
- Skillset resolution/merge logic (S476.4)
- Other language skillsets (future work)
- CLI `rai skill set create` implementation (use manual creation if CLI not ready)

## Done When
- `.raise/skills/raise-dev/` exists with 3 overlay files
- Each overlay contains explicit Python tool commands (pytest, pyright, ruff)
- Overlays are minimal -- only verification sections, not full skill copies
