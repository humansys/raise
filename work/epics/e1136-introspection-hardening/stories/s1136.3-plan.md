# S1136.3 Plan — Auto-commit Learning Records

**Story:** S1136.3
**Size:** XS (1 SP)
**Date:** 2026-04-03
**Branch:** story/s1136.3/auto-commit-learnings

## Overview

Add explicit instructions to skill files so that learning records under `.raise/rai/learnings/` are staged and committed alongside task artifacts. Three files need edits — all are Markdown skill docs, no code changes.

## Tasks

### T1: Update skill commit instructions to include learning records

**Description:** Edit three files to ensure `.raise/rai/learnings/` is staged with every task commit:

1. **`.claude/skills/rai-story-implement/SKILL.md`** — Step 4 "Commit & Checkpoint": add explicit `git add .raise/rai/learnings/` instruction before the commit step.
2. **`.claude/skills/rai-story-review/SKILL.md`** — LEARN section: add a note that the learning record must be staged and committed (story-review produces records but has no explicit commit step — add one).
3. **`aspects/introspection.md`** — LEARN Protocol section: add a step after "Write learning record" to stage the record for commit (`git add .raise/rai/learnings/`).

**Files to modify:**
- `.claude/skills/rai-story-implement/SKILL.md`
- `.claude/skills/rai-story-review/SKILL.md`
- `aspects/introspection.md`

**TDD cycle:**
- RED: No test — this is a docs-only change to Markdown skill files. No executable code is modified.
- GREEN: Apply the edits.
- REFACTOR: Ensure wording is consistent across all three files.

**Verification:**
- `grep -r "learnings" .claude/skills/rai-story-implement/SKILL.md` shows the new staging instruction
- `grep -r "learnings" .claude/skills/rai-story-review/SKILL.md` shows the commit/staging instruction
- `grep -r "git add.*learnings" aspects/introspection.md` shows the staging step
- `uv run ruff check` passes (no Python changes)
- `uv run pyright` passes (no Python changes)

**Size:** S
**Dependencies:** None

### T2: Manual integration test

**Description:** Verify end-to-end that the instructions are clear and actionable:
1. Read each modified file in context and confirm the new instructions are unambiguous
2. Confirm no contradictions with existing instructions
3. Verify the `.raise/rai/learnings/` path matches what `rai learn write` actually produces

**Verification:** Human review of the three modified files.

**Size:** XS
**Dependencies:** T1

## Execution Order

1. **T1** — All three file edits (no dependencies between files, but they form one logical unit)
2. **T2** — Manual verification after edits

**Rationale:** Straightforward linear order. T1 is the only real task; T2 is validation.

## Risks & Mitigations

| Risk | Likelihood | Impact | Mitigation |
|------|-----------|--------|------------|
| Instructions not followed by AI agent | Low | Medium | Place instruction at decision point (Step 4), not in footnotes |
| Path mismatch with `rai learn write` output | Low | High | Verify actual output path before editing |

## Duration Tracking

| Task | Estimated | Actual | Notes |
|------|-----------|--------|-------|
| T1 | 10 min | | |
| T2 | 5 min | | |
| **Total** | **15 min** | | |
