# S1136.2: `rai learn write` CLI — Schema Validation

**Epic:** E1136 — Introspection Hardening
**Tracker:** RAISE-1237
**Type:** code
**Size:** XS

## User Story

As the LEARN protocol, I need a CLI command `rai learn write` that validates a learning record against the Pydantic model before writing to disk, so that schema drift (like S1064.8's nested format) is impossible.

## References

- E1136 scope: F2 (Schema Drift)
- LearningRecord model: `packages/raise-cli/src/raise_cli/memory/learning.py`
- `write_record()` / `read_record()` already exist
