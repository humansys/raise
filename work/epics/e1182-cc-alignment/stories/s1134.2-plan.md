# S1134.2: Allowed-Tools Declaration — Plan

## Prerequisites
- [x] Design complete (s1134.2-design.md) — 7 tiers, 35 skills classified
- [x] Scope committed (s1134.2-scope.md)

## Tasks

### T1: Add allowed-tools to T1 Read-only skills (7 files) ~5min
**Skills:** rai-architecture-review, rai-code-audit, rai-doctor, rai-mcp-status, rai-problem-shape, rai-quality-review, rai-session-start

**Tools list:**
```yaml
allowed-tools:
  - Read
  - Grep
  - Glob
```

**Steps:**
1. Read each SKILL.md frontmatter
2. Add `allowed-tools` block after `disable-model-invocation` (or `description` if absent), before `license`
3. Verify YAML valid on all 7 files

### T2: Add allowed-tools to T2 Read+CLI skills (5 files) ~4min
**Skills:** rai-epic-plan, rai-session-close, rai-skillset-manage, rai-story-plan, rai-story-review

**Tools list:**
```yaml
allowed-tools:
  - Read
  - Grep
  - Glob
  - "Bash(rai:*)"
```

**Steps:**
1. Read each SKILL.md frontmatter
2. Add `allowed-tools` block in correct position
3. Verify YAML valid on all 5 files

### T3: Add allowed-tools to T3 Read+Write+CLI skills (11 files) ~8min
**Skills:** rai-discover, rai-docs-update, rai-epic-design, rai-epic-docs, rai-mcp-add, rai-mcp-remove, rai-project-create, rai-project-onboard, rai-skill-create, rai-story-design, rai-welcome

**Tools list:**
```yaml
allowed-tools:
  - Read
  - Edit
  - Write
  - Grep
  - Glob
  - "Bash(rai:*)"
```

**Steps:**
1. Read each SKILL.md frontmatter
2. Add `allowed-tools` block in correct position
3. Verify YAML valid on all 11 files

### T4: Add allowed-tools to T4 Read+Write+CLI+Git skills (6 files) ~5min
**Skills:** rai-epic-close, rai-epic-start, rai-framework-sync, rai-publish, rai-story-close, rai-story-start

**Tools list:**
```yaml
allowed-tools:
  - Read
  - Edit
  - Write
  - Grep
  - Glob
  - "Bash(rai:*)"
  - "Bash(git:*)"
```

**Steps:**
1. Read each SKILL.md frontmatter
2. Add `allowed-tools` block in correct position
3. Verify YAML valid on all 6 files

### T5: Add allowed-tools to T5/T6/T7 skills (6 files) ~5min

**T5 Full dev — rai-bugfix, rai-debug, rai-story-implement:**
```yaml
allowed-tools:
  - Read
  - Edit
  - Write
  - Grep
  - Glob
  - Bash
```

**T6 Research — rai-research:**
```yaml
allowed-tools:
  - Read
  - Grep
  - Glob
  - "Bash(ddgr:*)"
  - WebFetch
  - WebSearch
```

**T7 Orchestration — rai-epic-run, rai-story-run:**
```yaml
allowed-tools:
  - Read
  - Grep
  - Glob
  - Bash
  - Agent
  - Skill
```

**Steps:**
1. Read each SKILL.md frontmatter
2. Add tier-specific `allowed-tools` block in correct position
3. Verify YAML valid on all 6 files

### T6: Validate all 35 files + commit ~3min
**Steps:**
1. Count skills with `allowed-tools` — expect 35
2. Verify no bare `Bash` in T1-T4/T6 skills
3. Validate YAML frontmatter on all 35 files
4. Commit

**Verification commands:**
```bash
# Verify all 35 skills have allowed-tools
for f in .claude/skills/*/SKILL.md; do
  skill=$(basename $(dirname $f))
  grep -q 'allowed-tools:' "$f" || echo "MISSING: $skill"
done

# Verify YAML valid
for f in .claude/skills/*/SKILL.md; do
  python3 -c "import yaml; yaml.safe_load(open('$f').read().split('---')[1])" 2>&1 || echo "FAIL: $f"
done
```

## Summary

| Task | Files | Time |
|------|-------|------|
| T1: T1 Read-only | 7 | ~5min |
| T2: T2 Read+CLI | 5 | ~4min |
| T3: T3 Read+Write+CLI | 11 | ~8min |
| T4: T4 Read+Write+CLI+Git | 6 | ~5min |
| T5: T5/T6/T7 | 6 | ~5min |
| T6: Validate + commit | 35 | ~3min |
| **Total** | **35** | **~30min** |

## Dependencies
- T1-T5 are independent (can be done in any order)
- T6 depends on T1-T5 completion

## Constraints
- Frontmatter only — no skill body changes (AC5)
- Placement: after `disable-model-invocation` or `description`, before `license` (AC4)
- Bash patterns use subcommand granularity except T5 and T7 (AC2)
- YAML must be valid on all 35 files (AC6)
