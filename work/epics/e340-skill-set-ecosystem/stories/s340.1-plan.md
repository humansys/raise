# Plan: S340.1 — Skill Set Overlay Deployment

## Tasks

### T1: Unify `_copy_skill_tree` typing (AR R1)
- Change `source_dir: Traversable` → `source_dir: Path | Traversable`
- Verify both types support: `.iterdir()`, `.is_file()`, `.is_dir()`, `.read_text()`, `.name`
- RED: test `_copy_skill_tree` with `Path` source (currently only tested with Traversable)
- GREEN: type annotation change + test passes
- Files: `src/rai_cli/onboarding/skills.py`

### T2: Add `skill_set` param to `scaffold_skills()` + overlay loop
- Add `skill_set: str | None = None` parameter
- After existing builtin loop (L211-385), add overlay block:
  - Read `.raise/skills/{skill_set}/` directory
  - For each skill dir with SKILL.md: copy with overwrite, set origin="project"
- Add `skill_set` field to `SkillManifest`
- RED: test overlay copies skill, same-name overrides builtin, new skill added
- GREEN: implement overlay loop
- Files: `src/rai_cli/onboarding/skills.py`, `src/rai_cli/onboarding/skill_manifest.py`

### T3: Add `--skill-set` to `rai init` CLI
- Add Typer option to init command
- Pass through to `scaffold_skills(skill_set=...)`
- RED: test CLI accepts `--skill-set` flag
- GREEN: wire option through
- Files: `src/rai_cli/cli/commands/init.py`

## Estimated: 3 tasks, ~2h
