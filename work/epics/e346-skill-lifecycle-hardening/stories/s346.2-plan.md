# S346.2: Language-Agnostic Quality Review — Plan

**Story:** S346.2 (RAISE-351) | **Size:** M | **Date:** 2026-03-02

## Tasks

### T1: Add Step 0 — Language Detection (S)
- Add new Step 0 before current Step 1
- Detect language from `.raise/manifest.yaml` (`project.languages` or `project.project_type`)
- Fallback: scan file extensions of changed files
- Files: `.claude/skills/rai-quality-review/SKILL.md`
- Verification: Step 0 documented with detection logic and fallback

### T2: Genericize Step 1 — Changed File Detection (S)
- Replace hardcoded `-- '*.py'` with language-adaptive extension filter
- Reference detected language from Step 0
- Provide extension examples per language
- Files: `.claude/skills/rai-quality-review/SKILL.md`
- Verification: No hardcoded `*.py` in Step 1

### T3: Restructure Step 2 — Semantic Checks by Language (M)
- Replace Python-only checks with per-language sections
- Universal checks (all languages): inverted conditionals, off-by-one, wrong variable, unhandled edge cases
- Python: `type: ignore`, `cast()`, `except Exception`, `raise X from exc`
- TypeScript: `as` casts, `any` types, `@ts-ignore`, unhandled promises
- C#: null `!` operator, unchecked casts, `IDisposable` without `using`
- Error handling: language-appropriate patterns
- Files: `.claude/skills/rai-quality-review/SKILL.md`
- Verification: Each language has equivalent semantic checks

### T4: Adapt Steps 3-4 — Test Heuristics and API Surface (S)
- Step 3 (Test Quality): keep 7 universal heuristics, add framework examples per language (pytest/jest/xunit)
- Step 4 (API Surface): replace `__all__` with language-appropriate visibility idioms
- Files: `.claude/skills/rai-quality-review/SKILL.md`
- Verification: Heuristics remain universal, examples cover 3 languages

### T5: Verify and Commit (XS)
- Review final SKILL.md for consistency
- Verify no remaining Python-only references (except within Python section)
- Commit all changes
- Files: `.claude/skills/rai-quality-review/SKILL.md`
- Verification: `grep -c '\.py' SKILL.md` only appears within Python section

## Execution Order

T1 → T2 → T3 → T4 → T5 (linear, each builds on previous)

## Risks

| Risk | Mitigation |
|------|------------|
| Genericized checks lose Python depth | Keep Python as first/most-detailed language |
| Too many languages make skill unwieldy | Limit to 3 (Python, TS, C#) + "universal" |
