# S654.3 Plan — CLI Adaptation

## Tasks

### T1: `start` — new ID + prefix + pointer

Modify `start()`: add positional `session_name`, replace `get_next_id()` with `generate_session_id(prefix)`, auto-register prefix, write active pointer. Keep existing flow intact.

**Files:** `src/raise_cli/cli/commands/session.py`
**Tests:** `tests/cli/test_session_start.py` (or extend existing)

### T2: `close` — shared index + clear pointer

After `process_session_close()`, build `SessionIndexEntry` and write to shared index. Clear active pointer. Dual-write with personal index (deprecate later).

**Files:** `src/raise_cli/cli/commands/session.py`, `src/raise_cli/session/close.py`
**Tests:** `tests/cli/test_session_close.py` (or extend existing)

### T3: `list` — new command

New `list_sessions()` command. Read shared index, display table.

**Files:** `src/raise_cli/cli/commands/session.py`
**Tests:** `tests/cli/test_session_list.py`

### T4: Integration test

Start → close → list roundtrip verifying new format end-to-end.

**Files:** `tests/session/test_session_integration.py`

## Sequence

```
T1 → T2 → T3 → T4
```
