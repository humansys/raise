# S654.1 Plan — Session ID Model & Prefix Registry

## Tasks

### T1: Path constants (`config/paths.py`)

Add `SHARED_SESSIONS_DIR`, `PREFIXES_FILE`, `ACTIVE_SESSION_FILE` constants and `get_shared_sessions_dir()`, `get_developer_sessions_dir()` helpers. Tests for both helpers.

**Files:** `src/raise_cli/config/paths.py`, `tests/config/test_paths.py`
**TDD:** RED (test helpers return expected paths) → GREEN → REFACTOR

### T2: Session ID generation (`session/identity.py`)

Create new module with `generate_session_id(prefix, now)`. Pure function, 3 lines.

**Files:** `src/raise_cli/session/identity.py`, `tests/session/test_identity.py`
**TDD:** RED (3 tests: format, injected time, prefix variants) → GREEN → REFACTOR

### T3: Prefix registry (`session/prefix.py`)

Create `PrefixEntry`, `PrefixRegistry` Pydantic models. Methods: `register()`, `resolve_collision()`, `load()`, `save()`.

**Files:** `src/raise_cli/session/prefix.py`, `tests/session/test_prefix.py`
**TDD:** RED (4 tests: empty create, register new, collision, roundtrip) → GREEN → REFACTOR

### T4: Resolver update (`session/resolver.py`)

Update `_normalize_session_id()` to handle new `SES-{prefix}-{timestamp}` format alongside old `SES-NNN`. Keep all existing behavior intact.

**Files:** `src/raise_cli/session/resolver.py`, `tests/session/test_resolver.py`
**TDD:** RED (2 new tests: new format normalization, mixed format) → GREEN → REFACTOR

## Sequence

```
T1 (paths) → T2 (identity) → T3 (prefix) → T4 (resolver)
```

T1 first because T3 uses the path helpers. T2 and T3 could be parallel but sequential is simpler for a small story. T4 last because it's the lightest change.
