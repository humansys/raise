# S654.1 Scope — Session ID Model & Prefix Registry

## In Scope

- Timestamp-based session ID generation: `SES-{prefix}-{YYYYMMDD}T{HHMM}`
- Prefix registry module (`session/prefix.py`): load, save, register, collision detection
- Prefix registry file: `.raise/rai/sessions/prefixes.yaml`
- Updated path constants in `config/paths.py` for new directory structure
- Updated `_normalize_session_id()` in `session/resolver.py` to handle new format
- Session ID generation function (replaces counter-based `get_next_id` for sessions)

## Out of Scope

- Shared index read/write (S654.2)
- CLI command changes (S654.3)
- Migration of old sessions (S654.5)
- Changes to `ActiveSession` model in profile.py (deferred to S654.3)

## Done Criteria

- [ ] `generate_session_id(prefix)` returns `SES-{prefix}-{YYYYMMDD}T{HHMM}`
- [ ] `PrefixRegistry` loads/saves `prefixes.yaml`
- [ ] Auto-registration creates registry on first use
- [ ] Collision detection suggests extended prefix
- [ ] `_normalize_session_id()` handles both old (`SES-177`) and new (`SES-E-20260322T1430`) formats
- [ ] Path constants added for `SHARED_SESSIONS_DIR`, `PREFIXES_FILE`
- [ ] All tests pass, type checks clean, lint clean
