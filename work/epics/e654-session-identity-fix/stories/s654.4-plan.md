# S654.4 Plan — Context Bundle & Orientation

## Tasks

### T1: Update `_format_recent_sessions()` to read shared index

Add `developer_prefix` param, read from shared index, format with names and duration. Fallback to personal index.

**Files:** `src/raise_cli/session/bundle.py`, `tests/session/test_bundle.py`
**TDD:** RED (shared index test) → GREEN → REFACTOR

### T2: Wire prefix into `assemble_context_bundle()`

Pass profile prefix through to `_format_recent_sessions()`. Show active session name.

**Files:** `src/raise_cli/session/bundle.py`
**TDD:** RED (bundle integration test) → GREEN → REFACTOR

## Sequence

```
T1 → T2
```
