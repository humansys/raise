# S654.4 Scope — Context Bundle & Orientation

## In Scope

- Update `session/bundle.py` to read from shared index for session history
- Include session names in the context bundle output
- Show active session name when present
- Graceful fallback when shared index doesn't exist

## Out of Scope

- Changes to session start/close (S654.3 — done)
- Migration (S654.5)
- Context bundle format redesign (out of scope per epic)

## Done Criteria

- [ ] `assemble_context_bundle()` reads shared index for session history
- [ ] Recent sessions show names (not just IDs) in bundle
- [ ] Active session name visible in bundle
- [ ] Empty/missing shared index doesn't break bundle assembly
- [ ] Tests pass, types clean
