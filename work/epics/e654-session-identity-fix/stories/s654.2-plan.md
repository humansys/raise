# S654.2 Plan — Shared Session Index

## Tasks

### T1: SessionIndexEntry model

Pydantic frozen model with all fields. JSON serialization helpers for JSONL format.

**Files:** `src/raise_cli/session/index.py`
**TDD:** RED (test model creation, serialization roundtrip) → GREEN → REFACTOR

### T2: Write/read shared index

`write_session_entry()` appends JSONL to `.raise/rai/sessions/{prefix}/index.jsonl`. `read_session_entries()` reads back. Auto-creates directory.

**Files:** `src/raise_cli/session/index.py`, `tests/session/test_index.py`
**TDD:** RED (write creates dir, append doesn't overwrite, read returns all, empty/nonexistent) → GREEN → REFACTOR

### T3: Active session pointer

`write_active_session()`, `read_active_session()`, `clear_active_session()` — plain text file management.

**Files:** `src/raise_cli/session/index.py`, `tests/session/test_index.py`
**TDD:** RED (write, read, read nonexistent → None, clear) → GREEN → REFACTOR

## Sequence

```
T1 (model) → T2 (index read/write) → T3 (active pointer)
```
