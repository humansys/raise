# S348.6 Retrospective — Docs Validation + Cross-links

## Summary

Final validation story for E348. Replaced all stale `rai memory` command
references across 11 documentation files (EN + ES) with current CLI equivalents.
Verified Astro build passes clean (66 pages, 0 errors).

## What was done

1. **Stale command references fixed** (11 files, 112 lines changed):
   - `rai memory query/context/build/validate/list/viz/extract` -> `rai graph *`
   - `rai memory add-pattern/reinforce` -> `rai pattern *`
   - `rai memory emit-work/emit-session/emit-calibration` -> `rai signal *`
   - `rai memory add-calibration/add-session` -> `rai signal *`
   - Fixed broken anchor link (`#rai-memory-build` -> `#rai-graph-build`)

2. **Files updated**:
   - EN: concepts/memory.mdx, concepts/knowledge-graph.mdx, concepts/skills.mdx,
     getting-started.mdx, guides/setting-up.mdx
   - ES: Same 5 files + es/docs/cli/index.mdx (the full ES CLI reference)

3. **Astro build verified**: 66 pages, 0 errors, 5.51s build time.

4. **Remaining `rai memory` reference**: Only `rai memory generate` deprecation
   notice in ES CLI index — intentionally preserved as it documents a deprecated command.

## What went well

- sed-based bulk replacement was efficient for this volume of changes
- Astro build caught no issues — changes were syntactically clean

## What could improve

- The ES CLI index (es/docs/cli/index.mdx) was not updated in S348.4 when the
  EN version was restructured. Future doc stories should always update both
  language versions in the same story.

## Metrics

| Metric | Value |
|--------|-------|
| Files changed | 11 |
| Lines changed | 112 (symmetric: 112 additions, 112 deletions) |
| Build time | 5.51s |
| Build errors | 0 |
