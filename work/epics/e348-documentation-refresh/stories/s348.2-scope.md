# S348.2: CLI Reference Completion -- Scope

## In Scope

- Document all 72 current CLI subcommands across 20 command groups
- Remove deprecated `rai memory` namespace documentation (12 subcommands)
- Remove deprecated `rai publish` namespace documentation (2 subcommands)
- Include for each subcommand: synopsis, description, flags/options table, usage example, exit codes
- Update partially documented commands (init, session, skill, release, discover) with current flags
- Organize by command group with consistent formatting
- Verify all documented flags against `rai <command> --help` output

## Out of Scope

- Spanish translation updates (S348.6)
- Cross-link fixes in other doc pages that reference deprecated commands (S348.6)
- Developer/extension guides (S348.4)
- Getting-started or concept page updates (S348.6)
- API reference or docstring documentation

## Done Criteria

- [ ] All 72 current subcommands documented in `docs/src/content/docs/docs/cli/index.mdx`
- [ ] All deprecated namespace docs (`rai memory`, `rai publish`) removed
- [ ] Each command has: description, flags/options, at least one example
- [ ] All 11 new command groups present: adapter, artifact, backlog, docs, doctor, gate, graph, mcp, pattern, signal, skill (complete)
- [ ] All 4 partially documented groups updated: init, session, release, discover
- [ ] Documented flags verified against `--help` output for each command
