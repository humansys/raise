# S348.6 Scope — Docs Validation + Cross-links

## In Scope

- Replace all `rai memory` command references with current equivalents across EN and ES docs
- Command mapping:
  - `rai memory query` -> `rai graph query`
  - `rai memory context` -> `rai graph context`
  - `rai memory build` -> `rai graph build`
  - `rai memory validate` -> `rai graph validate`
  - `rai memory list` -> `rai graph list`
  - `rai memory viz` -> `rai graph viz`
  - `rai memory add-pattern` -> `rai pattern add`
  - `rai memory reinforce` -> `rai pattern reinforce`
  - `rai memory emit-work` -> `rai signal emit-work`
  - `rai memory emit-session` -> `rai signal emit-session`
  - `rai memory emit-calibration` -> `rai signal emit-calibration`
  - `rai memory add-calibration` -> `rai session add-calibration` (session subcommand)
  - `rai memory add-session` -> `rai session add` (session subcommand)
  - `rai memory extract` -> `rai discover scan` (discovery subcommand)
  - `rai memory generate` -> deprecated, reference `rai graph build`
- Verify Astro build succeeds
- Cross-link verification

## Out of Scope

- Content rewriting beyond command reference fixes
- New documentation pages
- CLI reference pages (already updated in S348.4)
