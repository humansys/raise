---
story_id: "S350.2"
epic_id: "E350"
tracker: "RAISE-455"
status: "in_progress"
---

# S350.2: Sanitize Git State — Scope

## In Scope

1. **Add .gitignore rules:**
   - `.claude/worktrees/` — per-developer ephemeral worktree directories (M10)
   - `.claude/projects/` — machine-specific project memory paths

2. **Remove legacy files from git tracking:**
   - `.claude/RAI.md` — superseded by CLAUDE.md + skills system (M7)
   - `.claude/RAI-naming.md` — superseded, no active use (M8)
   - `.claude/rai.archive/` — 4 historical files, all superseded (M9):
     - `calibration.md`, `identity.md`, `memory.md`, `session-index.md`

3. **Ensure `rai init` creates `.raise/rai/personal/`:**
   - Directory with `.gitkeep` so structure exists after fresh clone + init

## Out of Scope

- Telemetry migration (`.raise/rai/telemetry/` to `personal/telemetry/`) — tracked as M6, separate concern
- CLAUDE.md regeneration — S350.3
- Pattern destillation — S350.4
- Jira credentials separation — S350.7
- `.raise/manifests/skills.json` derived status — S350.5

## Done Criteria

- [ ] `.gitignore` updated with `.claude/worktrees/` and `.claude/projects/` rules
- [ ] Legacy files removed from git (`git rm`): RAI.md, RAI-naming.md, rai.archive/
- [ ] `.gitignore` rules added for removed legacy paths (prevent re-addition)
- [ ] `rai init` creates `.raise/rai/personal/` with `.gitkeep`
- [ ] `git status` clean after changes (no untracked personal/ephemeral artifacts)
- [ ] Existing tests pass

## Tasks

1. Update `.gitignore` with new rules
2. `git rm` legacy files from tracking
3. Verify/add `rai init` personal directory creation
4. Run tests, commit

## Risks

| Risk | L | I | Mitigation |
|------|---|---|------------|
| Removing legacy files breaks something referencing them | L | L | Grep for references before removal |
| `.claude/projects/` gitignore too broad | L | M | Pattern targets only machine-specific subdirs |
