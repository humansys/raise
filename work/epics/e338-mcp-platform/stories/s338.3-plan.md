# Plan: S338.3 — `rai mcp call`

## Tasks

### T1: Create `rai mcp call` command (S)

**TDD Cycle:**
- RED: Test server-not-found error, test invalid JSON args error, test successful call (mock bridge), test bridge error handling
- GREEN: Create `src/rai_cli/cli/commands/mcp.py` with `mcp_app` and `call` command
- REFACTOR: Clean up

**Implementation:**
1. Create `tests/cli/commands/test_mcp_call.py` — 4 tests:
   - `test_call_server_not_found` — exit 1 + error message
   - `test_call_invalid_args_json` — exit 1 + error message
   - `test_call_success` — exit 0 + JSON stdout with McpToolResult fields
   - `test_call_bridge_error` — exit 1 + error message
2. Create `src/rai_cli/cli/commands/mcp.py` — Typer app with `call` command
3. All tests green

### T2: Register command + verify (XS)

1. Add `mcp_app` import and `app.add_typer()` to `main.py`
2. Run `rai mcp call --help` to verify registration
3. Run full test suite

**Commit after T2.**

## Sequencing

T1 → T2 (sequential, T2 depends on T1)

## Estimated effort: 45 min (S)
