# Scope: S338.7 — `rai mcp scaffold`

## In Scope

- `rai mcp scaffold <name> --command CMD [--args ARGS] [--env ENV] [--force]`
- Connect to server via McpBridge, list_tools() to discover tools
- Generate `.raise/mcp/<name>.yaml` with McpServerConfig format
- Include discovered tool names as YAML comments for reference
- Create `.raise/mcp/` directory if missing
- Refuse overwrite without --force

## Out of Scope

- Generating declarative adapter YAML (`.raise/adapters/`) — that requires protocol mapping
- Auto-detecting command/args from package name (that's S338.6 install)
- Interactive tool selection or filtering

## Done Criteria

- [ ] `rai mcp scaffold` generates valid `.raise/mcp/<name>.yaml`
- [ ] Generated YAML parseable by `McpServerConfig`
- [ ] Tool names listed as comments in output
- [ ] Directory creation, overwrite protection work
- [ ] pyright + ruff + pytest green
