# Scope: S338.3 — `rai mcp call`

## In Scope

- New Typer command: `rai mcp call <server> <tool> [--args JSON]`
- Registry lookup: resolve server name → McpServerConfig via discover_mcp_servers()
- Bridge invocation: connect → call tool → print JSON result → close
- Error handling: server not found, bridge error, invalid args JSON
- JSON output to stdout (machine-parseable for skill consumption)

## Out of Scope

- `rai mcp list/health/tools` (S338.2)
- Telemetry/hooks on call (S338.4)
- `--verbose` flag (S338.4)
- Streaming output
- Non-JSON output formats

## Done Criteria

- `rai mcp call <server> <tool> --args '{...}'` invokes tool and prints JSON
- Server-not-found gives clear error with non-zero exit
- Bridge errors surfaced to user with non-zero exit
- Unit tests cover: happy path, missing server, bridge error, no-args call
- All existing tests still pass
