# Plan: S338.4 — Telemetry hooks

## Tasks

### T1: McpCallEvent dataclass (XS)

**TDD Cycle:**
- RED: test event is frozen dataclass, event_name="mcp:call", all fields present with defaults
- GREEN: Add `McpCallEvent` to `hooks/events.py`

**Files:** `src/rai_cli/hooks/events.py` (modify), `tests/hooks/test_mcp_call_event.py` (create)
**Commit after green.**

### T2: Emit event + --verbose from `rai mcp call` (S)

**TDD Cycle:**
- RED: test event emitted on success, test event emitted on failure, test --verbose shows details, test --verbose on error
- GREEN: Modify `cli/commands/mcp.py` — add `--verbose` flag, measure latency, emit `McpCallEvent`, print verbose line to stderr

**Files:** `src/rai_cli/cli/commands/mcp.py` (modify), `tests/cli/commands/test_mcp_call.py` (modify)
**Commit after green.**

### T3: Verify (XS)

- Full test suite + pyright + ruff
- Commit story artifacts

## Sequencing

T1 → T2 → T3 (sequential)

## Estimated effort: 30 min (S)
