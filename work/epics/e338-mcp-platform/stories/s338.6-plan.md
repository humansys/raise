# Plan: S338.6 — `rai mcp install`

## Overview

| Field | Value |
|-------|-------|
| Story | S338.6 |
| Size | M |
| Tasks | 3 |
| Date | 2026-03-02 |

## Tasks

### T1: Extract `_write_mcp_config()` from scaffold (XS)

**Description:** Extract the shared YAML generation pipeline (overwrite check → mkdir → serialize → write) from `scaffold` into a reusable `_write_mcp_config()` helper. Refactor `scaffold` to call it.

**Files:**
- Modify: `src/rai_cli/cli/commands/mcp.py`

**TDD:**
- Existing scaffold tests must still pass (refactor, no behavior change)

**Verification:** `pytest tests/cli/commands/test_mcp_scaffold.py -x`

### T2: Install command (S)

**Description:** Add `install` command with --type uvx|npx|pip, --name, --module, --env, --force. Type strategy generates command/args. pip runs `sys.executable -m pip install`. Health check after install (non-fatal). Calls `_write_mcp_config()`.

**Files:**
- Modify: `src/rai_cli/cli/commands/mcp.py`
- Create: `tests/cli/commands/test_mcp_install.py`

**TDD:**
- RED: Tests for npx, uvx, pip types, missing --module for pip, health failure warning, overwrite protection
- GREEN: Implement install command
- REFACTOR: Clean up

**Verification:** `pytest tests/cli/commands/test_mcp_install.py -x && pyright src/rai_cli/cli/commands/mcp.py && ruff check src/rai_cli/cli/commands/mcp.py`

### T3: Full verification (XS)

**Description:** Full test suite, pyright, ruff.

**Verification:** `pytest --no-header -q`

## Execution Order

T1 → T2 → T3 (sequential)

## Duration Tracking

| Task | Estimated | Actual | Notes |
|------|-----------|--------|-------|
| T1 | 5 min | | |
| T2 | 20 min | | |
| T3 | 5 min | | |
| Total | 30 min | | |
