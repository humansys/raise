# Plan: S338.8 — MCP Skills (add/remove/status)

## Overview

| Field | Value |
|-------|-------|
| Story | S338.8 |
| Size | M |
| Tasks | 4 |
| Date | 2026-03-02 |

## Tasks

### T1: Create `/rai-mcp-add` skill (M)

**Description:** Write SKILL.md for guided MCP server registration. Conversational flow that collects server intent, package type, name, env vars, then delegates to `rai mcp install`.

**Files:**
- Create: `src/rai_cli/skills_base/rai-mcp-add/SKILL.md`
- Create: `.claude/skills/rai-mcp-add/SKILL.md` (copy)

**Verification:**
- `uv run rai skill validate rai-mcp-add` passes
- `diff` between builtin and deployment is clean
- Skill appears in `uv run rai skill list`

**TDD:** N/A (SKILL.md — no Python code). Validate with `rai skill validate`.

### T2: Create `/rai-mcp-remove` skill (S)

**Description:** Write SKILL.md for safe MCP server removal. Checks adapter dependencies via grep on `.raise/adapters/*.yaml` for `ref:` references, warns if found, deletes config file.

**Files:**
- Create: `src/rai_cli/skills_base/rai-mcp-remove/SKILL.md`
- Create: `.claude/skills/rai-mcp-remove/SKILL.md` (copy)

**Verification:**
- `uv run rai skill validate rai-mcp-remove` passes
- `diff` between builtin and deployment is clean

### T3: Create `/rai-mcp-status` skill (XS)

**Description:** Write SKILL.md for MCP health overview. Runs `rai mcp list` + `rai mcp health` per server, presents summary table.

**Files:**
- Create: `src/rai_cli/skills_base/rai-mcp-status/SKILL.md`
- Create: `.claude/skills/rai-mcp-status/SKILL.md` (copy)

**Verification:**
- `uv run rai skill validate rai-mcp-status` passes
- `diff` between builtin and deployment is clean

### T4: Integration verification (XS)

**Description:** Verify all 3 skills are discoverable, validate structure, and test `/rai-mcp-status` against real Context7 server.

**Verification:**
- `uv run rai skill list` shows all 3 new skills
- `/rai-mcp-status` produces output against live Context7

## Execution Order

```
T1 → T2 → T3 → T4
```

Sequential (each builds on skill pattern established in T1). T4 depends on all three.

## Duration Tracking

| Task | Est | Actual | Notes |
|------|-----|--------|-------|
| T1 | 10 min | — | |
| T2 | 7 min | — | |
| T3 | 5 min | — | |
| T4 | 5 min | — | |
| **Total** | **27 min** | — | |
