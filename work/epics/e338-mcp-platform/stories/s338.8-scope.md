# Scope: S338.8 — MCP Skills (add/remove/status)

## In Scope

- `/rai-mcp-add` skill — guided MCP server registration (delegates to `rai mcp install` / `rai mcp scaffold`)
- `/rai-mcp-remove` skill — safe removal with dependency checking
- `/rai-mcp-status` skill — health overview of all registered servers
- Skills deployed to both `src/rai_cli/skills_base/` and `.claude/skills/`

## Out of Scope

- CLI changes (all CLI work done in S338.2-S338.7)
- Stack-aware recommendations (S338.9)
- Session start integration (S338.10)
- MCP catalog / known-servers database

## Done When

- `/rai-mcp-add` guides human from "I want X" to working config — zero CLI knowledge required
- `/rai-mcp-remove` checks adapter dependencies before deleting
- `/rai-mcp-status` shows health of all registered servers
- All 3 skills exist in both builtin and deployment locations
- Skills are listed in available skills (rai skill list finds them)
