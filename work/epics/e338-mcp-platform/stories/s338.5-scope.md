# Scope: S338.5 — Declarative adapter `server.ref`

## In Scope

- `ServerRef` model in `rai_cli.adapters.declarative.schema` with `ref: str | None` + optional `command`
- Pydantic validator: at least one of `ref` or `command` must be present
- `_create_bridge()` resolves `ref` via `discover_mcp_servers()` registry
- Backwards compatibility: existing inline `server.command` configs still parse and work
- Tests for schema validation, ref resolution, and backwards compat

## Out of Scope

- Modifying `ServerConnection` in `rai_cli.mcp.schema` (stays clean for registry)
- Inline override of ref fields (e.g. ref + custom env) — deferred
- Lazy registry loading or caching

## Done Criteria

- [ ] `ServerRef` model validates: ref-only, inline-only, rejects neither
- [ ] `_create_bridge()` resolves ref from registry
- [ ] All existing declarative adapter tests pass (backwards compat)
- [ ] New tests cover all 4 AC scenarios
- [ ] pyright + ruff + pytest green
