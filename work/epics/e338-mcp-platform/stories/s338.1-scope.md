# S338.1 Scope: MCP Infrastructure + Registry

## In Scope

- Create `src/rai_cli/mcp/` package with `__init__.py`, `bridge.py`, `models.py`, `schema.py`, `registry.py`
- Move McpBridge from `adapters/mcp_bridge.py` to `mcp/bridge.py` (AR-Q1)
- Create re-export shim at old path for backwards compat
- Create `McpHealthResult` in `mcp/models.py`, remove `AdapterHealth` import from bridge (AR-C1)
- Create `ServerConnection` shared model in `mcp/schema.py` (AR-C2)
- Update `declarative/schema.py` to use `ServerConnection`
- Create `McpServerConfig` Pydantic model for `.raise/mcp/*.yaml`
- Create `discover_mcp_servers()` in `mcp/registry.py`
- Update existing adapter consumers to import from new path (Jira, Confluence, declarative)

## Out of Scope

- CLI commands (S338.2, S338.3)
- Telemetry events (S338.4)
- `server.ref` in declarative adapters (S338.5)
- Any changes to adapter protocols or models

## Done When

- [ ] `rai_cli.mcp.bridge.McpBridge` is the canonical location
- [ ] `rai_cli.adapters.mcp_bridge` re-exports everything (shim)
- [ ] Bridge does NOT import from `rai_cli.adapters.models`
- [ ] `ServerConnection` shared between mcp schema and declarative schema
- [ ] `discover_mcp_servers()` works with `.raise/mcp/*.yaml`
- [ ] All existing tests pass (2770+)
- [ ] New tests for schema, registry, health model
- [ ] pyright + ruff clean
