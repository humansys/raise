# Retrospective: S338.6 — `rai mcp install`

## What Went Well

- **Refactor-first (T1)** — extracting `_write_mcp_config()` before implementing install paid off. Zero duplication between scaffold and install.
- **Early overwrite check pattern** — discovered during T1 that overwrite must happen before connecting to server (not inside the helper). Both scaffold and install now check early.
- **AR caught sys.executable question** — using `sys.executable -m pip` ensures correct Python env.

## What Could Improve

- Initially forgot to add `--force` test for install (QR caught it). When extracting shared behavior, test coverage for both consumers needs explicit verification.

## Patterns

- PAT-E-610: When extracting shared helpers, verify test coverage exists for each consumer independently — shared code doesn't mean shared tests are sufficient.

## Metrics

| Metric | Value |
|--------|-------|
| Estimated | 30 min (M) |
| Actual | ~25 min |
| Velocity | ~1.2x |
| Files created | 1 (test_mcp_install.py) |
| Files modified | 1 (mcp.py) |
| Tests added | 8 |
| Total tests | 3275 passing |

## AR Decisions

- Q1-B: `sys.executable -m pip` for correct Python env
- Q2: Full write pipeline in `_write_mcp_config()`

## QR Findings Applied

- R1: Added --force overwrite test for install
