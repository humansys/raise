# Retrospective: S338.4 — Telemetry hooks

## What Went Well

- **Pattern replication** — McpCallEvent follows the exact same frozen dataclass pattern as 10+ existing events. Zero novelty, zero risk.
- **AR caught dual telemetry question** — logfire spans (bridge) vs McpCallEvent (CLI) is intentional dual-write for distinct consumers (APM vs RaiSE hooks). Documented decision.
- **QR caught -v collision** — short form removed to avoid global verbosity flag conflict.

## What Could Improve

- Should have caught -v collision during design, not QR. CLI flag conventions deserve a checklist item.

## Patterns

- PAT-E-608: When adding CLI flags, check for collisions with global flags defined in main.py callback. Global flags: --format/-f, --verbose/-v, --quiet/-q, --version/-V.

## Metrics

| Metric | Value |
|--------|-------|
| Estimated | 30 min (S) |
| Actual | ~20 min |
| Velocity | ~1.5x |
| Files created | 1 (test_mcp_call_event.py) |
| Files modified | 3 (events.py, mcp.py, test_mcp_call.py) |
| Tests added | 10 (6 event + 4 call emission/verbose) |
| Total tests | 3251 passing |

## AR Decisions

- Q1-A: Keep dual telemetry (logfire + hooks) — distinct consumers
- Q2-A: Remove -v short form — global flag convention

## QR Findings Applied

- R2: -v short form removed
