# Plan: S338.9 — Stack-aware MCP recommendations

## Overview

| Field | Value |
|-------|-------|
| Story | S338.9 |
| Size | S |
| Tasks | 3 |
| Date | 2026-03-02 |

## Tasks

### T1: Create `.raise/mcp/catalog.yaml` (S)

**Description:** Create governance file with known MCP servers, package details, and `recommended_for` mapping per language.

**Files:**
- Create: `.raise/mcp/catalog.yaml`

**Verification:**
- Valid YAML, parseable
- Contains at least 4 servers (context7, github, filesystem, fetch)
- Each entry has: package, type, description, recommended_for

### T2: Update `/rai-mcp-status` to show recommendations (S)

**Description:** Add recommendations section to status skill. Read catalog, detect stack (artifact-first, glob fallback), show missing capabilities excluding already-registered servers.

**Files:**
- Modify: `src/rai_cli/skills_base/rai-mcp-status/SKILL.md`
- Copy: `.claude/skills/rai-mcp-status/SKILL.md`

**Verification:**
- `rai skill validate` passes
- Diff clean between builtin and deployment

### T3: Update `/rai-mcp-add` to read catalog (S)

**Description:** Replace hardcoded well-known servers table with instruction to read `catalog.yaml`. When human says a server name that matches a catalog entry, use its package/type/env/module.

**Files:**
- Modify: `src/rai_cli/skills_base/rai-mcp-add/SKILL.md`
- Copy: `.claude/skills/rai-mcp-add/SKILL.md`

**Verification:**
- `rai skill validate` passes
- Diff clean between builtin and deployment
- Hardcoded table removed, catalog reference added

## Execution Order

```
T1 → T2 → T3
```

T1 creates the data. T2 and T3 consume it (could be parallel, but sequential is cleaner for one dev).

## Duration Tracking

| Task | Est | Actual | Notes |
|------|-----|--------|-------|
| T1 | 5 min | — | |
| T2 | 7 min | — | |
| T3 | 5 min | — | |
| **Total** | **17 min** | — | |
