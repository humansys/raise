# Plan: S338.5 — Declarative adapter `server.ref`

## Overview

| Field | Value |
|-------|-------|
| Story | S338.5 |
| Size | S |
| Tasks | 2 |
| Date | 2026-03-02 |

## Tasks

### T1: ServerRef model + schema change (S)

**Description:** Create `ServerRef` in declarative schema with `ref: str | None`, `command: str | None`, `args`, `env`, and model validator. Change `DeclarativeAdapterConfig.server` type to `ServerRef`.

**Files:**
- Modify: `src/rai_cli/adapters/declarative/schema.py`
- Modify: `tests/adapters/declarative/test_schema.py`

**TDD:**
- RED: Test ref-only parses, inline-only parses, neither raises ValidationError, existing valid.yaml still parses
- GREEN: Add `ServerRef` model, change `DeclarativeAdapterConfig.server` type
- REFACTOR: Clean up imports

**Verification:** `pytest tests/adapters/declarative/test_schema.py -x && pyright src/rai_cli/adapters/declarative/schema.py && ruff check src/rai_cli/adapters/declarative/schema.py`

### T2: Ref resolution in adapter + integration (S)

**Description:** Modify `_create_bridge()` to resolve `server.ref` via `discover_mcp_servers()`. Add tests for ref resolution (success + missing ref error).

**Files:**
- Modify: `src/rai_cli/adapters/declarative/adapter.py`
- Create: `tests/adapters/declarative/test_server_ref_resolution.py`

**TDD:**
- RED: Test ref resolves to registry server's connection, test missing ref raises McpBridgeError
- GREEN: Add ref resolution logic to `_create_bridge()`
- REFACTOR: Clean up

**Verification:** `pytest tests/adapters/declarative/ -x && pyright src/rai_cli/adapters/declarative/adapter.py && ruff check src/rai_cli/adapters/declarative/`

## Execution Order

T1 → T2 (sequential — T2 depends on ServerRef model from T1)

## Duration Tracking

| Task | Estimated | Actual | Notes |
|------|-----------|--------|-------|
| T1 | 10 min | | |
| T2 | 15 min | | |
| Total | 25 min | | |
