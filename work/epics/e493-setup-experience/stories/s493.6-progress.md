# S493.6 Progress — Pipx Install Fix

| Task | Estimated | Actual | Status |
|------|-----------|--------|--------|
| T1 — Investigate pipx install | 15 min | 8 min | done |
| T2 — Document findings | 10 min | 5 min | done |

## Investigation Findings

### T1 Results

1. **`pipx` not installed** on this machine — cannot reproduce directly
2. **Package metadata analysis** shows entry point is correctly declared:
   - Main repo: `raise-cli` package → `rai = raise_cli.cli.main:app`
   - Worktree: `rai-cli` package → `rai = rai_cli.cli.main:app`
3. Both packages use `[project.scripts]` which is the standard mechanism pipx uses to expose commands
4. The original `--include-deps` issue was likely from an older package layout that didn't declare `[project.scripts]`

### T2 Resolution

**Verdict: Already fixed.** The `[project.scripts]` entry point is correctly declared in both package versions. The original friction point (#2 from the new-machine setup) is resolved by the current package metadata.

No code changes needed. Story closes as **verified working**.
