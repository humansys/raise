# Scope: S493.6 — Fix pipx install

## In Scope

- Investigate pipx install behavior with current package metadata
- Fix package metadata if `rai` command not exposed by pipx
- Document findings

## Out of Scope

- Supporting pip install (only pipx)
- Homebrew or other package managers
- Auto-update mechanisms

## Done Criteria

- `pipx install .` exposes `rai` command without `--include-deps`
- `rai --version` works after clean pipx install
- OR: documented as already working and closed
