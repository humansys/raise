# S493.4 Plan — Add Developer Checks to `rai doctor`

**Story:** S493.4 | **Size:** S (redefined from M) | **Date:** 2026-03-06

## Tasks

### T1: DeveloperCheck class with 4 checks (S)

**RED:** Write tests for all 4 checks — profile exists/missing, credentials set/missing, claude in PATH/missing, MCP configured/missing. Mock `Path.exists()`, `shutil.which()`, `os.environ`.

**GREEN:** Create `src/raise_cli/doctor/checks/developer.py` implementing `DoctorCheck` protocol with:
- `_check_profile()` — check `~/.rai/developer.yaml`, show name+prefix when found
- `_check_credentials()` — check `JIRA_API_TOKEN` / `JIRA_URL` in env, also check `.env` in working_dir
- `_check_claude_code()` — `shutil.which("claude")`
- `_check_mcp_servers()` — parse `~/.claude.json` for `mcpServers`

**REFACTOR:** Ensure all fix hints are actionable.

**Files:** `src/raise_cli/doctor/checks/developer.py`, `tests/doctor/test_developer_check.py`
**Verification:** `uv run pytest tests/doctor/test_developer_check.py -v`
**Depends on:** —

### T2: Register entry point + integration test (XS)

**RED:** Write test that `rai doctor -v --json` output includes `"developer"` category.

**GREEN:** Add entry point to `pyproject.toml`. Reinstall package.

**REFACTOR:** —

**Files:** `pyproject.toml`, `tests/doctor/test_developer_check.py`
**Verification:** `uv run pytest tests/doctor/ -v && rai doctor -v`
**Depends on:** T1

### T3: Manual integration test (XS)

Run `rai doctor -v` and verify developer checks appear. Test with/without profile.

**Verification:** Visual confirmation of output format.
**Depends on:** T2

## Execution Order

T1 → T2 → T3 (sequential, each depends on previous)

## Risks

| Risk | Mitigation |
|------|------------|
| Entry point not discovered after install | Reinstall with `uv pip install -e ".[dev]"` |
| `~/.claude.json` format varies | Parse defensively, return WARN on any parse error |
