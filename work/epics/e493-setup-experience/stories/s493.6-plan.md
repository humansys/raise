# S493.6 Plan — Fix pipx install

**Story:** S493.6 | **Size:** S | **Date:** 2026-03-06

## Tasks

### T1: Investigate pipx install behavior (S)

**Investigation steps:**
- Uninstall any existing `rai-cli` / `raise-commons` pipx package
- Run `pipx install .` from the raise-commons root
- Check `pipx list` output for exposed commands
- Run `rai --version` to verify the command is on PATH
- If it fails, inspect pipx venv to understand why (missing entrypoint, wrong package name, etc.)

**Files to inspect:**
- `pyproject.toml` — verify `[project.scripts]` section
- pipx venv structure (if debugging needed)

**AC ref:** AC1 (pipx install exposes rai), AC2 (rai --version works)

**Verification:**
```bash
pipx install . && rai --version
pipx list | grep rai
```

**Dependencies:** None

### T2: Fix or document (XS)

**If broken:**
- Fix package metadata in `pyproject.toml` (entrypoint, package name, etc.)
- Re-run `pipx install .` and verify `rai --version`
- Add any findings to story scope

**If working:**
- Document as verified in story scope
- No code changes needed

**Verification:**
```bash
# Clean re-install to confirm
pipx uninstall raise-commons || pipx uninstall rai-cli || true
pipx install .
rai --version
```

**Dependencies:** T1

## Execution Order

1. T1 — investigate current state (risk: may already work, making T2 trivial)
2. T2 — fix or document based on T1 findings

## Risks

| Risk | Mitigation |
|------|------------|
| pipx caches old installs | Force uninstall before reinstall |
| Package name mismatch between pyproject.toml and pipx | Inspect `pipx list` output carefully |
| Entrypoint works but import fails at runtime | Test `rai --version` not just `which rai` |

## Duration Tracking

| Task | Estimated | Actual | Status |
|------|-----------|--------|--------|
| T1 | 15 min | — | pending |
| T2 | 10 min | — | pending |
