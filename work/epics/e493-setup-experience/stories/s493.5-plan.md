# S493.5 Plan — CLI .env Loading

**Story:** S493.5 | **Size:** S | **Date:** 2026-03-06

## Tasks

### T1: Add python-dotenv dependency + load at entrypoint (S)

**TDD cycle:**
- RED: Test that `load_dotenv` is called during CLI startup
- GREEN: Add `python-dotenv` to `pyproject.toml`, call `load_dotenv()` in `main()` callback
- REFACTOR: —

**Files:**
- Modify: `pyproject.toml` (add dependency)
- Modify: `src/rai_cli/cli/main.py` (import + call `load_dotenv` in `main()`)

**AC ref:** AC1 (CLI loads .env), AC2 (works without .env), AC3 (precedence)

**Verification:**
```bash
pytest tests/ -k "dotenv" -x
ruff check src/rai_cli/cli/main.py
pyright src/rai_cli/cli/main.py
```

**Dependencies:** None

### T2: Integration test — verify .env loading E2E (XS)

**Manual verification:**
- Create a temp `.env` with `TEST_RAI_DOTENV=hello`
- Run `rai --version` (should not crash)
- Verify env var is loaded in Python process

**Verification:** Manual + existing test suite passes

**Dependencies:** T1

## Execution Order

1. T1 — dependency + entrypoint (risk: none, straightforward)
2. T2 — integration test

## Risks

| Risk | Mitigation |
|------|------------|
| `load_dotenv` slows CLI startup | Negligible — reads one small file |
| CWD is not project root | Acceptable — standard dotenv behavior, document in scope |

## Duration Tracking

| Task | Estimated | Actual | Status |
|------|-----------|--------|--------|
| T1 | 15 min | — | pending |
| T2 | 5 min | — | pending |
