# Plan: S325.2 — Delegation Profile Model

**Epic:** E325 Agent-Orchestrated Workflow
**Jira:** RAISE-331
**Size:** S
**Date:** 2026-03-01

---

## Tasks

### T1: DelegationLevel enum + DelegationConfig model + resolve_delegation()

**Size:** S
**Dependencies:** None

**RED:** Tests for:
- Enum values (review/notify/auto)
- DelegationConfig model validation (valid + invalid)
- resolve_delegation() precedence: override > default_level > ShuHaRi derivation
- Backward compat: delegation=None uses ShuHaRi defaults

**GREEN:** Add to `src/rai_cli/onboarding/profile.py`:
- `DelegationLevel(StrEnum)`
- `DelegationConfig(BaseModel)` with default_level + overrides
- `delegation` field on `DeveloperProfile`
- `resolve_delegation()` function

**REFACTOR:** Minimal expected.

**Files:**
- Modify: `src/rai_cli/onboarding/profile.py`
- Create: `tests/onboarding/test_delegation.py`

**Verification:** `uv run pytest tests/onboarding/test_delegation.py && uv run pyright src/rai_cli/onboarding/profile.py && uv run ruff check src/rai_cli/onboarding/profile.py`

### T2: YAML round-trip integration test

**Size:** XS
**Dependencies:** T1

**RED:** Tests that:
- Profile with delegation section saves to YAML and loads back correctly
- Profile without delegation section loads fine (real backward compat)

**GREEN:** Should pass with T1 code (Pydantic + yaml handles it). Validates real-world persistence.

**Files:**
- Modify: `tests/onboarding/test_delegation.py`

**Verification:** `uv run pytest tests/onboarding/test_delegation.py`

## Execution Order

T1 → T2 (sequential, T2 depends on T1 models)

## Duration Tracking

| Task | Estimated | Actual | Notes |
|------|-----------|--------|-------|
| T1 | 15 min | — | |
| T2 | 5 min | — | |
