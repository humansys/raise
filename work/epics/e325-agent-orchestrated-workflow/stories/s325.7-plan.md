# S325.7 — Epic Orchestrator: Implementation Plan

**Date:** 2026-03-01
**Size:** L (5-8 tasks)
**Design:** `s325.7-design.md` (D1-D2-D3-D4, arch review Q1+Q2 resolved)

---

## Tasks

### T1: Create `/rai-epic-run` SKILL.md (L)

**Description:** Write the unified epic orchestrator skill following ADR-040 contract template (7 sections, ≤150 body lines). Includes:
- I/O contract in frontmatter (epic_id input, merge_commit output)
- Epic phase detection table (D1: 5 states, `### Progress Tracking` marker for plan detection)
- 5-phase chain: start → design → plan → stories → close
- Story iteration logic (D2: read Progress Tracking, filter Status != Done, execute via /rai-story-run)
- Delegation gates at post-design and post-plan (D3)
- Stop-on-failure behavior (D4)

**Files:**
- Create: `src/rai_cli/skills_base/rai-epic-run/SKILL.md`

**Reference:** `/rai-story-run` (S325.6) as structural template. `/rai-discover` (S325.5) for line budget discipline.

**Verification:**
- `rai skill validate` passes
- Body lines ≤150
- All 7 ADR-040 sections present

**AC:** MUST-1 (chains 4 epic skills + iteration), MUST-2 (phase detection), MUST-3 (story iteration), MUST-4 (delegation gates), MUST-5 (validates)

**Dependencies:** None

---

### T2: Sync to `.claude/skills` deployment (XS)

**Description:** Copy SKILL.md to `.claude/skills/rai-epic-run/SKILL.md`. Verify with diff.

**Files:**
- Create: `.claude/skills/rai-epic-run/SKILL.md`

**Verification:**
- `diff` shows no differences between builtin and deployment copies
- `rai skill validate` passes for all skills (no regressions)

**AC:** MUST-5 (deployed to both locations)

**Dependencies:** T1

---

### T3: Integration walkthrough (S)

**Description:** Structured walkthrough of `/rai-epic-run` through all 5 phase states, verifying:
- Phase detection covers all states without gaps (including spike rows in table)
- `### Progress Tracking` marker detection is unambiguous
- Story iteration correctly filters Done, executes in order
- Delegation gates fire at post-design and post-plan only
- `/rai-story-run` delegation is clean (no behavior override)
- Failure at any phase produces clear stop + report
- No state file created (Q5 compliance)
- No contradictions with individual epic/story skill SKILL.md files

**Verification:**
- All 5 phase states verified
- All skills validate (`rai skill validate`)
- Full test suite passes (no regressions)

**AC:** All MUST criteria verified via walkthrough

**Dependencies:** T2

---

## Execution Order

```
T1 (create SKILL.md) → T2 (sync deployment) → T3 (integration walkthrough)
```

Sequential. Risk-first: T1 is highest risk (line budget tight with story iteration logic).

## Risks

| Risk | L/I | Mitigation |
|------|:---:|------------|
| Body exceeds 150 lines | H/M | Story iteration adds ~15 lines vs S325.6. Use tables over prose, compress examples. Target 140. |
| Progress Tracking table format varies | L/M | Format is consistent across all epics (added by /rai-epic-plan). Specify exact column names. |
| Phase detection ambiguity at check 2 vs 3 | L/M | Explicit `### Progress Tracking` marker resolves (arch review Q2). |

## Duration Tracking

| Task | Size | Estimated | Actual | Notes |
|------|:----:|:---------:|:------:|-------|
| T1 | L | — | — | |
| T2 | XS | — | — | |
| T3 | S | — | — | |
| **Total** | **L** | — | — | |
