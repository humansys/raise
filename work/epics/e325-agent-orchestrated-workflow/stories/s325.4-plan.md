# Plan: S325.4 — Backlog-Aware Skills

**Epic:** E325 Agent-Orchestrated Workflow
**Jira:** RAISE-333
**Size:** M
**Date:** 2026-03-01

---

## Tasks

### T1: WorkLifecycleEvent + bridge in emit-work

**Size:** S
**Dependencies:** None

**RED:** Tests for:
- `WorkLifecycleEvent` has correct fields (work_type, work_id, event, phase)
- `emit-work` CLI command fires `WorkLifecycleEvent` via hook system (mock emitter)

**GREEN:**
- Add `WorkLifecycleEvent` dataclass to `src/rai_cli/hooks/events.py`
- Add bridge in `src/rai_cli/cli/commands/signal.py` `emit_work()` — after telemetry write, fire event

**Files:**
- Modify: `src/rai_cli/hooks/events.py`
- Modify: `src/rai_cli/cli/commands/signal.py`
- Create: `tests/hooks/test_backlog_hook.py` (event tests)

**Verification:** `uv run pytest tests/hooks/test_backlog_hook.py && uv run pyright src/rai_cli/hooks/events.py src/rai_cli/cli/commands/signal.py && uv run ruff check src/rai_cli/hooks/events.py src/rai_cli/cli/commands/signal.py`

### T2: BacklogHook — event-to-backlog mapping

**Size:** M
**Dependencies:** T1

**RED:** Tests for:
- `start` event → search for Jira key, create if missing, transition to in-progress
- `complete` event → search for Jira key, transition to done
- Other events (blocked, unblocked) → no-op, returns ok
- Adapter unavailable → returns error result (no crash)
- No jira.yaml → returns error result (no crash)
- Jira key resolution via search using work_id

**GREEN:**
- Create `src/rai_cli/hooks/builtin/backlog.py`
- `BacklogHook` class: resolve adapter, load jira.yaml, map event→action, execute
- Helper: `_load_lifecycle_mapping()` from `.raise/jira.yaml`
- Helper: `_resolve_jira_key()` via adapter.search

**Files:**
- Create: `src/rai_cli/hooks/builtin/backlog.py`
- Modify: `tests/hooks/test_backlog_hook.py` (add hook tests)

**Verification:** `uv run pytest tests/hooks/test_backlog_hook.py && uv run pyright src/rai_cli/hooks/builtin/backlog.py && uv run ruff check src/rai_cli/hooks/builtin/backlog.py`

### T3: Entry point registration + integration test

**Size:** XS
**Dependencies:** T2

**RED:** Tests for:
- BacklogHook discovered via entry points
- End-to-end: `emit-work story X --event start` → adapter.create_issue + adapter.transition_issue called (mocked adapter)

**GREEN:**
- Add `backlog = "rai_cli.hooks.builtin.backlog:BacklogHook"` to `pyproject.toml` under `[project.entry-points."rai.hooks"]`
- Verify discovery works

**Files:**
- Modify: `pyproject.toml`
- Modify: `tests/hooks/test_backlog_hook.py` (integration test)

**Verification:** `uv run pytest tests/hooks/test_backlog_hook.py && uv run pytest --tb=short -q`

## Execution Order

T1 → T2 → T3 (sequential, each depends on previous)

## Risks

| Risk | L/I | Mitigation |
|------|:---:|------------|
| `resolve_adapter()` imports heavy MCP dependencies | M/L | Lazy import inside handle(), timeout enforced by emitter |
| Jira key resolution by search is fragile (summary match) | M/M | Use `--format agent` for reliable parsing, exact match on work_id |
| Hook system entry point discovery requires `uv sync` after pyproject.toml change | L/L | Run `uv sync` in T3 |

## Duration Tracking

| Task | Estimated | Actual | Notes |
|------|-----------|--------|-------|
| T1 | 15 min | — | |
| T2 | 30 min | — | |
| T3 | 10 min | — | |
