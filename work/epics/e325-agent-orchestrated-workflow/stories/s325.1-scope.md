# Scope: S325.1 — Skill I/O Contract Definition

**Story:** S325.1
**Epic:** E325 Agent-Orchestrated Workflow
**Size:** S
**Branch:** epic/e325/agent-orchestrated-workflow (S-sized, stays on epic)

---

## In Scope

- Define `raise.inputs` and `raise.outputs` metadata schema for SKILL.md frontmatter
- Add I/O contracts to all 12 lifecycle skills (6 story + 4 epic + 2 session)
- Sync builtin → deployment copies for all modified skills
- Validator accepts I/O metadata without error (optional fields, backward compatible)

## Out of Scope

- Code enforcement of contracts (metadata only, agent reads it)
- Non-lifecycle skills (rai-research, rai-debug, rai-quality-review, etc.)
- Orchestrator logic (S325.6)
- State file creation (architecture review Q5: use git-derived state)

## Done Criteria

- [ ] All 12 lifecycle SKILL.md files have `raise.inputs` and `raise.outputs` in frontmatter
- [ ] Schema is consistent across all skills (same field names, types, sources)
- [ ] Validator passes on all modified skills (`rai skill validate`)
- [ ] Non-lifecycle skills validate without I/O fields (backward compat)
- [ ] Builtin and deployment copies identical (verified with diff)
- [ ] Existing tests pass

---

*Initialized: 2026-03-01, SES-305*
