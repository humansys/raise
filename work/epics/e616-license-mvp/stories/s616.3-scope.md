# S616.3: E2E Integration Tests — Scope

**Story:** S616.3 | **Epic:** E616 | **Size:** S
**Depends on:** S616.1 (done), S616.2 (done)

## In Scope

- Pytest E2E test module (`tests/e2e/`) with real PG via docker compose
- `conftest.py` with fixtures: DB engine, session factory, seed data, httpx AsyncClient
- Cross-story flow: seed → auth → plan enforcement → CRUD → license replace
- `@pytest.mark.e2e` marker for selective execution
- pytest config to skip E2E by default (opt-in via `-m e2e`)

## Out of Scope

- CI pipeline configuration (just the tests, CI comes with deployment story)
- Performance/load testing
- Mock-based tests (those already exist in unit tests)

## Done Criteria

- [ ] E2E tests pass against real PG with `uv run pytest tests/e2e/ -m e2e`
- [ ] Full cross-story flow validated (S616.1 auth + S616.2 CRUD)
- [ ] Soft-deleted member auth rejection verified
- [ ] License replacement reflects in plan enforcement
- [ ] Tests skip cleanly when PG is unavailable
- [ ] Unit tests still pass without PG
