# S616.1: Data Model, Auth, and Plan Enforcement — Scope

**Epic:** E616 (raise-server license MVP)
**Branch:** story/s616.1/data-model-auth-plan

## In Scope

- Alembic migration 003: drop old `api_keys`, create `members`, `api_keys` (new), `licenses`
- SQLAlchemy models: `MemberRow`, `ApiKeyRow`, `LicenseRow`
- Pydantic schemas: `MemberContext`, request/response models
- `verify_member()` replacing `verify_api_key()` in auth.py
- `requires_plan(minimum)` FastAPI dependency
- `requires_role(role)` FastAPI dependency
- Update all existing routers (graph, memory, agent) to use new auth
- Unit tests for models, auth, plan check

## Out of Scope

- New endpoints (organizations, members, keys, license CRUD) — S616.2
- Seed script — S616.2
- CLI changes — E621

## Done Criteria

- [ ] Migration 003 creates members, api_keys, licenses tables
- [ ] Old api_keys table dropped
- [ ] `verify_member()` returns MemberContext with plan + features
- [ ] `requires_plan()` returns 403 for insufficient plan
- [ ] `requires_role()` returns 403 for insufficient role
- [ ] Existing endpoints (graph, memory, agent) use new auth
- [ ] No license → community plan (not error)
- [ ] Expired license → community plan
- [ ] All tests pass
