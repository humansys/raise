WHAT: `rai doctor --fix` auto-remediates common issues with backup before mutation.
DONE WHEN: --fix rebuilds stale graph, scaffolds missing config, and creates .bak backups. Tests verify fix actions.
