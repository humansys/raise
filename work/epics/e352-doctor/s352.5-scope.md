WHAT: `rai doctor report` generates a diagnostic report to a local .md file. `rai doctor report --send` opens a mailto: link with the report pre-filled.
DONE WHEN: Report contains only non-sensitive data. mailto opens email client. Fallback copies to clipboard.
