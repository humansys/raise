WHAT: Built-in EnvironmentCheck that validates Python version, rai-cli version, OS, installed extras, and optional dependencies.
DONE WHEN: `rai doctor` shows environment category with pass/warn/error results. Tests cover all checks.
