WHAT: Built-in ProjectCheck that validates .raise/ structure, manifest.yaml coherence, graph staleness, adapter config presence, and skill sync status.
DONE WHEN: `rai doctor` shows project category with pass/warn/error results. Tests cover all checks.
