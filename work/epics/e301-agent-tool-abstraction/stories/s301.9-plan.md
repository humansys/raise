# S301.9: FilesystemPMAdapter — Plan

> **Size:** S | **Tasks:** 3 | **Date:** 2026-03-01
> **Design:** `s301.9-design.md`

## Tasks

### T1: FilesystemPMAdapter — read operations (M)

**Files:** `src/rai_cli/adapters/filesystem.py` (create), `tests/adapters/test_filesystem.py` (create)

**TDD:**
- RED: test search returns IssueSummary from backlog table, test get_issue returns IssueDetail, test health reports healthy/unhealthy, test get_comments returns [], test search with no backlog returns []
- GREEN: FilesystemPMAdapter class with `__init__(project_root)`, reuse `extract_epics()` for parsing, simple field=value query filter, bare text name match
- REFACTOR: extract shared parsing if needed

**Verify:** `pytest tests/adapters/test_filesystem.py -x && ruff check src/rai_cli/adapters/filesystem.py && pyright src/rai_cli/adapters/filesystem.py`

### T2: Write operations + no-ops (S)

**Depends on:** T1

**Files:** `src/rai_cli/adapters/filesystem.py` (modify), `tests/adapters/test_filesystem.py` (modify)

**TDD:**
- RED: test create_issue appends row with auto-ID, test transition_issue updates status with emoji, test update_issue modifies fields, test batch_transition loops, test no-ops (add_comment, link_to_parent, link_issues) return empty refs
- GREEN: Implement markdown table writer — find table end, append/update rows, status→emoji mapping, auto-increment ID
- REFACTOR: ensure writes preserve existing formatting

**Verify:** `pytest tests/adapters/test_filesystem.py -x && ruff check src/rai_cli/adapters/filesystem.py && pyright src/rai_cli/adapters/filesystem.py`

### T3: Entry point + integration test (XS)

**Depends on:** T2

**Files:** `pyproject.toml` (modify)

**Steps:**
- Register entry point: `filesystem = rai_cli.adapters.filesystem:FilesystemPMAdapter`
- `uv sync` to refresh entry points
- Manual integration: `rai backlog search "status = complete" --adapter filesystem`
- Verify `isinstance(adapter, ProjectManagementAdapter)` in test

**Verify:** `uv sync && pytest tests/adapters/test_filesystem.py -x && rai backlog search "status = complete" --adapter filesystem`

## Execution Order

```
T1 (read) → T2 (write) → T3 (entry point + integration)
```

Sequential — each builds on the previous. Risk-first: T1 validates that
BacklogParser output maps cleanly to adapter models.

## Risks

| Risk | Impact | Mitigation |
|------|--------|------------|
| BacklogParser output doesn't map cleanly to IssueSummary/IssueDetail | Medium | T1 validates early, adapt mapping |
| Table write corrupts existing formatting | Medium | T2 uses regex on exact row, tests with real backlog format |

## Duration Tracking

| Task | Estimated | Actual | Notes |
|------|-----------|--------|-------|
| T1 | 20 min | | |
| T2 | 20 min | | |
| T3 | 5 min | | |
| **Total** | **~45 min** | | |
