# Scope: S301.2 — rai backlog CLI group

## In Scope

- `rai backlog` Typer group registered in CLI app
- 7 commands: `create`, `transition`, `update`, `link`, `comment`, `search`, `batch-transition`
- Each command instantiates adapter via factory pattern (no global import)
- Compact output formatter for backlog operations (<200 tokens per response)
- `--adapter mock` flag to substitute StubPMAdapter for testing
- Unit tests for all 7 commands using StubPMAdapter
- CLI integration tests (CliRunner) validating compact output format

## Out of Scope

- JiraAdapter (real Jira integration) → S301.3
- Offline queue / pending sync → Phase 2 (E275)
- `rai backlog reconcile` → Phase 2
- Authentication / credential management → S301.3
- Auto-sync hooks → S301.6
- Output to JSON format → future story

## Done Criteria (Observable Outcomes)

1. `rai backlog --help` lists all 7 commands
2. `rai backlog transition RAISE-XXX done --adapter mock` exits 0 with compact output
3. `rai backlog search "status=Open" --adapter mock` returns compact list
4. `rai backlog create --type Task "Title" --adapter mock` returns created key
5. All 7 commands have unit tests using StubPMAdapter — all green
6. `pytest tests/backlog/` passes with zero failures
7. `mypy src/rai_cli/commands/backlog.py` clean
8. `ruff check src/rai_cli/commands/backlog.py` clean

## Dependencies

- S301.1 (Protocols + Models) — `ProjectManagementAdapter` protocol and Pydantic models must exist ✅
- StubPMAdapter from S301.1 tests reused here
