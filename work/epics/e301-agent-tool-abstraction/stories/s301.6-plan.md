# S301.6: Skill Auto-Sync Hooks — Implementation Plan

> **Size:** S (3 tasks)
> **Date:** 2026-03-01 (SES-306)
> **Design:** `s301.6-design.md`

## Tasks

### T1: Work lifecycle events [XS]

**What:** Add `WorkStartEvent` and `WorkCloseEvent` to `events.py`, add to parametrized test list in `test_events.py`.

**Files:**
- MODIFY: `src/rai_cli/hooks/events.py` — add 2 frozen dataclasses
- MODIFY: `tests/test_hooks/test_events.py` — add 2 entries to `_ALL_EVENTS`

**TDD:**
- RED: Add events to `_ALL_EVENTS` list, import them → ImportError
- GREEN: Add dataclasses to `events.py`
- REFACTOR: Verify parametrized tests pass (event_name, frozen, isinstance, timestamp)

**Verify:** `pytest tests/test_hooks/test_events.py -v`

**Dependencies:** None

---

### T2: JiraSyncHook + entry point [S]

**What:** Create `JiraSyncHook` implementing `LifecycleHook`, with `_load_lifecycle_mapping()` and `_resolve_status_name()` config readers. Register entry point in `pyproject.toml`.

**Files:**
- CREATE: `src/rai_cli/hooks/builtin/jira_sync.py` — hook + config readers
- MODIFY: `pyproject.toml` — add `jira-sync` entry point
- CREATE: `tests/test_hooks/test_jira_sync.py` — unit tests

**TDD:**
- RED: Test `JiraSyncHook` isinstance `LifecycleHook` → ImportError
- GREEN: Implement hook class with handle(), config readers
- Tests to write:
  - `test_protocol_conformance` — isinstance LifecycleHook
  - `test_events_and_priority` — events = ["work:start", "work:close"], priority = -10
  - `test_story_start_transitions` — mock adapter, assert transition_issue called with correct args
  - `test_story_close_transitions` — same for close
  - `test_epic_start_transitions` — work_type="epic"
  - `test_no_issue_key_skips` — empty issue_key → ok, no adapter call
  - `test_no_lifecycle_mapping_skips` — no config → ok, no adapter call
  - `test_no_mapping_for_key_skips` — mapping exists but no entry for this event type
  - `test_adapter_failure_returns_error` — adapter raises → HookResult(status="error")
  - `test_load_lifecycle_mapping` — reads from jira.yaml fixture
  - `test_resolve_status_name` — reverse lookup from status_mapping

**Verify:** `pytest tests/test_hooks/test_jira_sync.py -v && ruff check src/rai_cli/hooks/builtin/jira_sync.py && pyright src/rai_cli/hooks/builtin/jira_sync.py`

**Dependencies:** T1 (needs WorkStartEvent/WorkCloseEvent)

---

### T3: Integration test + full suite verification [XS]

**What:** Test that `EventEmitter` with a manually registered `JiraSyncHook` dispatches work events correctly. Verify full test suite passes.

**Files:**
- MODIFY: `tests/test_hooks/test_jira_sync.py` — add integration test class

**Tests:**
- `test_emitter_dispatches_to_jira_sync_hook` — create emitter, register hook, emit WorkStartEvent, verify adapter called
- `test_emitter_error_isolation` — adapter raises, emitter result has no handler_errors (hook catches internally)

**Verify:** `pytest tests/ -x -q && ruff check src/ && pyright src/rai_cli/hooks/builtin/`

**Dependencies:** T2

---

## Execution Order

```
T1 (events) → T2 (hook + tests) → T3 (integration + full verify)
```

Linear sequence — each builds on previous. Risk-first: T1 is lowest risk (pattern copy), T2 is the core logic, T3 validates integration.

## Duration Tracking

| Task | Estimated | Actual | Notes |
|------|-----------|--------|-------|
| T1 | 5 min | — | Pattern copy from existing events |
| T2 | 20 min | — | Core hook + 11 tests |
| T3 | 5 min | — | Integration + full suite |
| **Total** | **30 min** | — | |
