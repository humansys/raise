# Plan: S301.2 — rai backlog CLI group

- **Story:** S301.2 (M)
- **Date:** 2026-02-27
- **Design:** s301.2-design.md (8 decisions + 5 arch review findings)

## Tasks

### T1: resolve_adapter() + tests (S)
**What:** Create `resolve_adapter()` function with auto-detect logic (D3, AR3).

**Files:**
- Create: `src/rai_cli/cli/commands/_adapter_resolve.py`
- Create: `tests/cli/test_adapter_resolve.py`

**TDD cycle:**
- RED: 4 test cases — 0 adapters (error), 1 adapter (auto), 2+ adapters (error), --adapter flag override. Monkeypatch `get_pm_adapters()` to control registry returns.
- GREEN: Implement `resolve_adapter(adapter_name: str | None) -> ProjectManagementAdapter`. Calls `get_pm_adapters()`, counts results, instantiates with no-arg constructor.
- REFACTOR: Error messages clear and actionable (AR3).

**Verify:** `pytest tests/cli/test_adapter_resolve.py -v && mypy src/rai_cli/cli/commands/_adapter_resolve.py && ruff check src/rai_cli/cli/commands/_adapter_resolve.py`

**Dependencies:** None (first task, riskiest — resolver is the foundation)

---

### T2: Rewrite backlog.py — 7 commands (M)
**What:** Delete demo commands, write 7 new commands delegating to adapter. Delete orphaned test file (AR2).

**Files:**
- Rewrite: `src/rai_cli/cli/commands/backlog.py`
- Delete: `tests/cli/test_backlog_auth.py`

**Implementation (not TDD — glue code, PAT-E-444):**
- Delete all existing content of backlog.py
- Write 7 commands: create, transition, update, link, comment, search, batch-transition
- Each command: parse args → resolve_adapter() → call adapter method → print result
- All commands accept `--adapter NAME` optional flag
- `link` uses only `link_issues()` (AR4)
- `search` --help notes query format is adapter-specific (AR5)
- `create` has `--project` obligatory (D8), full signature (D7)
- Output: inline f-strings (D4)
- Delete `tests/cli/test_backlog_auth.py`

**Verify:** `ruff check src/rai_cli/cli/commands/backlog.py && mypy src/rai_cli/cli/commands/backlog.py`

**Dependencies:** T1 (needs resolve_adapter)

---

### T3: batch-transition parsing + tests (S)
**What:** The batch-transition command parses comma-separated keys. This has edge cases worth testing.

**Files:**
- Modify: `src/rai_cli/cli/commands/backlog.py` (parsing logic, if not trivial inline)
- Create: `tests/cli/test_backlog_batch.py`

**TDD cycle:**
- RED: Test cases — "RAISE-1,RAISE-2" → ["RAISE-1", "RAISE-2"], spaces around commas, empty string, single key, trailing comma.
- GREEN: Implement parsing (may be trivial — `keys.split(",")` with strip).
- REFACTOR: If parsing is truly a one-liner, fold tests into T1 test file and skip separate file.

**Verify:** `pytest tests/cli/test_backlog_batch.py -v`

**Dependencies:** T2 (command must exist)

---

### T4: Smoke test + integration verify (S)
**What:** Smoke test that `rai backlog --help` lists all 7 commands. Manual verification that wiring works end-to-end.

**Files:**
- Create: `tests/cli/test_backlog_smoke.py`

**TDD cycle:**
- RED: Test `rai backlog --help` output contains all 7 command names.
- GREEN: Should already pass if T2 is correct.
- REFACTOR: N/A.

**Manual integration:** Run `rai backlog --help` in terminal, verify output.

**Verify:** `pytest tests/cli/test_backlog_smoke.py -v && pytest tests/cli/test_adapter_resolve.py -v && mypy src/rai_cli/cli/commands/backlog.py src/rai_cli/cli/commands/_adapter_resolve.py && ruff check src/rai_cli/cli/commands/`

**Dependencies:** T2, T3

## Execution Order

```
T1 (resolve_adapter + tests)
 └──→ T2 (rewrite backlog.py, delete demo)
       └──→ T3 (batch parsing + tests)
             └──→ T4 (smoke + full verify)
```

**Rationale:** Risk-first — T1 is the only piece with real logic and unknowns (entry point instantiation). T2 is mechanical glue. T3 handles the one edge-case-prone command. T4 validates everything together.

## Risks

| Risk | Mitigation |
|------|------------|
| No-arg constructor fails for some adapter class | Error message explains failure. S301.3 will handle config injection. |
| Existing tests elsewhere depend on demo backlog commands | AR1 confirmed blast radius is safe — only main.py imports |
| Typer argument parsing edge cases | Covered by T3 tests for batch, smoke test for wiring |

## Duration Tracking

| Task | Estimated | Actual | Notes |
|------|-----------|--------|-------|
| T1 | 20 min | | |
| T2 | 30 min | | |
| T3 | 15 min | | |
| T4 | 10 min | | |
| **Total** | **~75 min** | | |
