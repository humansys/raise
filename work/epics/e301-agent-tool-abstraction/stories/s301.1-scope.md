# S301.1: Protocols + Models — Scope

## In Scope

- Extend `ProjectManagementAdapter` protocol: add `batch_transition`, `link_to_parent`, `link_issues`, `add_comment`, `get_comments`, `search`, `health`
- Extend `DocumentationTarget` protocol: add `get_page`, `search`
- New Pydantic models in `models.py`: `IssueDetail`, `IssueSummary`, `Comment`, `CommentRef`, `BatchResult`, `PageContent`, `PageSummary`, `AdapterHealth`
- Update existing `get_issue` signature: `ref: IssueRef` → `key: str` return `IssueDetail`
- Update existing `transition_issue` signature: `transition: str` → `status: str`

## Out of Scope

- Concrete adapter implementations (S301.3, S301.5)
- CLI commands (S301.2, S301.4)
- Registry or discovery mechanism
- Config loading

## Done When

- `ProjectManagementAdapter` has all methods from scope.md protocol contract
- `DocumentationTarget` has `can_publish`, `publish`, `get_page`, `search`
- All new models instantiate, validate, and serialize correctly
- Existing `IssueRef`, `IssueSpec`, `PublishResult` unchanged (backward compat)
- `@runtime_checkable` preserved on both protocols
- Tests pass, types check, lint clean
