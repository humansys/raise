# S301.1: Protocols + Models — Scope

> **Size:** M (upgraded from S after architecture review)

## In Scope

- Extend `ProjectManagementAdapter` sync protocol (4→11 methods): add `batch_transition`, `link_to_parent`, `link_issues`, `add_comment`, `get_comments`, `search`, `health`
- Extend `DocumentationTarget` sync protocol (2→5 methods): add `get_page`, `search`, `health`
- New `AsyncProjectManagementAdapter` protocol (11 async methods, mirrors sync)
- New `AsyncDocumentationTarget` protocol (5 async methods, mirrors sync)
- New `SyncPMAdapter` wrapper: bridges async PM adapter to sync protocol
- New `SyncDocsAdapter` wrapper: bridges async docs target to sync protocol
- New Pydantic models in `models.py`: `IssueDetail`, `IssueSummary`, `Comment`, `CommentRef`, `FailureDetail`, `BatchResult`, `PageContent`, `PageSummary`, `AdapterHealth`
- Update existing `get_issue` signature: `ref: IssueRef` → `key: str`, return `IssueDetail`
- Update existing `update_issue` signature: `ref: IssueRef` → `key: str`
- Update existing `transition_issue` signature: `ref: IssueRef, transition: str` → `key: str, status: str`
- Update `__init__.py`: re-export all new models and protocols in `__all__`

## Out of Scope

- Concrete adapter implementations (S301.3, S301.5)
- CLI commands (S301.2, S301.4)
- Registry or discovery mechanism changes
- Config loading or adapter instantiation logic
- Changes to `GovernanceSchemaProvider` or `GovernanceParser` protocols

## Done When

- `ProjectManagementAdapter` (sync) has 11 methods per design
- `AsyncProjectManagementAdapter` has 11 async methods mirroring sync
- `DocumentationTarget` (sync) has 5 methods per design
- `AsyncDocumentationTarget` has 5 async methods mirroring sync
- `SyncPMAdapter` wraps async adapter and satisfies sync protocol (isinstance check)
- `SyncDocsAdapter` wraps async target and satisfies sync protocol (isinstance check)
- All 9 new models instantiate, validate, and serialize correctly
- `IssueDetail` substitutes for `IssueRef` (Liskov)
- `@runtime_checkable` on all 4 protocols
- `__init__.py` exports updated
- Existing models unchanged (backward compat)
- Tests pass, types check (pyright), lint clean (ruff)
