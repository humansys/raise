# S301.5: McpConfluenceAdapter — Plan

> **Size:** S (3-5 SP)
> **Tasks:** 3
> **Date:** 2026-03-01
> **Design:** `s301.5-design.md` (7 decisions, probed formats)

## Tasks

### T1: Bridge array parsing (XS)

**Goal:** `McpBridge._parse_result()` handles JSON arrays from MCP servers.

**Files:**
- Modify: `src/rai_cli/adapters/mcp_bridge.py`
- Modify: `tests/adapters/test_mcp_bridge.py`

**TDD:**
- RED: Test `_parse_result()` with a `CallToolResult` containing JSON array text → expect `data["items"]` with the array contents
- RED: Test existing dict parsing still works (regression)
- GREEN: Add `elif isinstance(parsed, list): data = {"items": parsed}` in `_parse_result()`
- REFACTOR: None expected

**AC:** Design D6 — arrays wrapped without breaking existing dict flows.

**Verify:** `pytest tests/adapters/test_mcp_bridge.py -x && ruff check src/rai_cli/adapters/mcp_bridge.py && pyright src/rai_cli/adapters/mcp_bridge.py`

---

### T2: McpConfluenceAdapter + entry point (S)

**Goal:** Adapter implementing `AsyncDocumentationTarget` via McpBridge.

**Files:**
- Create: `src/rai_cli/adapters/mcp_confluence.py`
- Create: `tests/adapters/test_mcp_confluence.py`
- Modify: `pyproject.toml` (entry point `rai.docs.targets`)

**TDD:**
- RED: `test_can_publish_returns_true` — no MCP call
- RED: `test_publish_create_new_page` — mock bridge, no yaml entry → calls `confluence_create_page`, returns PublishResult, saves page_id to yaml
- RED: `test_publish_update_existing` — mock bridge, yaml has entry → calls `confluence_update_page`
- RED: `test_publish_auto_heal` — mock bridge, yaml has stale entry → update fails → removes entry → creates new page
- RED: `test_get_page` — mock bridge → returns PageContent from `metadata.*` format
- RED: `test_search` — mock bridge → returns list[PageSummary] from `data["items"]` array
- RED: `test_health_ok` / `test_health_fail` — mock bridge
- RED: `test_config_missing` — no `.raise/confluence.yaml` → FileNotFoundError
- RED: `test_space_key_from_yaml` / `test_space_key_env_fallback`
- GREEN: Implement adapter class with parsers
- REFACTOR: Extract common patterns if duplication with McpJiraAdapter

**AC:** Design MUST 1, 2, 3, 5 + SHOULD 1, 2.

**Verify:** `pytest tests/adapters/test_mcp_confluence.py -x && ruff check src/rai_cli/adapters/mcp_confluence.py && pyright src/rai_cli/adapters/mcp_confluence.py`

---

### T3: Integration test (XS)

**Goal:** End-to-end verification — entry point discovery + protocol compliance + smoke.

**Files:**
- Modify: `tests/adapters/test_mcp_confluence.py` (add integration tests)

**TDD:**
- RED: `test_entry_point_discoverable` — `get_doc_targets()` returns dict with "confluence" key
- RED: `test_protocol_compliance` — `isinstance(McpConfluenceAdapter(...), AsyncDocumentationTarget)`
- RED: `test_sync_wrapper_satisfies_protocol` — `isinstance(SyncDocsAdapter(adapter), DocumentationTarget)`
- GREEN: Fix any issues (likely entry point config)
- Manual smoke: `uv run rai docs search "test"` against real Confluence

**AC:** Design MUST 5 — entry point registered and discoverable.

**Verify:** `pytest tests/adapters/test_mcp_confluence.py -x && uv run rai docs search "test"`

## Execution Order

```
T1 (bridge array) → T2 (adapter) → T3 (integration)
```

Linear. Risk-first: T1 touches shared code (bridge).

## Risks

| Risk | Mitigation |
|------|------------|
| Bridge array change breaks Jira | T1 includes regression test for dict parsing |
| Entry point not discovered after install | T3 tests discovery explicitly; `uv sync` before smoke |
| Confluence page IDs change format | Parsers use string IDs from probed format |

## Duration Tracking

| Task | Estimated | Actual | Notes |
|------|:---------:|:------:|-------|
| T1 | 10min | — | |
| T2 | 30min | — | |
| T3 | 10min | — | |
| **Total** | **50min** | — | |
