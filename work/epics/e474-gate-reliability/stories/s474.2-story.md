---
story_id: "S474.2"
epic_ref: "E474"
size: "S"
type: "code"
status: "draft"
---

# S474.2: FormatGate + Update gate-code.md

## User Story

As a RaiSE developer, I want a FormatGate that runs `ruff format --check` so that formatting errors are caught by `rai gate check --all` before reaching CI.

## Acceptance Criteria

- AC1: FormatGate class exists in `src/raise_cli/gates/builtin/format.py`, registered via entry point
- AC2: `rai gate check --all` includes format gate and catches formatting violations
- AC3: `gate-code.md` updated to reflect Python stack (pytest, ruff check, ruff format, pyright) instead of npm/JS
- AC4: All existing tests pass, new tests cover FormatGate
