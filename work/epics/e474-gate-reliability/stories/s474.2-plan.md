# Plan: S474.2 — Configurable Gates + FormatGate

**Story:** S474.2 | **Size:** S (3-5 SP) | **Tasks:** 3 + integration test

## Task Overview

| # | Task | Size | Dependencies | Risk |
|---|------|------|-------------|------|
| T1 | Shared runner + model extension + detection table | M | none | HIGH — foundational; all other tasks depend on it |
| T2 | Refactor 4 existing gates + create FormatGate + entry point | M | T1 | MEDIUM — mechanical but touches 5 files + pyproject.toml |
| T3 | Update gate-code.md governance doc | XS | T2 | LOW — documentation only |
| T4 | Manual integration test | XS | T3 | LOW |

---

## T1: Shared Runner + Model Extension + Detection Table

**Description:** Create `_runner.py` helper, add `format_command` to `ProjectInfo`, add `format_command` to all 14 language toolchains in detection.py, update local manifest.yaml.

**Files to create:**
- `src/raise_cli/gates/builtin/_runner.py`
- `tests/test_gates/test_runner.py`

**Files to modify:**
- `src/raise_cli/onboarding/manifest.py` — add `format_command: str | None = None` to `ProjectInfo`
- `src/raise_cli/onboarding/detection.py` — add `format_command` to `ToolchainInfo` and all entries in `LANGUAGE_TOOLCHAIN`
- `.raise/manifest.yaml` — add `format_command: "uv run ruff format --check"`

**TDD cycle:**

RED:
- Test `run_manifest_command` returns failed result when no manifest found
- Test `run_manifest_command` returns skip (passed=True) when command key not configured
- Test `run_manifest_command` returns passed when command exits 0
- Test `run_manifest_command` returns failed with details when command exits non-zero
- Test `run_manifest_command` handles subprocess exception gracefully
- Test `ToolchainInfo` has `format_command` field
- Test `ProjectInfo` has `format_command` field
- Test Python toolchain includes `format_command`

GREEN: Implement `_runner.py`, update models.

REFACTOR: Extract shared patterns if any emerge.

**AC coverage:** AC4 (skip when not configured), AC5 (format_command in detection), AC7 (three test scenarios)

**Verification:**
```bash
uv run pytest tests/test_gates/test_runner.py tests/test_onboarding/ -x --tb=short
uv run pyright src/raise_cli/gates/builtin/_runner.py src/raise_cli/onboarding/manifest.py src/raise_cli/onboarding/detection.py
```

---

## T2: Refactor Existing Gates + Create FormatGate + Entry Point

**Description:** Refactor TestGate, LintGate, TypeGate, CoverageGate to delegate to `run_manifest_command`. Create new FormatGate. Register FormatGate entry point.

**Files to create:**
- `src/raise_cli/gates/builtin/format.py`

**Files to modify:**
- `src/raise_cli/gates/builtin/tests.py` — use `run_manifest_command("gate-tests", "test_command", ...)`
- `src/raise_cli/gates/builtin/lint.py` — use `run_manifest_command("gate-lint", "lint_command", ...)`
- `src/raise_cli/gates/builtin/types.py` — use `run_manifest_command("gate-types", "type_check_command", ...)`
- `src/raise_cli/gates/builtin/coverage.py` — use runner with coverage-specific logic (test_command + coverage flags)
- `pyproject.toml` — add `format = "raise_cli.gates.builtin.format:FormatGate"` to `[project.entry-points."rai.gates"]`
- `tests/test_gates/test_builtin_gates.py` — update existing tests for refactored gates, add FormatGate tests, update protocol conformance fixture to include FormatGate, update entry point discovery to expect 5 gates

**TDD cycle:**

RED:
- Test FormatGate protocol conformance (gate_id, description, workflow_point, evaluate)
- Test FormatGate reads `format_command` from manifest
- Test FormatGate pass/fail/skip scenarios
- Test existing gate tests still pass with refactored implementation
- Test entry point discovery finds 5 gates (not 4)

GREEN: Implement FormatGate, refactor 4 existing gates, register entry point.

REFACTOR: Ensure consistent error handling across all gates.

**AC coverage:** AC1 (FormatGate reads format_command), AC2 (all gates read from manifest), AC3 (5 gates discoverable), AC7 (FormatGate test scenarios)

**Verification:**
```bash
uv run pytest tests/test_gates/ -x --tb=short
uv run pyright src/raise_cli/gates/builtin/
uv run ruff check src/raise_cli/gates/builtin/
```

---

## T3: Update gate-code.md Governance Doc

**Description:** Rewrite `.raise/gates/gate-code.md` to reference manifest-driven gates with Python stack examples (pytest, ruff check, ruff format, pyright) instead of npm/JS.

**Files to modify:**
- `.raise/gates/gate-code.md`

**No TDD** (documentation-only task).

**AC coverage:** AC6

**Verification:** Manual review of updated content.

---

## T4: Manual Integration Test

**Description:** Validate end-to-end with running software.

**Steps:**
1. Run `uv run rai gate check --all` — verify 5 gates listed, all pass
2. Run `uv run rai gate list` — verify format gate appears
3. Verify `.raise/manifest.yaml` includes `format_command`
4. Run full test suite: `uv run pytest --tb=short`
5. Run type checks: `uv run pyright`
6. Run lint: `uv run ruff check`

**AC coverage:** AC3 (5 gates, all pass), full regression

---

## Dependency Graph

```
T1 (runner + model + detection)
 |
 v
T2 (refactor gates + FormatGate + entry point)
 |
 v
T3 (gate-code.md update)
 |
 v
T4 (integration test)
```

Strictly sequential. T1 is risk-first (foundational helper).

## Notes

- CoverageGate is a special case: it uses `test_command` but appends coverage flags. The runner helper should support an `extra_args` parameter or CoverageGate can compose the command itself.
- Existing tests mock `subprocess.run` — refactored gates will need mocks at the `load_manifest` + `subprocess.run` level instead. Update test fixtures accordingly.
- The scope document says "Out of Scope: Other gates" but the design (s474.2-design.md) explicitly includes refactoring all 4 gates. Following the design as the later, more refined artifact.
