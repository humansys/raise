# S369.3 -- CI Hardening: Plan

## Tasks

### T1: Add Python 3.13 to CI matrix

- **File:** `.github/workflows/ci.yml`
- **Action:** Add `"3.13"` to the `python-version` matrix array
- **Detail:** Add `continue-on-error` conditional on Python 3.13 so failures
  are visible but non-blocking (per D-2 in design)
- **Verification:** `grep -A3 "python-version" .github/workflows/ci.yml` shows
  both 3.12 and 3.13; YAML syntax valid (`python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"`)
- **Commit:** `fix(s369.3): add Python 3.13 to CI test matrix`

### T2: Document Trusted Publishers setup

- **File:** Create `work/epics/e369-open-source-audit/stories/s369.3-trusted-publishers.md`
- **Action:** Document what release.yml already provides and what manual PyPI
  configuration is needed
- **Content:**
  - Workflow-side: `id-token: write`, `environment: pypi`, `pypa/gh-action-pypi-publish@release/v1`
  - Manual config: add Trusted Publisher on PyPI for raise-cli (and future
    raise-core, raise-server): owner=humansys-ai, repo=raise-commons,
    workflow=release.yml, environment=pypi
- **Verification:** File exists and contains all required sections
- **Commit:** `docs(s369.3): document Trusted Publishers setup requirements`

### T3: Verify CI badge and create progress log

- **File:** Verify `README.md` badge URL; create
  `work/epics/e369-open-source-audit/stories/s369.3-progress.md`
- **Action:** Confirm badge URL (`ci.yml`) matches workflow filename; create
  progress log summarizing T1-T3 outcomes
- **Verification:** Badge URL string matches workflow filename
- **Commit:** `chore(s369.3): verify CI badge and create progress log`

## Execution Order

T1 -> T2 -> T3 (sequential)

## Notes

- No TDD -- CI configuration changes only
- No code changes to src/ or tests/
- continue-on-error for 3.13 keeps main branch green while gaining visibility
