# S369.1 -- Documentation Polish: Design

## Problem

Community docs contain stale package names, wrong URLs, and outdated version
references. A senior dev evaluating raise-commons would notice these
inconsistencies immediately.

## Approach

Direct text edits to 4 files. No code, no tests, no automation.

## Changes

| File | What to fix |
|------|-------------|
| CONTRIBUTING.md | `rai-cli` / `src/rai_cli/` -> `raise-cli` / `src/raise_cli/` |
| SECURITY.md | Version table: remove "2.0.0-alpha.x", reflect current 2.x |
| CHANGELOG.md | Compare URLs: `humansys/raise` -> `humansys-ai/raise-commons` |
| CHANGELOG.md | Add missing blank lines between version entries |
| README.md | Add CI badge |
| README.md | Verify Python 3.14 claim, correct if needed |

## Verification

```bash
grep -r "rai-cli\|rai_cli\|humansys/raise[^-]" *.md
# Expected: no matches
```

## Decisions

- **D-1:** Direct text edits, no automation. XS story, 4 files -- automation
  would be over-engineering.
