# S369.4 Progress Log — Secrets and Hygiene Scan

## Task 1: Secrets Scan

**Status:** CLEAN

### API Keys / Secrets (`sk-`, `api_key=`, `secret_key=`, `password=`)
- `src/raise_cli/graph/backends/__init__.py:31` — reads `RAI_API_KEY` from env var (not hardcoded)
- `src/raise_cli/graph/backends/api.py:39` — accepts `api_key` parameter (not hardcoded)
- `src/rai_pro/providers/jira/models.py:100` — comment containing "task-specific" (false positive on `sk-`)
- `src/raise_cli/cli/commands/session.py:264` — docstring containing "task-relevant" (false positive)

**Verdict:** All false positives. No hardcoded secrets.

### Tokens (`ghp_`, `ghs_`, `glpat-`, `Bearer`)
- No matches.

### Private Keys (`BEGIN PRIVATE`, `BEGIN RSA`)
- Matches only in `.venv/` (not tracked). No matches in project files.

### Tracked .env Files
- `.envrc` is tracked — it's a shell loader script that sources `.env` but contains no secrets itself. Acceptable.

---

## Task 2: Internal References Scan

**Status:** 2 fixes applied

### Internal Emails (`emilio@`, `@humansys.ai`)
- No matches in `src/`.
- Present in `SECURITY.md` and `pyproject.toml` (intentional contact info).

### Internal GitLab References
- No matches in `src/` or root `*.md` files.

### Hardcoded Absolute Paths
- **FIXED:** `src/rai_pro/providers/jira/properties.py:48` — docstring example had `/home/emilio/Code/raise-commons` -> changed to `/path/to/project`
- **FIXED:** `src/rai_pro/providers/jira/models.py:81` — docstring example had `/home/emilio/Code/raise-commons` -> changed to `/path/to/project`

---

## Task 3: .gitignore Review

**Status:** Minor addition

### Coverage Check
| Pattern | Present? | Line |
|---------|----------|------|
| `.raise/rai/personal/` | Yes | 23 |
| `.env`, `.env.*` | Yes | 93-94 |
| `__pycache__/`, `*.py[cod]` | Yes | 39-40 |
| `dist/`, `build/`, `*.egg-info/` | Yes | 43-44, 55 |
| `htmlcov/`, `.coverage` | Yes | 66, 69 |
| `.claude/` local files | Yes | 5-8 |
| `.specstory/`, `.cursor/` | Yes | 1-2 |
| `.idea/` | **Added** | 4 |
| `.vscode/` | **Added** | 5 |
| `.pyright/` | Yes | 82 |
| `*.pem`, `*.key` | Yes | 95-96 |
| `.pypirc`, `credentials.json` | Yes | 97-98 |
| `.mcp.json` | Yes | 90 |

---

## Task 4: Distribution Content Audit

**Status:** CLEAN

### sdist (`raise_cli-2.2.0.tar.gz`)
Non-source files included (all expected):
- `.gitignore`, `LICENSE`, `NOTICE`, `PKG-INFO`, `pyproject.toml`, `README.md`

No internal directories (`work/`, `dev/`, `.raise/`, `.github/`, `.claude/`) present.

### wheel (`raise_cli-2.2.0-py3-none-any.whl`)
- 265 files total, all under `raise_cli/` package namespace
- No internal/private directories leaked
- `rai_base/framework/` and `rai_base/governance/` are intentional template files shipped with the CLI

**Verdict:** Distribution packages are clean and properly scoped.
