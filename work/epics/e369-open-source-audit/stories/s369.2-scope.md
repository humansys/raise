# S369.2: Package Metadata & Deps — Scope

## In Scope

- Add `[project.urls]` to pyproject.toml (Homepage, Docs, Repository, Changelog)
- Remove dead `tomli` dependency (guard `python_version < "3.11"` but project requires >= 3.12)
- Add Python 3.13 classifier to pyproject.toml
- Run dependency vulnerability scan (pip-audit or safety)

## Out of Scope

- Changing minimum Python version
- Adding new dependencies
- Modifying CI/CD (covered by S369.3)
- Refactoring code that imports tomli (verify no runtime usage first)

## Done Criteria

- PyPI metadata includes Homepage, Docs, Repository, Changelog URLs
- No dead dependencies in requires list
- Python 3.13 classifier present in classifiers
- No known critical/high vulnerabilities in dependencies
