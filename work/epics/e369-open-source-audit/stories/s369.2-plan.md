# S369.2 -- Package Metadata & Deps: Plan

## Tasks

### T1: Update pyproject.toml metadata

**Goal:** Fix project URLs, remove dead dependency, add missing classifier.

**Steps:**
1. Verify tomli is not imported at runtime: `grep -rn "import tomli" src/`
2. Add `[project.urls]` section:
   - Homepage = "https://raiseframework.ai"
   - Documentation = "https://docs.raiseframework.ai"
   - Repository = "https://github.com/humansys-ai/raise-commons"
   - Changelog = "https://github.com/humansys-ai/raise-commons/blob/main/CHANGELOG.md"
3. Remove `tomli` dependency line (the one with `python_version < '3.11'` marker)
4. Add `"Programming Language :: Python :: 3.13"` classifier

**Verification:**
- `uv lock` succeeds (lock file stays consistent)
- `grep -A4 "\[project.urls\]" pyproject.toml` shows URLs

**Commit:** `fix(s369.2): add project URLs, remove dead dep, add 3.13 classifier`

---

### T2: Dependency vulnerability scan

**Goal:** Audit dependencies for known vulnerabilities.

**Steps:**
1. Run pip-audit: `uv run pip-audit`
2. Document results
3. If critical/high vulns found, either fix or document as accepted risk

**Verification:**
- Scan completes, results documented

**Commit:** `chore(s369.2): dependency vulnerability scan results` (if changes needed)

---

## Execution Order

T1 -> T2 (sequential)

## Notes

- No TDD -- configuration/metadata changes only
- XS story, 2 tasks, purely declarative edits + audit
