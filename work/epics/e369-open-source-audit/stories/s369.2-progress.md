# S369.2 Progress — Package Metadata and Deps

## Task 1: Update pyproject.toml metadata

- **Status:** Done
- **Commit:** `fix(s369.2): add project URLs, remove dead dep, add 3.13 classifier`
- Changes:
  - Added `[project.urls]` section (Homepage, Documentation, Repository, Changelog)
  - Removed dead `tomli>=2.0.1; python_version < '3.11'` dependency (project requires >=3.12, so the guard never activates)
  - Added `Programming Language :: Python :: 3.13` classifier
  - `uv lock` resolved cleanly (100 packages)
- Note: `src/raise_cli/config/settings.py` still has the `import tomli as tomllib` fallback in an unreachable `else` branch. Not in scope for this story but could be cleaned up later.

## Task 2: Dependency vulnerability scan

- **Status:** Done
- **Tool:** `uv run --with pip-audit pip-audit`
- **Result:** No known vulnerabilities found
- Skipped packages (expected — not on PyPI):
  - raise-cli 2.2.0
  - raise-core 2.1.0
  - raise-server 0.1.0
- No dependency changes needed
