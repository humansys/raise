# S680.5: Implementation Plan

## Tasks

### T1: Execute core smoke tests (V1-V3)

**Goal:** Run the 3 original verification tests and capture evidence.

| Step | Action | Verification |
|------|--------|-------------|
| 1.1 | Run `uv run rai session start --project . --context` | Completes without error, context bundle printed |
| 1.2 | Run `uv run rai session close --summary "smoke test" --type maintenance --project .` | Completes without error |
| 1.3 | Run `uv run rai backlog search "RAISE-680" -a jira` | Returns results, no adapter errors |
| 1.4 | Create temp venv: `uv venv /tmp/raise-smoke` | Venv created |
| 1.5 | Install package: `uv pip install -e . --python /tmp/raise-smoke/bin/python` | Install succeeds |
| 1.6 | Run `/tmp/raise-smoke/bin/rai --help` | Help output displays |
| 1.7 | Clean up temp venv | Removed |

**Done when:** V1, V2, V3 output captured with pass/fail status.

### T2: Execute expanded verifications (V4-V6)

**Goal:** Run changelog-driven verifications and capture evidence.

| Step | Action | Verification |
|------|--------|-------------|
| 2.1 | Run `uv run rai doctor` | Runs without crash, shows ACLI check status |
| 2.2 | Run `uv run rai pattern add "smoke test pattern" -c "test" -t process` | Pattern added to project scope (not personal) |
| 2.3 | Check `uv run rai --help` for extension commands | Extensions listed or no crash |

**Done when:** V4, V5, V6 output captured with pass/fail status.

### T3: Document results & create report

**Goal:** Consolidate all evidence into results report.

| Step | Action | Verification |
|------|--------|-------------|
| 3.1 | Create `s680.5-results.md` with pass/fail table | All 6 verifications documented |
| 3.2 | Include evidence snippets for each test | Output captured per test |
| 3.3 | Assess failures with severity (blocker/major/minor) | Severity documented if any failures |
| 3.4 | Write overall release readiness verdict | Verdict present |

**Done when:** Results file committed with complete evidence.

## Task Order

```
T1 → T2 → T3
```

Sequential — results from T1/T2 feed into T3 report.

## Constraints

- **Document only** — do not fix bugs found; log them as known issues
- All commands run with `uv run` prefix from project root
- Evidence = actual command output snippets (not summaries)
