---
story_id: "S680.4"
title: "Quality gates & security — Results"
executed: "2026-03-23"
verdict: "RELEASE READY (advisory issues only)"
---

# S680.4 Results: Quality gates & security

## T1: Test suite + type check + lint

### pytest — PASS
- **3978 passed**, 9 skipped, 22 deselected (108.72s)
- Coverage: 92%
- No failures.

### pyright — PASS
- 0 errors, 0 warnings, 0 informations

### ruff check — FAIL (advisory)
- **12 errors** across non-core files:
  - 4x D103 (missing docstrings) in `packages/raise-server/alembic/versions/`
  - 1x S104 (bind 0.0.0.0) in `packages/raise-server/src/raise_server/config.py`
  - 1x D415 (docstring punctuation) in `scripts/sync-skills.py`
  - 1x C901 (complexity) in `scripts/sync-skills.py`
  - 5x S101 (assert in non-test) in `work/.legacy-epics/` manual test script
- **Assessment:** All errors are in auxiliary files (alembic migrations, legacy test scripts, server config). No errors in core `raise_cli` or `raise_core` packages. Non-blocking for release.

---

## T2: Framework gates + skill validation

### rai gate check — PARTIAL
- `--all` hangs (pytest gate times out in CLI runner context)
- Individual gate results:
  - **gate-types:** PASS — No type errors
  - **gate-format:** FAIL — 9 files need reformatting (server package, scripts, legacy test)
  - **gate-lint:** FAIL — Uses invalid `ruff check --check` flag (gate config bug)
  - **gate-tests:** Not tested individually (would hang like `--all`)
  - **gate-coverage:** Not tested individually (timed out waiting for tests)
- **Assessment:** Types pass. Format and lint failures are in non-core files. gate-lint has a config bug (passes `--check` to ruff which is not a valid flag). Advisory, not blocking.

### rai skill validate — FAIL (advisory)
- **35 skills checked: 2 errors, 11 warnings**
- Errors (2):
  - `rai-doctor`: Missing required sections "Mastery Levels" and "References"
- Warnings (11):
  - Body length exceeds 150-line target in: rai-bugfix (225), rai-code-audit (255), rai-epic-close (185), rai-epic-docs (300), rai-epic-run (180), rai-quality-review (172), rai-session-close (155), rai-story-close (160), rai-story-design (175), rai-story-review (152), rai-story-run (270)
- **Assessment:** rai-doctor is a newer skill and needs section fixes. Warnings are advisory (line count targets). Non-blocking for release.

---

## T3: Security audit

### pip-audit — PASS
- **No known vulnerabilities found**
- 1 skip: `raise-pro` (private package, not on PyPI — expected)

---

## T4: Knowledge graph build

### rai graph build — PASS
- 46,229 total relationships built
- 260 nodes changed (21 added, 239 modified), 3 modules affected
- Saved to `.raise/rai/memory/index.json`

---

## Summary

| Task | Tool | Result | Blocking? |
|------|------|--------|-----------|
| T1 | pytest | PASS (3978 passed, 92% cov) | — |
| T1 | pyright | PASS (0 errors) | — |
| T1 | ruff | 12 errors (auxiliary files only) | No |
| T2 | gate check | types PASS, format/lint FAIL (non-core) | No |
| T2 | skill validate | 2 errors (rai-doctor), 11 warnings | No |
| T3 | pip-audit | PASS (0 CVEs) | — |
| T4 | graph build | PASS (46k relationships) | — |

## Verdict: RELEASE READY

All core quality signals are green:
- Full test suite passes with 92% coverage
- Zero type errors (pyright strict)
- Zero known CVEs in dependencies
- Knowledge graph builds successfully

Advisory issues to address post-release:
1. Ruff lint errors in auxiliary files (server migrations, scripts, legacy tests)
2. Format violations in 9 non-core files
3. `rai-doctor` skill missing required sections
4. `gate-lint` config bug (invalid `--check` flag passed to ruff)
