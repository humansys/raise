---
story_id: "S680.3"
epic_id: "E680"
jira_key: "RAISE-684"
title: "User & Dev Docs — Plan (Community Only)"
created: "2026-03-23"
---

# S680.3: Implementation Plan

## Tasks

### T1: Update session docs

- **File:** `docs/cli/session.mdx`
- **Action:** Add "Session Identity (v2.3.0)" section covering:
  - New ID format `S-{prefix}-{YYMMDD}-{HHMM}` — deterministic, timestamp-based
  - Developer prefix registry at `.raise/rai/prefixes.yaml` — auto-register, collision detection
  - Per-project session data in `.raise/rai/personal/` (gitignored)
  - Privacy model: only `prefixes.yaml` committed; session data local by default
- **Breaking change callout:**
  - Session data path moved from `~/.rai/developer.yaml` to `.raise/rai/personal/`
  - Old `SES-NNN` format readable but new sessions use new format
  - No automatic migration
- **Source:** `work/epics/e680-release-v2.3.0-prep/stories/s680.1-e654-docs.md`
- **ACs:** AC1, AC4, AC5

### T2: Update extending guide

- **File:** `docs/guides/extending.mdx`
- **Action:** Add "CLI Extensions" section (after Lifecycle Hooks, before Validation) covering:
  - Entry point group: `rai.cli.commands`
  - Convention: register a `typer.Typer` app in `pyproject.toml`
  - Example `pyproject.toml` entry
  - Protection: built-in command name collision detection, duplicate extension detection
  - Validation: extensions appear in `rai --help` when installed
- **Source:** `src/raise_cli/cli/extensions.py` (read for accurate entry point mechanism)
- **ACs:** AC2

### T3: Update pattern docs

- **File:** `docs/cli/pattern.mdx`
- **Action:**
  - Modify `--scope` default from `personal` to `project` in flags table
  - Add breaking change callout with before/after example:
    - Before: `rai pattern add "..." → personal scope`
    - After: `rai pattern add "..." → project scope`
    - Migration: add `--scope personal` explicitly for old behavior
- **ACs:** AC3, AC4, AC5

### T4: Verification

- Review all 3 updated files for consistency
- Verify no Pro feature references leaked in (AC7)
- Verify breaking changes have clear callouts (AC4)
- Verify existing doc structure preserved (AC8)

## Task Order

```
T1 → T2 → T3 → T4
```

Sequential execution. T1–T3 are independent but T4 verifies all three.

## Estimates

| Task | Size | Notes |
|------|------|-------|
| T1 | XS | Read source doc, add section + callout |
| T2 | XS | Read source code, add section |
| T3 | XS | Modify table + add callout |
| T4 | XS | Review pass |

**Total:** S (4 XS tasks)
