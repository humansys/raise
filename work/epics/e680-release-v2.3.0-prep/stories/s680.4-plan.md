---
story_id: "S680.4"
title: "Quality gates & security"
---

# S680.4 Plan: Quality gates & security

## Tasks

### T1: Test suite + type check + lint

**Commands:**
```bash
uv run pytest --tb=short -q
uv run pyright
uv run ruff check .
```

**Success criteria:** All three pass with 0 errors/failures.

**If fails:** Fix the failing tests, type errors, or lint violations. Re-run until clean. These are blockers — do not proceed with failures.

---

### T2: Framework gates + skill validation

**Commands:**
```bash
uv run rai gate check --all
uv run rai skill validate
```

**Success criteria:** All gates green. All 19 skills valid.

**If fails:** Investigate gate failure details. Fix structural issues. Advisory-only gates can be noted and deferred.

---

### T3: Security audit

**Commands:**
```bash
uv run pip-audit
```

**Success criteria:** 0 known CVEs in dependencies.

**If fails:** Check if CVE has a fix available. Update dependency if patch exists. Document unfixed CVEs with risk assessment for release decision.

---

### T4: Knowledge graph build

**Commands:**
```bash
uv run rai graph build
```

**Success criteria:** Graph builds without error. Output is current.

**If fails:** Check for missing modules or broken references. Fix discovery metadata if needed.

---

## Execution Order

T1 > T2 > T3 > T4 (sequential — each builds confidence for the next).

## Done When

All 7 tools pass. Results captured in story file checkboxes and available as evidence for S680.5.
