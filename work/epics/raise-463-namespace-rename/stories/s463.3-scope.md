# S463.3: Rename raise-cli — Scope

## In Scope

- Rename dir `src/rai_cli/` → `src/raise_cli/`
- Update Python namespace rai_cli → raise_cli (197 src files)
- Update all imports in tests/ (~364 files)
- Update 329 mock paths (`patch("rai_cli.x")` → `patch("raise_cli.x")`)
- Update conftest.py (root + integration)
- Update importlib.resources refs (`files("rai_cli.rai_base")`)
- Update root pyproject.toml (name, entry points, scripts, wheel, tool paths)
- CLI command stays `rai`

## Out of Scope

- Entry point group names (`rai.*` stays)
- CI/CD workflows (S463.4)
- Version bump (S463.5)

## Done Criteria

1. `uv run rai --version` works
2. Zero `rai_cli` imports in src/ and tests/ .py files
3. All tests pass (3699+)
4. importlib.resources resolves `raise_cli.rai_base` correctly
