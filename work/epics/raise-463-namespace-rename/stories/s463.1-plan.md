# S463.1: Rename raise-core — Plan

## Tasks

### T1: Directory rename (→ checkpoint C1)
1. `git mv packages/rai-core packages/raise-core`
2. `git mv packages/raise-core/src/rai_core packages/raise-core/src/raise_core`
3. Commit C1

**Validation:** `ls packages/raise-core/src/raise_core/__init__.py` exists

### T2: Internal imports + pyproject (→ checkpoint C2)
1. `sed -i 's/rai_core/raise_core/g'` on all .py in `packages/raise-core/src/raise_core/`
2. Update `packages/raise-core/pyproject.toml`: name → raise-core, wheel → raise_core
3. Syntax check: `python -c "import ast; ast.parse(open(f).read())"` on each file
4. Commit C2

**Validation:** `grep -r 'rai_core' packages/raise-core/` returns zero hits

### T3: External imports in source (→ checkpoint C3)
1. `sed -i 's/from rai_core/from raise_core/g'` on `src/rai_cli/**/*.py`
2. `sed -i 's/import rai_core/import raise_core/g'` on `src/rai_cli/**/*.py`
3. Same sed on `tests/**/*.py`
4. Update comments/docstrings referencing `rai_core` namespace (not historical docs)
5. `git diff` review for over-replacement
6. Commit C3

**Validation:** `grep -rn 'rai_core' src/ tests/ --include='*.py'` returns zero hits

### T4: Workspace config + verify (→ checkpoint C4)
1. Update root `pyproject.toml`:
   - workspace source: `rai-core` → `raise-core`
   - dependency: `rai-core==2.1.0` → `raise-core==2.1.0`
   - tool paths: `packages/rai-core/src` → `packages/raise-core/src`
   - entry point: `rai_core.graph.backends` → `raise_core.graph.backends`
   - coverage: `rai_core` → `raise_core`
2. `uv cache clean && uv lock`
3. Smoke: `uv run python -c "from raise_core.graph.models import GraphNode; print('OK')"`
4. Tests: `uv run pytest tests/core/ -q` then `uv run pytest -q`
5. Type check: `uv run pyright packages/raise-core/`
6. Commit C4

**Validation:** All tests pass, smoke import works, zero `rai_core` in src/tests .py files

## Done

- [ ] T1 complete (C1 committed)
- [ ] T2 complete (C2 committed)
- [ ] T3 complete (C3 committed)
- [ ] T4 complete (C4 committed, tests green)
