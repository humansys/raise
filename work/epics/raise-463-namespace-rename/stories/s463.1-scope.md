# S463.1: Rename raise-core -- Scope

## In Scope

1. Rename directory `packages/rai-core/` to `packages/raise-core/`
2. Rename source directory `packages/rai-core/src/rai_core/` to `packages/raise-core/src/raise_core/`
3. Update `packages/raise-core/pyproject.toml` (project name, wheel target)
4. Update all internal imports within raise_core (11 .py files)
5. Update all imports FROM rai_core in `src/rai_cli/` (~49 files)
6. Update all imports FROM rai_core in `tests/` (~38 files)
7. Update root `pyproject.toml` workspace member and `uv.sources` reference
8. Run tests to verify no breakage

## Out of Scope

- Renaming rai-cli or rai-server packages (S463.3, S463.2)
- Changing the `rai` CLI command name
- CI/CD workflow updates (S463.4)
- PyPI publishing (S463.5)
- Entry point group renaming (`rai.*` stays)
- Structural refactoring of any modules

## Done Criteria

1. Directory `packages/raise-core/` exists; `packages/rai-core/` does not
2. Source namespace is `raise_core` (not `rai_core`)
3. `pyproject.toml` inside raise-core has `name = "raise-core"`
4. No `from rai_core` or `import rai_core` in any source or test file
5. Root `pyproject.toml` references `raise-core` in workspace members and uv.sources
6. `from raise_core.graph.models import GraphNode` works at runtime
7. All tests pass

## Risks

- **Mock paths in tests** may reference `rai_core` as string literals -- grep must cover string contents, not just import lines.
- **uv workspace cache** may need clearing after rename (`uv cache clean`).
