# S463.2: Rename raise-server — Scope

## In Scope

- Rename dir `packages/rai-server/` → `packages/raise-server/`
- Rename namespace `rai_server` → `raise_server` (25 src files)
- Update `packages/raise-server/pyproject.toml` (name, deps, wheel)
- Update internal imports (rai_core already → raise_core from S463.1)
- Update Dockerfile paths and uvicorn command
- Update docker-compose.yml paths
- Update alembic/env.py import
- Update tests (17 files)
- Update root pyproject.toml refs

## Out of Scope

- rai_cli rename (S463.3)
- CI/CD (S463.4)
- Docker image build verification (no Docker in CI currently)

## Done Criteria

1. `from raise_server.app import create_app` works
2. Zero `rai_server` imports in packages/raise-server/ and tests/
3. Dockerfile references raise_server/raise-server
4. All tests pass (3699+)
