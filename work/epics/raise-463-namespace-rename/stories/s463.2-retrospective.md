# S463.2: Rename raise-server — Retrospective

## Summary

Renamed rai-server → raise-server (package, namespace, imports, Dockerfile,
docker-compose, alembic, tests, README).

## What was delivered

- 25 src files + 17 test files renamed
- Dockerfile + docker-compose updated
- alembic/env.py import updated
- Root pyproject.toml updated
- 3699 tests pass

## What went well

- Checkpoint strategy continues to work smoothly
- Server package has zero cross-imports in rai_cli — clean boundary

## Learnings

- raise-server is a dev dependency (optional extra), smoke test needs
  `uv run --extra dev` not just `uv run`. Caught by testing.
