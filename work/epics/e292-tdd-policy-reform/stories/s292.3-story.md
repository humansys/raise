---
story_id: "S292.3"
epic_id: "RAISE-292"
title: "Cleanup: hooks module (2.7x)"
size: XS
status: in_progress
created: "2026-02-26"
---

# S292.3 — Cleanup: hooks module (2.7x)

## User Story

**As** a developer maintaining the hooks module,
**I want** the test suite to contain only tests that assert observable behavior,
**so that** the test-to-source ratio reflects real coverage value, not coverage theater.

## Acceptance Criteria

**Scenario: Muda identified and removed**
- Given the hooks test suite has a 2.7x test-to-source ratio
- When each test is evaluated against the 4 heuristics (constant assertions, mock-impl tests, magic counts, tautological tests)
- Then every deleted test fails at least one heuristic — deletion is justified by behavior, not by line count

**Scenario: Ratio is diagnostic, not a gate**
- Given the cleanup is complete
- When the ratio is measured
- Then it is recorded as an outcome — if ratio > 2.0x, that signals remaining muda to investigate, not a failed story
- And if ratio > 2.0x after honest heuristic application, the module's behavior justifies the higher ratio

**Scenario: Suite still green**
- Given tests are deleted
- When `pytest tests/test_hooks/` runs
- Then all remaining tests pass

## Notes

- XS story — stays on epic branch (no dedicated story branch)
- Skip design phase — mechanical cleanup, no integration decisions
- Pattern: read tests → identify muda → delete → verify green
