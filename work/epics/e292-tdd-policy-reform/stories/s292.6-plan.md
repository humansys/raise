---
story_id: "S292.6"
epic_id: "RAISE-292"
title: "Cleanup: telemetry module"
created: "2026-02-26"
---

# Plan: S292.6 — Cleanup telemetry module

## Overview

- Source: 544 lines (telemetry/ + writer/)
- Tests before: 968 lines (2 files)
- Ratio before: 968/544 = 1.78x
- Target: delete muda by heuristic (PAT-E-499)

## Muda Catalog

### test_schemas.py — 9 tests to delete

Most muda is in Pydantic model instantiation tests that assert only the values you
just passed (tautological-constructor), adding zero value over the validation tests
and serialization tests already in each class.

| # | Test | Lines | Heuristic | Rationale |
|---|------|-------|-----------|-----------|
| 1 | `TestSkillEvent.test_create_complete_event_with_duration` | 52-62 | tautological-constructor | Only asserts `event == "complete"` and `duration_sec == 1800` — both values you passed in |
| 2 | `TestSkillEvent.test_create_abandon_event` | 64-71 | tautological-constructor | Only asserts `event == "abandon"` and `duration_sec == 600` — both passed in |
| 3 | `TestSessionEvent.test_create_abandoned_event` | 122-128 | tautological-constructor | Only asserts `outcome == "abandoned"` — value you passed |
| 4 | `TestCalibrationEvent.test_velocity_less_than_one` | 179-190 | constant-assertion | Creates with velocity=0.67, asserts `velocity < 1.0` — 0.67 < 1.0 is always True |
| 5 | `TestErrorEvent.test_create_non_recoverable_error` | 230-240 | tautological-constructor | Only asserts `recoverable is False` — value you passed |
| 6 | `TestWorkLifecycle.test_create_epic_start_event` | 309-322 | tautological-constructor | Asserts type/work_type/work_id — all tautological; type default already covered by test_create_story_start_event |
| 7 | `TestWorkLifecycle.test_create_complete_event` | 323-334 | tautological-constructor | Only asserts `event == "complete"` and `phase == "review"` — both passed in |
| 8 | `TestWorkLifecycle.test_create_blocked_event_with_blocker` | 336-348 | tautological-constructor | Only asserts `event == "blocked"` and `blocker == "unclear requirements"` — both passed in |
| 9 | `TestWorkLifecycle.test_create_abandoned_event` | 350-360 | tautological-constructor | Only asserts `event.event == "abandoned"` — value you passed |

### test_writer.py — 1 test to delete

| # | Test | Lines | Heuristic | Rationale |
|---|------|-------|-----------|-----------|
| 10 | `TestEmitErrorEvent.test_emit_non_recoverable_error` | 387-401 | parametrize-duplicate | Identical pattern to test_emit_recoverable_error; only difference is True→False for recoverable field |

**Kept (explicit):**
- All validation tests (invalid_event_type, invalid_outcome, etc.) — test constraints
- All serialization tests — test JSON round-trip
- `test_create_start_event`, `test_create_success_event`, etc. — kept for default assertions (type discriminator, duration_sec=None, stories=[], blocker=None)
- `test_velocity_less_than_one` in TestSignalUnion — distinct behavior (discriminated union)
- `test_epic_review_phase`, `test_epic_implement_phase` — test that specific phase strings are valid

## Tasks

### T1: Delete 9 muda tests from test_schemas.py

Delete tests listed in catalog items 1-9.

Verify: `pytest tests/telemetry/test_schemas.py -q`
Commit.

### T2: Delete 1 muda test from test_writer.py

Delete `TestEmitErrorEvent.test_emit_non_recoverable_error` (lines 387-401).

Verify: `pytest tests/telemetry/ -q`
Commit.

### T3: Update epic scope and close

Update scope.md progress table.
Commit.

## Execution Order

T1 → T2 → T3 (sequential)

## Duration Tracking

| Task | Estimate | Actual | Status |
|------|----------|--------|--------|
| T1   | 5 min    | —      | pending |
| T2   | 2 min    | —      | pending |
| T3   | 2 min    | —      | pending |
