---
story_id: "S292.9"
epic_ref: "RAISE-292"
size: "XS"
date: "2026-02-26"
---

# Plan: S292.9 — Cleanup context/test_models.py

## Overview

Pure deletion story. 1 file, 7 test methods removed. No TDD cycle needed.

Constraint: file has pre-existing ImportError (RAISE-294). Verification uses
`ast.parse` for syntax validity instead of `pytest` on this file.

## Tasks

### T1: Delete 7 tautological-constructor tests

**File:** `tests/context/test_models.py`

**Delete:**
- `TestConceptNode.test_create_pattern_node`
- `TestConceptNode.test_create_module_node`
- `TestConceptEdge.test_create_learned_from_edge`
- `TestConceptEdge.test_create_depends_on_edge`
- `TestConceptEdge.test_all_edge_types_valid`
- `TestConceptEdge.test_custom_weight`
- `TestConceptEdge.test_any_edge_type_accepted`

**Verification:**
```bash
python -c "import ast; ast.parse(open('tests/context/test_models.py').read()); print('syntax OK')"
pytest --ignore=tests/context/test_builder.py --ignore=tests/context/test_builder_release.py --ignore=tests/context/test_models.py -q 2>&1 | tail -3
```

**Size:** XS | **Depends on:** — | **Commit:** yes

## Execution Order

T1 → verify syntax → verify no regression → commit

## Risks

| Risk | Mitigation |
|------|------------|
| Accidental deletion of a keeper test | Cross-check design.md "Tests Kept" list |
| Blank lines broken post-deletion | Run ruff check after (lesson from S292.8) |

## Duration Tracking

| Task | Estimated | Actual |
|------|-----------|--------|
| T1 | 3 min | — |
