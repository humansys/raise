---
story_id: "S292.2"
epic_id: "RAISE-292"
title: "Skill: update /rai-story-implement"
size: S
status: in_progress
created: "2026-02-26"
---

# S292.2 — Skill: Update /rai-story-implement

## User Story

**As** an AI agent running `/rai-story-implement`,
**I want** explicit test quality heuristics and anti-patterns in the skill,
**so that** I write tests that catch real bugs instead of tests that satisfy a coverage target.

## Acceptance Criteria

**Scenario: Agent reads Step 2 (Execute Task)**
- Given the agent reads the TDD cycle instructions
- When it looks for guidance on what makes a good test
- Then it sees explicit anti-patterns to avoid (constant assertions, mock-impl tests, magic counts)
- And it sees the quality heuristic: "each test asserts observable behavior"

**Scenario: Agent reads Quality Checklist**
- Given the agent reads the Quality Checklist
- When it checks the TDD item
- Then it references test quality, not coverage percentage

**Scenario: Coherence with S292.1**
- Given guardrails-stack.md and /rai-story-implement SKILL.md
- When both are read by an agent
- Then they give consistent guidance on testing quality

## Notes

- Single file: `.claude/skills/rai-story-implement/SKILL.md`
- Also needs update in `.agent/skills/rai-story-implement/SKILL.md` (multi-agent distribution)
- The skill already has good TDD RED-GREEN-REFACTOR structure — we add quality guidance within Step 2
- No changes to Step 1 (load plan), Step 3 (verify), Step 4 (commit), Step 5 (iterate)
