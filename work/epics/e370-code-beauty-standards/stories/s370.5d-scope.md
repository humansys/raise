# S370.5d Scope: Bundle Split

## In Scope

- Split session/bundle.py (821 lines) into 3 modules:
  - `bundle_formatters.py` — all `_format_*` functions
  - `bundle_data.py` — data fetching (graph queries, live backlog status, session index)
  - `bundle.py` — assembly orchestration (assemble_orientation, assemble_context_bundle, assemble_sections)
- Preserve all public API (assemble_context_bundle, assemble_orientation, assemble_sections, SECTION_REGISTRY, etc.)
- Update imports in callers

## Out of Scope

- Changing bundle behavior or output format
- Adding new sections or features
- Refactoring callers beyond import updates
- Performance optimization

## Done Criteria

1. bundle.py reduced to assembly orchestration only
2. _format_* functions live in bundle_formatters.py
3. Data fetching functions live in bundle_data.py
4. All existing tests pass without modification (or minimal import updates)
5. pyright 0 errors, ruff clean
6. assemble_context_bundle() produces identical output
