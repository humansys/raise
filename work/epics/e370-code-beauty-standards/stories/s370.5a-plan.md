# S370.5a Plan: Quick Wins

**Size:** S | **Tasks:** 4

---

## T1: Add __init__.py re-exports (R1)

**Files:** `src/raise_cli/schemas/__init__.py`, `src/raise_cli/backlog/__init__.py`, `src/raise_cli/tier/__init__.py`, `src/raise_cli/engines/__init__.py`

**Steps:**
1. Read each `__init__.py` and the module files they should re-export
2. Add re-exports and `__all__` to each
3. Verify: `python -c "from raise_cli.schemas import JournalEntry"` etc.

**TDD:** Write import tests first (RED), then add re-exports (GREEN).
**Verification:** `uv run pytest --tb=short -q` + `uv run ruff check` + `uv run pyright`
**Commit:** `refactor(s370.5a): add __init__.py re-exports to schemas, backlog, tier, engines`

---

## T2: Add logger.debug to silent except Exception (R2)

**Files:** `src/raise_cli/config/settings.py`, `src/raise_cli/agents/copilot_plugin.py`

**Steps:**
1. Read each file, find the silent `except Exception` catch
2. Add `import logging` (if needed) and `logger.debug(...)` in the except block
3. Ensure exception variable is bound (`as e`)

**TDD:** Existing tests cover these paths. Verify no regressions.
**Verification:** `uv run pytest --tb=short -q` + `uv run ruff check`
**Commit:** `fix(s370.5a): add debug logging to silent except Exception catches`

---

## T3: Move graph formatters to output/formatters/ (R3)

**Files:** `src/raise_cli/cli/commands/graph.py` → `src/raise_cli/output/formatters/graph.py`

**Steps:**
1. Read `cli/commands/graph.py` to identify the 5 `_format_*` functions
2. Create `output/formatters/graph.py` with those functions
3. Update `cli/commands/graph.py` imports to use `output.formatters.graph`
4. Update `output/formatters/__init__.py` if needed

**TDD:** Existing graph CLI tests cover this. Run before and after to verify no regressions.
**Verification:** `uv run pytest tests/cli/commands/test_graph* --tb=short -q` + full suite
**Commit:** `refactor(s370.5a): move graph formatters to output/formatters/`

---

## T4: Remove _current_output_format global (R4)

**Files:** `src/raise_cli/cli/main.py`

**Steps:**
1. Read `cli/main.py`, find `_current_output_format` global and all references
2. Remove the global, ensure all access goes through `ctx.obj`
3. Check if any code outside main.py references this global

**TDD:** Existing CLI tests cover output format routing.
**Verification:** Full test suite + `uv run ruff check` + `uv run pyright`
**Commit:** `refactor(s370.5a): remove _current_output_format global, use ctx.obj`

---

## Execution Order

T1 → T2 → T3 → T4 (sequential, low risk, each independent)
