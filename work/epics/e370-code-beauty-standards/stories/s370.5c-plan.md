# S370.5c Plan: God Class Decomposition

## Overview

7 tasks extracting method clusters from builder.py into focused modules. Order: extractors first (no loader dependencies), then loaders (independent), then builder cleanup. Each task creates one module and updates builder imports.

## Tasks

### T1: Extract relationships.py (extractors)

**Files:** `src/raise_cli/context/extractors/relationships.py` (NEW), `src/raise_cli/context/builder.py` (modify)

**Move:** `infer_relationships`, `_infer_learned_from`, `_infer_part_of`, `_infer_skill_edges`, `_infer_depends_on`, `_infer_release_part_of`, `_infer_keyword_relationships`, `_extract_keywords` (~316 lines)

**Signature:** `infer_relationships(nodes, project_root) -> list[GraphEdge]` — receives nodes list and project_root, returns edges.

**TDD:** Existing tests suffice. Run `uv run pytest tests/context/ -x -q` after.

**Size:** M | **Deps:** None

---

### T2: Extract structure.py (extractors)

**Files:** `src/raise_cli/context/extractors/structure.py` (NEW), `src/raise_cli/context/builder.py` (modify)

**Move:** `_extract_bounded_contexts`, `_extract_layers`, `_extract_constraints` (~247 lines)

**Signature:** Each becomes a module-level function receiving `(all_nodes, node_by_id)`.

**TDD:** Existing tests. `uv run pytest tests/context/ -x -q`

**Size:** M | **Deps:** None

---

### T3: Extract memory.py (loaders)

**Files:** `src/raise_cli/context/loaders/__init__.py` (NEW), `src/raise_cli/context/loaders/memory.py` (NEW), `src/raise_cli/context/builder.py` (modify)

**Move:** `load_memory`, `_load_memory_from_dir`, `_deduplicate_by_precedence`, `_load_jsonl`, `_memory_record_to_node` (~214 lines)

**Signature:** `load_memory(project_root) -> list[GraphNode]`

**TDD:** Existing tests. `uv run pytest tests/context/ -x -q`

**Size:** M | **Deps:** None

---

### T4: Extract architecture.py (loaders)

**Files:** `src/raise_cli/context/loaders/architecture.py` (NEW), `src/raise_cli/context/builder.py` (modify)

**Move:** `load_architecture`, `_parse_architecture_doc`, `_parse_module_doc`, `_parse_architecture_context`, `_parse_architecture_design`, `_parse_architecture_domain_model` (~217 lines)

**Signature:** `load_architecture(project_root) -> list[GraphNode]`

**TDD:** Existing tests. `uv run pytest tests/context/ -x -q`

**Size:** M | **Deps:** None

---

### T5: Extract identity.py (loaders)

**Files:** `src/raise_cli/context/loaders/identity.py` (NEW), `src/raise_cli/context/builder.py` (modify)

**Move:** `load_identity`, `_extract_identity_values`, `_extract_identity_boundaries` (~180 lines)

**Signature:** `load_identity(project_root, ide_config) -> list[GraphNode]`

**TDD:** Existing tests. `uv run pytest tests/context/ -x -q`

**Size:** S | **Deps:** None

---

### T6: Extract components.py and artifacts.py (loaders)

**Files:** `src/raise_cli/context/loaders/components.py` (NEW), `src/raise_cli/context/loaders/artifacts.py` (NEW), `src/raise_cli/context/builder.py` (modify)

**Move:** `load_components` (~37 lines), `load_artifacts` (~46 lines)

**Signature:** `load_components(project_root) -> list[GraphNode]`, `load_artifacts(project_root) -> list[GraphNode]`

**TDD:** Existing tests. `uv run pytest tests/context/ -x -q`

**Size:** S | **Deps:** None

---

### T7: Final cleanup + integration verification

**Files:** `src/raise_cli/context/builder.py` (final cleanup)

- Remove any remaining dead imports
- Verify builder.py is under 200 lines
- Run full test suite, pyright, ruff

**Verification:**
```bash
uv run pytest -x -q
uv run pyright src/raise_cli/context/
uv run ruff check src/raise_cli/context/
```

**Size:** XS | **Deps:** T1-T6

---

## Execution Order

```
T1 (relationships) ──┐
T2 (structure) ──────┤── extractors (independent)
                      │
T3 (memory) ─────────┤
T4 (architecture) ───┤── loaders (independent)
T5 (identity) ───────┤
T6 (components+art) ─┘
                      │
T7 (cleanup) ────────── final verification
```

T1-T6 are all independent (each moves a different method cluster). Sequential execution to keep diffs manageable and tests green at each step.

## Risk Mitigation

| Risk | Mitigation |
| --- | --- |
| Methods use self.project_root / self.ide_config | Pass as explicit parameters |
| Methods call other methods being moved | Move entire cluster together |
| Import cycles between new modules | Each module imports from raise_core only, no cross-loader imports |
| _extract_bounded_contexts is 144 lines | Acceptable — inherent complexity, already has noqa: C901 |
