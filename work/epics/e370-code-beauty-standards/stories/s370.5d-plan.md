# S370.5d Plan: Bundle Split

**Story:** S370.5d | **Size:** M | **Date:** 2026-03-06

## Tasks

### T1: Extract formatters to bundle_formatters.py (S)

**Risk:** Low — mechanical move.

**Files:**
- Create: `src/raise_cli/session/bundle_formatters.py`
- Modify: `src/raise_cli/session/bundle.py` (remove moved functions, import from formatters)

**Move these functions:**
- `_format_developer_section`
- `_format_work_section`
- `_format_last_session`
- `_format_deadlines`
- `_format_governance_primes`
- `_format_progress`
- `_format_recent_sessions`
- `_format_narrative`
- `_format_next_session_prompt`
- `_format_primes`
- `_format_coaching`
- `_format_pending`
- `_format_manifest`

**Verify:** `uv run pytest tests/session/test_bundle.py -x` + `uv run pyright` + `uv run ruff check`

---

### T2: Extract data fetchers to bundle_data.py (S)

**Risk:** Low — mechanical move.

**Files:**
- Create: `src/raise_cli/session/bundle_data.py`
- Modify: `src/raise_cli/session/bundle.py` (remove moved functions, import from data)

**Move these:**
- `LiveBacklogStatus` model
- `_fetch_live_status`
- `_query_adapter`
- `GRAPH_REL_PATH`, `SESSIONS_INDEX_REL_PATH` constants
- `get_foundational_patterns`
- `get_always_on_primes`
- `_find_release_for_current_epic`

**Verify:** `uv run pytest tests/session/test_bundle.py -x` + `uv run pyright` + `uv run ruff check`

---

### T3: Update imports + full validation (S)

**Risk:** Low — import path updates.

**Files:**
- Modify: `tests/session/test_bundle.py` (update imports to new modules)
- Modify: `src/raise_cli/cli/commands/session.py` (if needed)
- Modify: `tests/integration/test_backlog_integration.py` (if needed)

**Verify:** `uv run pytest --tb=short` + `uv run pyright` + `uv run ruff check`

---

## Execution Order

```
T1 (formatters) → T2 (data) → T3 (imports + validation)
```

Sequential — each builds on the previous split.

## Risks

| Risk | Mitigation |
|------|------------|
| Circular imports between new modules | Formatters and data are independent; only bundle.py imports both |
| Test imports break | T3 updates all imports, re-exports from bundle.py if needed |
