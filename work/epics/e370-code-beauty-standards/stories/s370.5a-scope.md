# Scope: S370.5a — Quick Wins

## In Scope

- R1: Add `__init__.py` re-exports to schemas/, backlog/, tier/, engines/
- R2: Add `logger.debug()` to silent `except Exception` in config/settings.py and agents/copilot_plugin.py
- R3: Move 5 `_format_*` functions from cli/commands/graph.py to output/formatters/graph.py
- R4: Remove `_current_output_format` global from cli/main.py, route through ctx.obj

## Out of Scope

- Any function decomposition (that's S370.5b)
- Any architectural refactoring (S370.5c-e)
- New tests beyond what's needed to verify the moves

## Done Criteria

- [ ] `from raise_cli.schemas import JournalEntry` works
- [ ] `from raise_cli.backlog import SyncResult` works
- [ ] `from raise_cli.tier import TierContext` works
- [ ] Silent `except Exception` catches now log at debug level
- [ ] No `_format_*` functions remain in cli/commands/graph.py
- [ ] No `_current_output_format` global in cli/main.py
- [ ] All 3717+ tests pass
- [ ] Ruff + Pyright clean
