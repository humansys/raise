# S370.5d Retrospective: Bundle Split

**Story:** S370.5d | **Size:** M | **Date:** 2026-03-06
**Jira:** RAISE-489

## Summary

Split `session/bundle.py` (821 lines) into 3 focused modules per R12 audit finding: formatters (350), data (202), assembly (323). Also resolved C901 complexity on `assemble_sections`.

## What Went Well

- **Clean mechanical split** — no behavior changes, all 73 bundle tests pass after import updates
- **No circular imports** — dependency chain is clean: data → formatters → assembly
- **C901 bonus** — extracted `_format_section` helper, removing the `noqa: C901` suppression
- **Mock patches survived** — `patch("raise_cli.session.bundle.get_always_on_primes")` still works because `bundle.py` re-imports the name

## What to Improve

- Could have done T1+T2+T3 as a single task — the split was mechanical with no risk points. Over-decomposed for the complexity level.

## Heutagogical Checkpoint

1. **Learned:** mock.patch targets import site, not definition site — patches survive module splits
2. **Process change:** For purely mechanical refactors (move functions, no behavior change), single-task plans are fine
3. **Framework:** No changes needed
4. **New capability:** Confident pattern for SRP module splits

## Patterns

- **PAT-E-026:** mock.patch targets import site — patches survive module splits

## Metrics

| Metric | Value |
|--------|-------|
| Tasks | 3 (planned), effectively 1 (mechanical) |
| Commits | 3 (scope + plan + implementation) |
| Tests passing | 3731 (73 bundle-specific) |
| bundle.py | 821 → 323 lines (-61%) |
| New modules | bundle_data.py (202), bundle_formatters.py (350) |
| C901 violations | 1 → 0 |
