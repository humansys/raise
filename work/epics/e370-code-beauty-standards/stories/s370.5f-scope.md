# S370.5f Scope: Migrate Loaders to YAML

## In Scope

- Create core.yaml as source of truth for identity (values, boundaries, principles)
- Migrate architecture docs from "YAML frontmatter in .md" to pure YAML
- Update load_identity() to read YAML instead of regex-parsing markdown
- Update load_architecture() to read pure YAML instead of frontmatter extraction
- Update rai init / rai-welcome to generate YAML artifacts
- Migrate existing files in the repo

## Out of Scope

- Changing Graph or GraphNode models (raise-core)
- Artifacts loader (already reads pure YAML — target pattern)
- Memory loader (reads JSONL — different format, already structured)
- Components loader (reads JSON — already structured)
- New graph features or sources
- Performance optimization

## Done Criteria

1. load_identity() reads core.yaml — no regex, no markdown parsing
2. load_architecture() reads pure YAML — no frontmatter extraction
3. rai init generates YAML for identity and architecture artifacts
4. All existing tests pass (or adapted for new format)
5. pyright 0 errors, ruff clean
6. Identity loader code reduced significantly (target: 180 -> ~30 lines)
