---
id: s370.5b
epic: e370
type: code
size: M
jira: RAISE-487
---
# S370.5b Scope: Extract Helpers

## In Scope

- **R4 (deferred from S370.5a):** Remove `_current_output_format` global in `cli/main.py` — requires error handler architecture changes
- **R5:** Extract CLI command functions into helper functions (top 8 by size) — `cli/init.py`, `session.py`, `signal.py`, `graph.py`, `discover.py`, `release.py`
- **R6:** Extract governance parser helpers for functions >100 lines — `governance/extractor.py`, `epic.py`, `backlog.py`
- **R7:** Split `discovery/scanner.py` into language-specific sub-modules
- **R8:** Add `GovernanceParser` Protocol, justify legacy `except Exception` clauses — `governance/`
- **R9:** Fix `hooks/emitter.py` `ThreadPoolExecutor` lifecycle
- **R10:** Extract `adapters/mcp_jira.py` format handlers, DRY flat/nested branching

## Out of Scope

- **S370.5c:** God class decomposition (`context/builder.py`) — separate story
- **S370.5d:** Bundle split (`session/bundle.py`) — separate story
- **S370.5e:** Onboarding cleanup (scaffold_skills, CLI reference) — separate story
- New features or CLI commands
- Performance optimization beyond what refactoring naturally provides

## Done Criteria

1. No function exceeds 100 lines in any targeted module
2. `_current_output_format` global removed from `cli/main.py`
3. `discovery/scanner.py` delegates to language-specific sub-modules
4. `GovernanceParser` Protocol defined and implemented
5. `ThreadPoolExecutor` in `hooks/emitter.py` has proper lifecycle management
6. `adapters/mcp_jira.py` flat/nested branching consolidated
7. All tests pass (`uv run pytest`)
8. Type checks pass (`uv run pyright`)
9. Linting passes (`uv run ruff check`)
10. McCabe complexity <=10 for all modified functions
