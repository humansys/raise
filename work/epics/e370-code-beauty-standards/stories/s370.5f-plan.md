# S370.5f Plan: Migrate Loaders to YAML

**Story:** S370.5f | **Size:** M | **Date:** 2026-03-06

## Tasks

### T1: Create core.yaml + rewrite identity loader (M)

**Risk:** High — most code change, regex → YAML rewrite.

**Files:**
- Create: `.raise/rai/identity/core.yaml`
- Rewrite: `src/raise_cli/context/loaders/identity.py`
- Delete: `.raise/rai/identity/core.md` (after core.yaml works)
- Modify: `tests/context/test_builder.py` (identity test fixtures → YAML)

**TDD:**
- RED: Update test fixtures to write `core.yaml` instead of `core.md`. Tests fail because loader still reads `.md`.
- GREEN: Rewrite `load_identity()` to read `core.yaml` with `yaml.safe_load()`. Same GraphNode IDs/types/metadata.
- REFACTOR: Remove `re` import, trim dead helper functions.

**Verify:** `uv run pytest tests/context/test_builder.py -k identity -x` + `uv run pyright` + `uv run ruff check`

**AC:** AC1, AC3

**Depends:** none

---

### T2: Migrate architecture docs to pure YAML + simplify loader (M)

**Risk:** Medium — many files to convert but mechanical transformation.

**Files:**
- Convert: `governance/architecture/*.md` → `*.yaml` (4 files)
- Convert: `governance/architecture/modules/*.md` → `*.yaml` (23 files)
- Rewrite: `src/raise_cli/context/loaders/architecture.py` (drop frontmatter extraction)
- Modify: `tests/context/test_builder.py` (architecture test fixtures → YAML)

**TDD:**
- RED: Update test fixtures to write `.yaml` instead of `.md` with frontmatter. Tests fail because loader still globs `*.md`.
- GREEN: Update `load_architecture()` to glob `*.yaml`, use `yaml.safe_load()` directly (no frontmatter strip). Same node output.
- REFACTOR: Remove frontmatter extraction code, simplify `_parse_architecture_doc`.

**Verify:** `uv run pytest tests/context/test_builder.py -k architecture -x` + `uv run pyright` + `uv run ruff check`

**AC:** AC2

**Depends:** none (parallel with T1)

---

### T3: Update onboarding to generate YAML (S)

**Risk:** Low — reference changes + template update.

**Files:**
- Modify: `src/raise_cli/onboarding/bootstrap.py` (`core.md` → `core.yaml`)
- Modify: `src/raise_cli/onboarding/instructions.py` (read YAML instead of parsing markdown)
- Modify: `src/raise_cli/rai_base/identity/__init__.py` (docstring reference)
- Modify: related tests if any

**TDD:**
- RED: Test that `rai init` scaffolds `core.yaml` (not `core.md`). Fails.
- GREEN: Update bootstrap to write `core.yaml`. Update instructions to read YAML.
- REFACTOR: Remove markdown parsing helpers from instructions.py.

**Verify:** `uv run pytest tests/ -k "init or onboarding or bootstrap or instruction" -x` + `uv run pyright` + `uv run ruff check`

**AC:** AC4

**Depends:** T1 (core.yaml schema must be finalized)

---

### T4: Full validation + cleanup (S)

**Risk:** Low — verification pass.

**Files:**
- Remove stale `.md` files replaced by YAML
- Any remaining `core.md` references in codebase

**Steps:**
1. `grep -rn "core\.md" src/ tests/` — verify zero references
2. `uv run pytest --tb=short` — full suite passes
3. `uv run pyright` — 0 errors
4. `uv run ruff check` — clean
5. Manual: `uv run rai graph build` — verify graph builds with YAML sources

**AC:** AC5, AC6

**Depends:** T1, T2, T3

---

## Execution Order

```
T1 (identity YAML) ──┐
                      ├──→ T3 (onboarding) ──→ T4 (validation)
T2 (architecture YAML)┘
```

T1 and T2 are independent — execute sequentially (T1 first, higher risk). T3 depends on T1's core.yaml schema. T4 is final validation.

## Risks

| Risk | Mitigation |
|------|-----------|
| Test fixtures tightly coupled to .md format | Update fixtures in same commit as loader change |
| Other code reads core.md directly | grep sweep in T4 catches stragglers |
| Architecture YAML conversion errors | Mechanical conversion, verify node count before/after |

## Duration Tracking

| Task | Estimate | Actual | Notes |
|------|----------|--------|-------|
| T1 | — | | |
| T2 | — | | |
| T3 | — | | |
| T4 | — | | |
