# S370.5b Plan: Extract Helpers

## Overview

9 tasks decomposing R4-R10 recommendations. Ordered risk-first: small independent fixes first (T1-T3), then architecture change (T4), then bulk extraction (T5-T8), then final verification (T9).

Total: 5-8 hours estimated.

## Tasks

### T1: R9 -- Fix ThreadPoolExecutor lifecycle (XS)

**Files:** `src/raise_cli/hooks/emitter.py`

**Description:** Replace manual `ThreadPoolExecutor` creation + `shutdown(wait=False)` in `_call_hook_with_timeout` with context manager pattern (`with ThreadPoolExecutor(...) as executor:`). Ensures proper thread cleanup on timeout/exception paths.

**TDD:** Existing tests suffice (pure refactor, no behavior change). Verify no resource leak warnings.

**Verification:** `uv run pytest tests/hooks/ -x -q`

**Size:** XS

**Dependencies:** None

---

### T2: R8 -- Add GovernanceParser Protocol (S)

**Files:**
- `src/raise_cli/governance/parsers/__init__.py` -- add Protocol definition
- `src/raise_cli/governance/extractor.py` -- type extractor to use Protocol, justify 11 `except Exception` clauses with inline comments

**Description:** Define `GovernanceParser` Protocol with `can_parse(locator) -> bool` and `parse(locator) -> list[GraphNode]`. All 9 existing parser classes (EpicScopeParser, AdrParser, RoadmapParser, VisionParser, ConstitutionParser, BacklogParser, PrdParser, GlossaryParser, GuardrailsParser) already conform -- no implementation changes needed. Add justifying comments to all `except Exception` clauses in extractor.py.

**TDD:** Write a test that verifies each parser class satisfies `isinstance` check against Protocol (RED: Protocol doesn't exist yet). GREEN: add Protocol. Then add comments to except clauses (refactor phase).

**Verification:** `uv run pytest tests/governance/ -x -q && uv run pyright src/raise_cli/governance/`

**Size:** S

**Dependencies:** None

---

### T3: R10 -- DRY adapters/mcp_jira.py (S)

**Files:** `src/raise_cli/adapters/mcp_jira.py`

**Description:** Extract shared `_extract_issue_fields(data: dict, is_flat: bool) -> dict` helper. Both `_parse_issue_detail` and `_parse_search_results` call this instead of duplicating flat/nested branching logic.

**TDD:** Existing tests cover both parsers. Write a unit test for the new `_extract_issue_fields` helper (RED), implement (GREEN), then refactor callers.

**Verification:** `uv run pytest tests/adapters/ -x -q`

**Size:** S

**Dependencies:** None

---

### T4: R4 -- Remove _current_output_format global (S)

**Files:**
- `src/raise_cli/cli/main.py` -- remove `_current_output_format` global and `get_output_format()`
- `src/raise_cli/__main__.py` -- update error handler call
- `src/raise_cli/exceptions.py` -- possibly add `output_format` to `RaiError`

**Description:** The `_current_output_format` module-level global (line 35 of main.py) is set by `main()` callback and read by `__main__.py` error handler. Simplest fix: store format on `RaiError` so the error handler reads `error.output_format` directly, or use a ContextVar. Analyze options during implementation; if too risky, document the global with justifying comment and defer.

**TDD:** Existing tests suffice for the happy path. Add a test verifying the error handler gets the correct format from the error object (RED/GREEN).

**Verification:** `uv run pytest tests/cli/ -x -q && uv run pyright src/raise_cli/cli/`

**Size:** S

**Dependencies:** None (but riskiest standalone task -- do after T1-T3 warmup)

---

### T5: R6 -- Extract governance parser helpers (M)

**Files:**
- `src/raise_cli/governance/extractor.py` -- `extract_with_result` (104 lines)
- `src/raise_cli/governance/parsers/epic.py` -- `extract_epic_details` (107), `extract_stories` (126)
- `src/raise_cli/governance/parsers/backlog.py` -- `extract_epics` (127)

**Description:** Extract helpers per design inventory:
- `extractor.py`: refactor `extract_with_result` to `_extract_from_single_file` loop
- `epic.py`: extract `_parse_epic_id_and_name`, `_build_epic_content`, `_parse_story_columns`
- `backlog.py`: extract `_should_yield_to_scope_parser`, `_build_epic_concept`

Target: no function >100 lines in governance.

**TDD:** Existing tests suffice (pure extraction, behavior unchanged).

**Verification:** `uv run pytest tests/governance/ -x -q && uv run ruff check src/raise_cli/governance/`

**Size:** M

**Dependencies:** T2 (Protocol should be in place before modifying parsers)

---

### T6: R5 -- Extract CLI command helpers batch 1 (M)

**Files:**
- `src/raise_cli/cli/commands/init.py` -- `init_command` (321), `_get_project_message` (147)
- `src/raise_cli/cli/commands/session.py` -- `close` (236), `start` (152)
- `src/raise_cli/cli/commands/signal.py` -- `emit_work` (177), `emit_session` (89), `emit_calibration` (97)

**Description:** Extract helpers per design inventory. These are the largest CLI functions:
- `init.py`: `_init_developer_profile`, `_create_and_save_manifest`, `_bootstrap_and_scaffold`, `_generate_output`, `_detect_and_generate_guardrails`, `_format_shu_project_message`, `_format_ri_project_message`
- `session.py`: `_close_legacy_session`, `_close_structured_session`, `_setup_new_session`, `_emit_and_output_start`
- `signal.py`: `_validate_work_inputs`, `_format_work_result`

Target: no function >100 lines.

**TDD:** Existing tests suffice (pure extraction).

**Verification:** `uv run pytest tests/cli/ -x -q && uv run ruff check src/raise_cli/cli/commands/`

**Size:** M

**Dependencies:** None (but logically after T4 since T4 touches cli/main.py)

---

### T7: R5 -- Extract CLI command helpers batch 2 (M)

**Files:**
- `src/raise_cli/cli/commands/graph.py` -- `query` (139), `extract` (122), `validate` (99), `list_graph` (99), `build` (85)
- `src/raise_cli/cli/commands/discover.py` -- `drift_command` (147), `analyze_command` (131), `build_command` (118), `scan_command` (103)
- `src/raise_cli/cli/commands/release.py` -- `publish_command` (141)

**Description:** Extract helpers per design inventory:
- `graph.py`: `_parse_query_options`, `_format_query_output`, `_format_single_file_extract`, `_format_all_files_extract`, `_check_completeness`, `_format_list_output`
- `discover.py`: `_load_baseline`, `_check_scan_path`, `_load_scan_input`, `_load_category_map`, `_load_and_validate_components`, `_validate_language`
- `release.py`: `_display_release_plan`, `_execute_release`

Target: no function >100 lines.

**TDD:** Existing tests suffice (pure extraction).

**Verification:** `uv run pytest tests/cli/ -x -q && uv run ruff check src/raise_cli/cli/commands/`

**Size:** M

**Dependencies:** None (but logically after T6 to batch CLI work)

---

### T8: R7 -- Extract scanner nested walk() functions (M)

**Files:** `src/raise_cli/discovery/scanner.py`

**Description:** Extract 4 nested `walk()` closures to module-level functions:
- `_extract_ts_js_symbols` (165 lines) -> extract `walk()` to `_walk_ts_js_node`
- `_extract_php_symbols` (127 lines) -> extract `walk()` to `_walk_php_node`
- `_extract_csharp_symbols_from_tree` (122 lines) -> extract `walk()` to `_walk_csharp_node`
- `_extract_dart_symbols_from_tree` (107 lines) -> extract `walk()` to `_walk_dart_node`

The nested `walk()` functions use closure variables (symbols list, parent tracking). When extracting, pass these as parameters or use a small dataclass to bundle walk state.

Target: no function >100 lines in scanner.py.

**TDD:** Existing tests suffice (pure extraction, behavior unchanged). Scanner has extensive language-specific tests.

**Verification:** `uv run pytest tests/discovery/ -x -q && uv run ruff check src/raise_cli/discovery/scanner.py`

**Size:** M

**Dependencies:** None

---

### T9: Integration verification (XS)

**Description:** Full validation pass across all modified files.

**Verification:**
```bash
uv run pytest -x -q
uv run pyright src/raise_cli/
uv run ruff check src/raise_cli/
uv run ruff check --select C901 src/raise_cli/  # McCabe <=10
python3 -c "..." # line count script to verify no function >100 lines
```

**Size:** XS

**Dependencies:** T1-T8 all complete

---

## Execution Order

```
T1 (R9, XS) ──┐
T2 (R8, S) ───┤── independent warmup
T3 (R10, S) ──┘
      │
T4 (R4, S) ──── architecture change (riskiest standalone)
      │
T5 (R6, M) ──── governance helpers (depends on T2)
      │
T6 (R5, M) ──┐
T7 (R5, M) ──┤── CLI helpers (two batches)
      │       │
T8 (R7, M) ──┘── scanner extraction (independent)
      │
T9 (XS) ──── final integration verification
```

## Risk Mitigation

| Risk | Mitigation |
|------|-----------|
| T4 global removal breaks error handling | Spike first, defer with comment if too risky |
| Scanner walk() extraction breaks closure state | Pass state explicitly, test each language |
| Large CLI functions have hidden side effects | Existing test coverage + commit after each extraction |

## Acceptance Criteria Mapping

| AC | Task(s) |
|----|---------|
| AC1: No CLI function >100 lines | T6, T7 |
| AC2: No governance function >100 lines | T5 |
| AC3: No scanner function >100 lines | T8 |
| AC4: ThreadPoolExecutor context manager | T1 |
| AC5: mcp_jira flat/nested consolidated | T3 |
| AC6: All tests pass | T9 |
| AC7: Type checks pass | T9 |
| AC8: Lint passes | T9 |
| SC1: GovernanceParser Protocol | T2 |
| SC2: except Exception justified | T2 |
| SC3: McCabe <=10 | T9 |
