# Story Scope: S370.2 — Expand Tooling Gates

## In Scope

- Ruff config expansion: +11 rule sets (S, C90, PL, PERF, FURB, RET, ARG, ERA, PT, TC, A)
- import-linter setup with ADR-001 layer contracts
- McCabe complexity cap: max-complexity = 10
- Fix all violations introduced by new rules (incremental, one rule set at a time)
- Update pyproject.toml with all new tool configuration

## Out of Scope

- wemake-python-styleguide (Tier 3 — deferred)
- Radon / Vulture (Tier 3 — deferred)
- New features or CLI commands
- Docstring additions (D rules deferred to audit phase)
- Performance optimization beyond what PERF rules flag
- Architectural changes (violations found by import-linter are fixed by adjusting imports, not redesigning layers)

## Done When

1. All 11 new Ruff rule sets enabled with zero violations
2. import-linter contracts configured and green
3. McCabe max-complexity = 10 passes for all functions
4. All tests pass (`uv run pytest`)
5. Type checks pass (`uv run pyright`)
6. Lint passes (`uv run ruff check`)

## Dependencies

- S370.1 (Draft Code Standard) — completed
- ADR-001 (three-layer architecture) — defines layer contracts

## Risks

| Risk | Likelihood | Mitigation |
|------|-----------|-----------|
| Hundreds of violations from new rules | Medium | One rule set at a time; fix incrementally |
| import-linter reveals unexpected cross-layer imports | Low | Fix imports, not architecture; escalate if systemic |
| Some rules conflict with existing conventions | Low | Use per-rule ignores with documented rationale |
