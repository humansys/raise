---
title: "Hello, World"
description: "Testing the blog infrastructure — this post validates MDX rendering, code blocks, and content collections."
pubDate: 2026-02-13
tags: ["test", "infrastructure"]
draft: true
hypothesis: "Blog infrastructure renders MDX correctly with styled prose"
---

# Hello, World

This is a test post to validate the blog pipeline. If you can read this, content collections and MDX rendering are working.

## What we're testing

- Markdown rendering with prose styling
- Code blocks with syntax highlighting
- Frontmatter metadata (title, date, tags)
- Content collection querying and routing

## Code block test

```python
from raise_core import Memory

memory = Memory.load()
patterns = memory.query("testing", types=["pattern"], limit=5)
print(f"Found {len(patterns)} patterns")
```

## The stack

This blog runs on **Astro** with MDX content collections, deployed on **Cloudflare Pages**. Zero JavaScript on the client unless a component explicitly needs it.

That's the way it should be.
