---
title: Referencia CLI
description: Referencia completa de todos los comandos del CLI de RaiSE — flags, opciones y ejemplos.
---

Referencia completa de la interfaz de línea de comandos `rai`.

## Opciones Globales

Estas opciones están disponibles en todos los comandos:

| Flag | Corto | Descripción |
|------|-------|-------------|
| `--version` | `-V` | Mostrar versión y salir |
| `--format` | `-f` | Formato de salida: `human`, `json` o `table` |
| `--verbose` | `-v` | Aumentar verbosidad (`-v`, `-vv`, `-vvv`) |
| `--quiet` | `-q` | Suprimir salida no-error |
| `--help` | | Mostrar ayuda y salir |

---

## Proyecto

### `rai init`

Inicializa un proyecto RaiSE en el directorio actual. Detecta el tipo de proyecto (greenfield o brownfield), crea `.raise/manifest.yaml` y configura la estructura del proyecto.

Con `--detect`, también analiza las convenciones de código y genera guardrails.

| Flag | Corto | Descripción |
|------|-------|-------------|
| `--name` | `-n` | Nombre del proyecto (por defecto: nombre del directorio) |
| `--path` | `-p` | Ruta del proyecto (por defecto: directorio actual) |
| `--detect` | `-d` | Detectar convenciones y generar `guardrails.md` |

```bash
# Proyecto nuevo
rai init

# Proyecto con nombre
rai init --name mi-api

# Proyecto existente con detección de convenciones
rai init --detect
```

---

## Sesión

### `rai session start`

Inicia una nueva sesión de trabajo. Incrementa el contador de sesiones y establece el estado de sesión activa. Verifica sesiones huérfanas (iniciadas pero no cerradas) y avisa si las encuentra.

Con `--context`, genera un bundle de contexto optimizado en tokens (~150 tokens) ensamblado desde tu perfil de desarrollador, estado de sesión y grafo de memoria.

| Flag | Corto | Descripción |
|------|-------|-------------|
| `--name` | `-n` | Tu nombre (requerido en la primera configuración) |
| `--project` | `-p` | Ruta del proyecto a asociar con esta sesión |
| `--agent` | | Identificador de agente |
| `--context` | | Generar bundle de contexto para consumo de IA |

```bash
# Primera configuración
rai session start --name "Alice" --project .

# Iniciar sesión con bundle de contexto
rai session start --project . --context

# Inicio simple
rai session start
```

### `rai session close`

Finaliza la sesión de trabajo actual. Con `--summary` o `--state-file`, realiza un cierre estructurado completo — registra sesión, patrones, correcciones y actualiza el estado.

| Flag | Corto | Descripción |
|------|-------|-------------|
| `--summary` | `-s` | Resumen de la sesión |
| `--type` | `-t` | Tipo de sesión (feature, research, maintenance, etc.) |
| `--pattern` | | Descripción del patrón a registrar (repetible) |
| `--state-file` | | Archivo YAML con salida estructurada completa |
| `--session` | | Identificador de sesión |

```bash
# Cierre simple
rai session close

# Cierre con resumen
rai session close --summary "Implementé módulo de auth" --type feature

# Cierre con patrón aprendido
rai session close --summary "Refactoricé tests" --type maintenance \
  --pattern "Usar fixtures para setup de base de datos"
```

### `rai session context`

Carga secciones específicas de contexto para consumo de IA.

| Flag | Corto | Descripción |
|------|-------|-------------|
| `--sections` | `-s` | Secciones a incluir (governance, behavioral, coaching, deadlines, progress) |
| `--project` | `-p` | Ruta del proyecto |

```bash
rai session context --sections governance,progress --project .
```

### `rai session journal`

Memoria incremental de sesión para decisiones, insights y tareas.

```bash
# Agregar una decisión al journal
rai session journal add "Elegimos Pydantic sobre dataclasses" --type decision

# Mostrar el journal
rai session journal show

# Vista compacta (para restaurar post-compactación)
rai session journal show --compact
```

---

## Graph

### `rai graph build`

Construye el índice del knowledge graph desde todas las fuentes: documentos de gobernanza, memoria (patrones, calibración, sesiones), seguimiento de trabajo (epics, stories), skills y componentes de discovery.

| Flag | Corto | Descripción |
|------|-------|-------------|
| `--output` | `-o` | Ruta para guardar el JSON del índice |
| `--no-diff` | | Omitir salida de diff |

```bash
# Construir índice en ubicación por defecto
rai graph build

# Guardar en ubicación personalizada
rai graph build --output custom_index.json
```

### `rai graph query`

Busca en el knowledge graph conceptos relevantes. El grafo contiene todas las fuentes de contexto: gobernanza (principios, requisitos, términos), memoria (patrones, calibración, sesiones), skills (metadatos de workflow) y trabajo (epics, stories, decisiones).

| Flag | Corto | Descripción |
|------|-------|-------------|
| `--format` | `-f` | Formato de salida: `human`, `json` o `compact` |
| `--strategy` | `-s` | Estrategia de búsqueda: `keyword_search` o `concept_lookup` |
| `--types` | `-t` | Filtrar por tipos (separados por coma: `pattern`, `calibration`, `principle`, etc.) |
| `--limit` | `-l` | Número máximo de resultados (por defecto: 10) |

```bash
# Buscar por palabras clave
rai graph query "planning estimation"

# Filtrar solo patrones
rai graph query "testing" --types pattern,calibration

# Buscar concepto específico por ID
rai graph query "PAT-001" --strategy concept_lookup

# Salida en JSON
rai graph query "velocity" --format json
```

### `rai graph context`

Muestra el contexto arquitectónico completo de un módulo. Retorna el bounded context (dominio), capa arquitectónica, guardrails aplicables (restricciones) y dependencias del módulo.

| Flag | Corto | Descripción |
|------|-------|-------------|
| `--format` | `-f` | Formato de salida: `human` o `json` |

```bash
# Mostrar contexto del módulo de memoria
rai graph context mod-memory

# Salida en JSON
rai graph context mod-memory --format json
```

### `rai graph validate`

Valida la estructura y relaciones del índice del grafo. Verifica ciclos en relaciones `depends_on`, tipos de relación válidos y que todos los targets de edges existan como nodos.

```bash
rai graph validate
```

### `rai graph extract`

Extrae conceptos de archivos markdown de gobernanza.

```bash
# Extraer de todos los archivos de gobernanza
rai graph extract

# Extraer de archivo específico
rai graph extract governance/prd.md
```

### `rai graph list`

Lista los conceptos en el knowledge graph.

| Flag | Corto | Descripción |
|------|-------|-------------|
| `--format` | `-f` | Formato de salida: `human`, `json` o `table` |
| `--memory-only` / `--all` | | Mostrar solo tipos de memoria o todos |

```bash
# Mostrar tabla resumen
rai graph list

# Mostrar solo patrones/calibraciones/sesiones
rai graph list --memory-only
```

### `rai graph viz`

Genera una visualización HTML interactiva del knowledge graph.

```bash
rai graph viz
```

---

## Pattern

### `rai pattern add`

Agrega un nuevo patrón a la memoria. Los patrones capturan aprendizajes del desarrollo — mejoras de proceso, descubrimientos técnicos, decisiones arquitectónicas.

| Flag | Corto | Descripción |
|------|-------|-------------|
| `--context` | `-c` | Palabras clave de contexto (separadas por coma) |
| `--type` | `-t` | Tipo de patrón: `codebase`, `process`, `architecture`, `technical` (por defecto: `process`) |
| `--from` | `-f` | Story/sesión donde se aprendió |
| `--scope` | `-s` | Alcance de memoria: `global`, `project`, `personal` (por defecto: `project`) |

```bash
# Agregar patrón de proceso
rai pattern add "HITL before commits" -c "git,workflow"

# Agregar patrón técnico
rai pattern add "Use capsys for stdout tests" -t technical -c "pytest,testing"

# Agregar con referencia de origen
rai pattern add "BFS reuse across modules" -t architecture --from S2.3
```

### `rai pattern reinforce`

Refuerza un patrón con una señal de voto.

```bash
rai pattern reinforce PAT-042
```

### `rai pattern promote`

Promueve un patrón de scope personal a scope de proyecto.

```bash
rai pattern promote PAT-042
```

---

## Signal

### `rai signal emit-work`

Emite un evento de ciclo de vida de trabajo para análisis de flujo Lean. Registra ítems de trabajo (epics, stories) a través de fases normalizadas.

| Flag | Corto | Descripción |
|------|-------|-------------|
| `--event` | `-e` | Tipo de evento: `start`, `complete`, `blocked`, `unblocked`, `abandoned` |
| `--phase` | `-p` | Fase: `design`, `plan`, `implement`, `review` |

```bash
# Ciclo de vida de story
rai signal emit-work story S9.4 --event start --phase implement
rai signal emit-work story S9.4 -e complete -p implement
```

### `rai signal emit-session`

Emite un evento de sesión a telemetría.

| Flag | Corto | Descripción |
|------|-------|-------------|
| `--type` | `-t` | Tipo de sesión: `story`, `research`, `maintenance`, etc. |
| `--outcome` | `-o` | Resultado: `success`, `partial`, `abandoned` |
| `--duration` | `-d` | Duración de la sesión en minutos |
| `--stories` | `-f` | Stories trabajadas (separadas por coma) |

```bash
rai signal emit-session --type story --outcome success -d 45 --stories S9.1,S9.2
```

### `rai signal emit-calibration`

Emite un evento de calibración a telemetría. Registra estimación vs. real para seguimiento de velocidad.

| Flag | Corto | Descripción |
|------|-------|-------------|
| `--size` | `-s` | Talla: `XS`, `S`, `M`, `L` |
| `--estimated` | `-e` | Duración estimada en minutos |
| `--actual` | `-a` | Duración real en minutos |

```bash
rai signal emit-calibration S9.4 --size S --estimated 30 --actual 15
```

---

## Discovery

### `rai discover scan`

Escanea un directorio y extrae símbolos de código (clases, funciones, métodos, interfaces, docstrings de módulos). Soporta Python, TypeScript y JavaScript.

| Flag | Corto | Descripción |
|------|-------|-------------|
| `--language` | `-l` | Lenguaje: `python`, `typescript`, `javascript` |
| `--output` | `-o` | Formato de salida: `human`, `json` o `summary` |
| `--exclude` | `-e` | Patrones a excluir (repetible) |

```bash
# Escanear directorio actual
rai discover scan

# Escanear solo archivos Python
rai discover scan src/ --language python

# Excluir tests
rai discover scan . --exclude "**/test_*" --exclude "**/__tests__/**"
```

### `rai discover drift`

Verifica drift arquitectónico contra el baseline de componentes.

```bash
rai discover drift
```

---

## Skills

### `rai skill list`

Lista todos los skills en el directorio de skills. Muestra skills agrupados por ciclo de vida con versión y descripción.

```bash
rai skill list
```

### `rai skill validate`

Valida la estructura de un skill contra el schema de RaiSE.

```bash
# Validar todos los skills
rai skill validate

# Validar skill específico
rai skill validate .claude/skills/story-start/SKILL.md
```

### `rai skill scaffold`

Crea un nuevo skill desde plantilla.

```bash
rai skill scaffold story-validate --lifecycle story --after story-implement --before story-close
```

### `rai skill check-name`

Verifica un nombre propuesto de skill contra convenciones de naming.

```bash
rai skill check-name story-validate
```

---

## Profile

### `rai profile show`

Muestra el perfil de desarrollador en formato YAML. Muestra el contenido de `~/.rai/developer.yaml`.

```bash
rai profile show
```

---

## Backlog

Gestiona ítems de backlog vía adaptadores de gestión de proyectos (ej. Jira).

### `rai backlog create`

Crea un nuevo ítem de backlog. SUMMARY es posicional, proyecto es el flag `-p`.

```bash
rai backlog create "Agregar autenticación de usuario" -p MIPROYECTO -t Story
```

### `rai backlog search`

Busca ítems de backlog. QUERY es posicional (JQL para Jira).

```bash
rai backlog search "project = MIPROYECTO AND status = 'In Progress'" -n 10
```

### `rai backlog get`

Obtiene detalles de un issue específico.

```bash
rai backlog get PROJ-123
```

### `rai backlog transition`

Transiciona un issue a un nuevo estado. Ambos argumentos son posicionales.

```bash
rai backlog transition PROJ-123 "In Progress"
```

### `rai backlog comment`

Agrega un comentario a un issue. Ambos argumentos son posicionales.

```bash
rai backlog comment PROJ-123 "Implementación completa, listo para review"
```

### `rai backlog link`

Vincula dos issues. Los tres argumentos son posicionales.

```bash
rai backlog link PROJ-123 PROJ-456 blocks
```

---

## Gate

### `rai gate list`

Lista los gates de workflow descubiertos.

```bash
rai gate list
```

### `rai gate check`

Ejecuta verificaciones de gates de workflow. Código de salida 0 si todos pasan, 1 si alguno falla.

```bash
# Verificar un gate específico
rai gate check pre-commit

# Verificar todos los gates
rai gate check --all
```

---

## Adapter

### `rai adapter list`

Lista adaptadores registrados por entry point.

```bash
rai adapter list
```

### `rai adapter check`

Valida adaptadores contra contratos Protocol.

```bash
rai adapter check
```

---

## MCP

Gestiona e invoca servidores MCP (Model Context Protocol) registrados en `.raise/mcp/`.

### `rai mcp list`

Lista servidores MCP registrados.

```bash
rai mcp list
```

### `rai mcp health`

Verifica el estado de un servidor MCP. SERVER es posicional.

```bash
rai mcp health mi-servidor
```

### `rai mcp tools`

Lista herramientas disponibles en un servidor MCP.

```bash
rai mcp tools mi-servidor
```

### `rai mcp call`

Llama a una herramienta en un servidor MCP. SERVER y TOOL son posicionales.

```bash
rai mcp call mi-servidor search --args '{"query": "test"}'
```

### `rai mcp install`

Instala un paquete de servidor MCP.

```bash
rai mcp install some-package --type uvx --name mi-servidor
```

### `rai mcp scaffold`

Genera configuración para un nuevo servidor MCP.

```bash
rai mcp scaffold mi-servidor --command "uvx some-tool"
```

---

## Release

### `rai release check`

Ejecuta gates de calidad antes del release.

```bash
rai release check
```

### `rai release publish`

Publica un release con bumping de versión.

```bash
# Bump de versión menor
rai release publish --bump minor

# Versión específica
rai release publish --version 2.3.0

# Dry run
rai release publish --bump patch --dry-run
```

---

## Docs

Gestiona documentación de gobernanza vía targets de documentación (ej. Confluence).

### `rai docs publish`

Publica un artefacto a un target de documentación.

```bash
rai docs publish roadmap --title "Roadmap Q1"
```

### `rai docs get`

Obtiene una página de un target de documentación.

```bash
rai docs get 12345
```

### `rai docs search`

Busca en targets de documentación.

```bash
rai docs search "decisiones de arquitectura" -n 5
```

---

## Doctor

### `rai doctor`

Diagnostica la configuración de RaiSE, reporta problemas y auto-corrige issues comunes.

```bash
rai doctor
```

---

## Base

### `rai base show`

Muestra información del paquete base de Rai. Muestra la versión del base incluido, contenidos (identidad, patrones, metodología) y si ha sido instalado en el proyecto actual.

```bash
rai base show
```
