# Rai Memory — raise-commons

> Permanent knowledge for this project. Loaded into system prompt.

---

## Key Patterns (from memory)

- **PAT-146:** Three-tier data architecture: Global (~/.rai) for cross-repo state, Project (.raise/rai/memory) for shared artifacts, Personal (.raise/rai/personal) for developer-specific data. Separation enables multi-developer repos without conflicts.
- **PAT-147:** Precedence via metadata: Store scope (global/project/personal) in node metadata, apply precedence at query time. Avoids data duplication while enabling filtering and override semantics.
- **PAT-148:** Research before L-sized features: Two focused research sessions (pattern analysis + impact analysis) before implementation prevents design churn on large features. Investment pays off in cleaner architecture.
- **PAT-149:** Single source of truth: remove redundant counters when authoritative data exists. Session count in profile drifted from index (73 vs 70) — eliminated counter, derive from index only.
- **PAT-150:** Drift review before implementation catches stale scopes - path references, architecture changes, and scope creep accumulate. Review epic/story scope against current state before coding.
- **PAT-151:** Large-scale renames have a long tail — plan for 3 passes: batch (80%), targeted (15%), comprehensive audit (5%). Multiple cleanup commits are normal.
- **PAT-152:** Schema Literal changes invalidate cached graphs — always rebuild indexes (raise memory build) after renaming Pydantic Literal types or enum values.
- **PAT-153:** JSONL backward compat pattern: read new key first, fall back to old key. Historical entries are immutable — the reader must handle both old and new schemas.
- **PAT-154:** Design gate for M+ stories catches integration decisions early — 5 decisions in S14.4 would have caused rework without design phase
- **PAT-155:** Use importlib.resources.abc.Traversable (not importlib.abc.Traversable) — the latter is deprecated in Python 3.14

---

*Last updated: 2026-02-06*
*Generated by `raise memory generate`*
